# 五大联赛拓展开发计划（可执行任务清单）

> 对应方案：《五大联赛拓展方案.md》
> 总工期：约 40 人日（1 人全职约 8 周；2 人并行约 4~5 周）
> 阶段依赖：M0 → M1 → M2 → M3 → M4，主线串行；M3 内部页面任务可并行

---

## 总览

| 阶段 | 内容 | 预估 | 依赖 |
|---|---|---|---|
| M0 | 基线修复：恢复前端构建、确认后端测试基线 | 0.5 人日 | 无 |
| M1 | 新增表与可空列（纯新增）+ 赛事元数据 + 兼容性验收 | 9 人日 | M0 |
| M2 | 联赛同步 + 联赛积分榜 + 管理后台（世界杯路径零改动） | 9 人日 | M1 |
> 2026-08-04 已完成，见下方任务状态 |
| M3 | 前端赛事切换 + 联赛页面叠加（默认世界杯） | 15 人日 | M1（可与 M2 部分并行） |
> 2026-08-04 已完成，见下方任务状态 |
| M4 | 跨赛事聚合、关注分组、打磨、上线 | 6.5 人日 | M2、M3 |
> 2026-08-04 已完成，见下方任务状态 |

**统一约定**

- **扩展原则（硬性）**：现有表结构/索引/约束/数据零改动，世界杯功能零改动，API 向后兼容，前端默认世界杯——任何任务违反此原则即不通过验收。
- 赛季字段 `season` 存起始年（int）：2026 世界杯 = `2026`；2025-26 英超 = `2025`
- football-data 联赛代码：`PL`(英超)、`PD`(西甲)、`BL1`(德甲)、`SA`(意甲)、`FL1`(法甲)、`WC`(世界杯)
- 接口返回保持 `{code, message, data}` 结构；新增查询参数全部可选，缺省时行为与改造前完全一致
- GORM `AutoMigrate` 只增不删：新增表/列不会影响现有表

---

## M0 基线修复（0.5 人日）

### M0-1 恢复前端生产构建
- **状态**：✅ 已完成（2026-08-04：`npm run build` 直接通过，README 记录的两处 TS 类型错误已不存在，无需改动）
- **涉及文件**：`frontend/src/data/mockUser.ts`、`frontend/src/pages/user/ProfilePage.vue`、`frontend/src/types/user.ts`
- **内容**：修复 README 记录的 2 处 TS 类型错误（`mockUser.ts` 缺少 `User.notificationEmail`；`ProfilePage.vue` 使用未声明的 `email`）。**仅修类型，不改功能**
- **验收**：`cd frontend && npm run build` 通过；`git diff` 确认无功能改动
- **预估**：0.5 人日

### M0-2 记录数据库基线（供 M1 兼容性验收比对）
- **状态**：✅ 已完成（2026-08-04：已生成 `docs/db-baseline.sql`，含全部 12 张现有表结构，取自 `worldcup_mate.sql` Navicat dump（MySQL 8.0.32）；本机无 MySQL 环境，实时 `SHOW CREATE TABLE` 留待有库环境时复核）
- **涉及文件**：无（执行 SQL）
- **内容**：对全部现有表执行 `SHOW CREATE TABLE` 存档（建议存 `docs/db-baseline.sql`），作为 M1 完成后表结构 diff 对比基线
- **验收**：基线文件入库（git commit）
- **预估**：0 人日（并入 M0-1）

---

## M1 新增表与可空列（9 人日）

> 本阶段**不修改**任何现有表的列、索引、约束；不修改任何老数据。

### M1-1 新增 Competition 模型与表（纯新增）
- **状态**：✅ 已完成（2026-08-04：新增 `backend/internal/models/competition.go`；已注册到 `main.go` 的 `AutoMigrate` 列表）
- **涉及文件**：`backend/internal/models/competition.go`（新建）
- **内容**：`Competition{ID, Code(unique), Name, NameEn, Country, LogoURL, Format(league/cup), Season, Status, SortOrder, CreatedAt, UpdatedAt}`
- **验收**：`AutoMigrate` 后新增 `competitions` 表；**现有表结构 diff 为空**（与 M0-2 基线一致）
- **预估**：0.5 人日

### M1-2 matches 表仅新增可空列
- **状态**：✅ 已完成（2026-08-04：`match.go` 新增 `CompetitionID/Season/Matchday` 三个可空指针字段，现有字段与索引未动）
- **涉及文件**：`backend/internal/models/match.go`
- **内容**：新增 `CompetitionID *uint(index)`、`Season *int(index)`、`Matchday *int(index)` 三个**可空**字段。**现有字段（GroupID/Stage/MatchNo/StadiumID/CityID 等）及索引、约束一律不动**
- **验收**：`SHOW CREATE TABLE matches` 与基线对比 = 仅多出 3 个可空列 + 其索引；现有世界杯比赛数据零变化；`GET /api/matches` 行为不变
- **预估**：0.5 人日

### M1-3 teams 表仅新增可空列
- **状态**：✅ 已完成（2026-08-04：`FIFACode`/`GroupID` 指针化——数据库层本就可空（dump 证据），表结构/索引/约束零改动；新增 `ExternalCode/TeamType/Country/Venue`；连带适配 `seed.go`、`sync_service.go`、`team_localization.go`、`admin_handler.go` 的指针赋值）
- **涉及文件**：`backend/internal/models/team.go`
- **内容**：新增 `ExternalCode *string(index)`（俱乐部 tla）、`TeamType string(default national)`、`Country`、`Venue` 四个字段。**`fifa_code` 唯一索引原样保留**（俱乐部该列存 NULL，不参与唯一约束）
- **验收**：`SHOW CREATE TABLE teams` 与基线对比 = 仅多出新列；国家队数据零变化；插入两个相同 tla 的俱乐部不冲突、插入 NULL fifa_code 俱乐部成功
- **预估**：0.5 人日

### M1-4 新增 LeagueStanding 模型与表（纯新增）
- **状态**：✅ 已完成（2026-08-04：新增 `backend/internal/models/league_standing.go`，含 `Team` 关联；已注册 `AutoMigrate`）
- **涉及文件**：`backend/internal/models/league_standing.go`（新建）
- **内容**：`LeagueStanding{ID, CompetitionID, Season, TeamID, Type(total/home/away), Position, Played, Won, Drawn, Lost, GoalsFor, GoalsAgainst, GoalDifference, Points, Zone, UpdatedAt}`；唯一键 `(competition_id, season, type, team_id)`
- **验收**：`AutoMigrate` 后新增 `league_standings` 表；**`group_standings` 表及 `RecalculateGroupStanding`/`RecalculateBestThird` 零改动**
- **预估**：0.5 人日

### M1-5 赛事元数据 seed（不回溯老数据）
- **状态**：✅ 已完成（2026-08-04：`seedCompetitions()` 写入 WC + 五大联赛 6 条元数据；`main.go` `AutoMigrate` 注册 `Competition`/`LeagueStanding`；老数据零回溯）
- **涉及文件**：`backend/internal/database/seed.go`
- **内容**：seed 中**新增** `competitions` 表数据：`WC`(format=cup, season=2026) + 五大联赛 5 条（format=league）。**不修改**现有小组/球队/比赛 seed；**不回溯**修改存量数据（老 matches/teams 的 `competition_id` 保持 NULL，NULL 即默认世界杯）
- **验收**：全新库启动后 `competitions` 表 6 行；现有世界杯页面/接口表现与改造前一致
- **预估**：0.5 人日

### M1-6 新增 Competition 仓储与接口（纯新增）
- **状态**：✅ 已完成（2026-08-04：新增 `competition_repo.go`、`competition_handler.go`（公开列表 + admin CRUD）；`router.go` 注册公开 `/api/competitions` 与 admin CRUD）
- **涉及文件**：`backend/internal/repositories/competition_repo.go`（新建）、`backend/internal/handlers/competition_handler.go`（新建）、`backend/internal/routes/router.go`
- **内容**：`GET /api/competitions`（公开，启用中的赛事按 sort_order 排序）；admin CRUD（列表/创建/更新/启停）
- **验收**：`curl /api/competitions` 返回 6 个赛事；admin 可新增/停用赛事；现有路由零修改（仅追加）
- **预估**：1 人日

### M1-7 repositories 增加可选过滤（不改现有方法签名行为）
- **状态**：✅ 已完成（2026-08-04：`MatchFilter` 新增可选 `CompetitionID/Season/Matchday`（零值不过滤）；新增 `ListTeamsFiltered`（`TeamFilter` 用 `teamType`/`country` 过滤——**teams 表无 `competition_id` 列**，按 review 建议取消了该过滤，避免 500）；新增 `GetLeagueStandings`）
- **涉及文件**：`backend/internal/repositories/match_repo.go`、`team_repo.go`
- **内容**：`MatchFilter` 增加**可选**字段 `CompetitionID/Season/Matchday`（零值 = 不过滤，走原逻辑）；新增 `GetLeagueStandings(filter)` 查询 `league_standings`。**现有查询方法与调用方不改写**
- **验收**：不带新参数的查询 SQL 与改造前完全一致（可用 SQL 日志对比）；带参数时正确过滤
- **预估**：1.5 人日

### M1-8 handlers 增加可选参数透传（缺省 = 原行为）
- **状态**：✅ 已完成（2026-08-04：`MatchQuery` 新增可选 `competitionId/season/matchday`（`ShouldBindQuery` 自动绑定，零值不走过滤）；新增 `GET /api/competitions/:code/standings`（`season`/`type` 可选，code 大小写不敏感）；`today/tomorrow/upcoming/live/recommended` 等快捷接口保持原样，联赛版留 M3/M4）
- **涉及文件**：`backend/internal/handlers/match_handler.go`、`team_handler.go`、`standing_handler.go`；`backend/internal/services/match_service.go`、`team_service.go`
- **内容**：现有查询接口解析可选 `competition_id`/`season`/`matchday` 参数，**仅当非空时**透传给 repo 过滤；缺省时走原逻辑（返回世界杯数据）。新增 `GET /api/competitions/:code/standings`（读 `league_standings`）
- **验收**：不带参数请求响应与改造前逐字段一致（回归用例）；`GET /api/matches?competition_id=X&season=2025&matchday=5` 返回正确子集
- **预估**：2 人日

### M1-9 兼容性验收（表结构 + 功能回归）
- **状态**：✅ 已完成（2026-08-04：`go build`/`go vet`/`go test ./...` 全绿；review 子代理审查通过，修复 1 个阻塞问题（teams 表按 `competition_id` 过滤会 500，已移除）；表结构兼容性经基线 `docs/db-baseline.sql` 与 dump 证据核验：仅新增表/可空列/索引，现有列与约束零改动；世界杯路径（小组积分/最佳第三名/种子/同步）代码未改）
- **涉及文件**：全后端
- **内容**：执行 `SHOW CREATE TABLE` 与 M0-2 基线 diff（仅允许新增表/新增可空列/新索引）；`go test ./...` 全绿；世界杯全接口回归（matches/teams/groups/standings/best-third/cities/stadiums）
- **验收**：diff 结果符合"纯新增"；回归全过；`git diff --stat` 确认老文件仅追加不改写
- **预估**：0.5 人日

---

## M2 联赛同步与积分（9 人日）

> 本阶段现有 WC 同步路径（`sync_service.go` 中 WC 相关函数、`match_syncer.go` 调度）**零改动**。

### M2-1 联赛同步服务（新增文件，不动 WC 路径）
- **状态**：✅ 已完成（2026-08-04：新增 `league_sync_service.go`（`SYNC_COMPETITIONS` 解析、`SyncAllLeagues`/`SyncLeague`、`upsertLeagueMatch`、`findOrCreateClub`、独立 sync_state resource=`matches:<CODE>`、9s 节流）；新增 `jobs/league_syncer.go`；`config.go` 新增 `SYNC_COMPETITIONS`/`LEAGUE_SYNC_INTERVAL_MINUTES`；`main.go` 接线；`sync_service.go` 零改动）
- **涉及文件**：`backend/internal/services/league_sync_service.go`（新建）、`backend/internal/providers/footballdata/client.go`（新增方法）、`backend/internal/jobs/league_syncer.go`（新建）、`backend/internal/config/config.go`
- **内容**：新增 `SyncLeagueCompetitions()`：遍历 `SYNC_COMPETITIONS` 环境变量（格式 `PL:2025,PD:2025,BL1:2025,SA:2025,FL1:2025`，缺省为空 = 不启用，行为与现在一致）；调用 `CompetitionMatches(ctx, code, season)`；联赛比赛写入 `matches` 新列（`competition_id/season/matchday`，`group_id`/`stadium_id`/`city_id` 为 NULL）；俱乐部 upsert 走 `teams` 新列（`external_code` + `team_type=club`，`fifa_code`=NULL）；每个赛事独立 `sync_state`（resource=`matches:PL` 等新行）；同步间隔复用现有调度机制
- **验收**：`SYNC_COMPETITIONS` 为空时零新增行为；配置后 5 个联赛各自完成同步、SyncState 相互独立；限速下请求间隔 > 6s；**WC 同步任务不受影响**
- **预估**：3 人日

### M2-2 联赛球场/城市处理
- **状态**：✅ 已完成（2026-08-04：`findOrCreateLeagueStadium` 按 venue 名建球场并关联 TBD 城市；venue 为空时回退 TBD 球场/城市——**review 发现的阻塞问题**：matches 表存在 `fk_matches_city`/`fk_matches_stadium` 外键，`city_id=0` 会导致全部插入失败，已修复）
- **涉及文件**：`backend/internal/services/league_sync_service.go`
- **内容**：联赛比赛 venue 按字符串 find-or-create 到 `stadiums` 表（`city_id` 可空；不新建城市，避免污染 `cities` 表的世界杯城市数据）
- **验收**：英超比赛球场落库、城市为空不报错；世界杯城市表数据零变化
- **预估**：0.5 人日

### M2-3 联赛积分榜同步与重算
- **状态**：✅ 已完成（2026-08-04：新增 `league_standing_service.go`——官方 Standings（TOTAL/HOME/AWAY）同步 + `zone` 分区规则表（PL/PD/SA 20 队，BL1/FL1 18 队，注释标注可配置）+ `RecalculateLeagueStanding`（先清后建）；`types.go` 新增 `StandingsResponse`；`client.go` 新增 `CompetitionStandings`；`standing_repo.go` 新增 `UpsertLeagueStanding`（ErrRecordNotFound 判断）/`DeleteLeagueStandings`）
- **涉及文件**：`backend/internal/services/league_standing_service.go`（新建）、`backend/internal/providers/footballdata/types.go`、`backend/internal/repositories/standing_repo.go`
- **内容**：同步官方 Standings（TOTAL/HOME/AWAY）写入 `league_standings`；`zone` 分区按位置规则计算（配置化：英超 1-4 欧冠/5-6 欧战/18-20 降级，其余联赛各表）；新增 `RecalculateLeagueStanding(competitionID, season)`（积分→净胜球→进球）供手动重算
- **验收**：英超当前赛季积分榜与官方一致（抽样比对前 5 名与降级区）；**`group_standings`、`RecalculateGroupStanding`、`RecalculateBestThird` 零改动**
- **预估**：2 人日

### M2-4 sync 状态接口兼容扩展
- **状态**：✅ 已完成（2026-08-04：**无需改动代码**——`GET /api/sync/status` 返回全部 `sync_states` 行，联赛状态以新行（resource=`matches:PL` 等）自动出现，与 WC 的 `matches` 行不冲突）
- **涉及文件**：`backend/internal/handlers/`（sync 状态展示）、`backend/internal/repositories/sync_repo.go`
- **内容**：`GET /api/sync/status` 响应中新增联赛各赛事同步状态条目（resource=`matches:PL` 等）；**旧字段与结构保持，前端 `SyncStatusBadge` 不做破坏性变更**
- **验收**：旧客户端解析 `/api/sync/status` 无报错；新条目可读
- **预估**：0.5 人日

### M2-5 管理后台赛事维度（叠加）
- **状态**：✅ 已完成（2026-08-04：`AdminListMatches` 支持可选 `competitionId`；`AdminSyncMatches` 支持 `?code=PL&season=2025` 单联赛触发（5min timeout + 真实错误透出）；新增 `POST /api/admin/standings/league/recalculate`（competition_id 必填、season 缺省取赛事当前赛季）；路由已注册）
- **涉及文件**：`backend/internal/handlers/admin_handler.go`、`backend/internal/routes/router.go`
- **内容**：admin CRUD 增加**可选** `competition_id` 查询参数（缺省 = 世界杯，行为不变）；新增赛事管理接口（复用 M1-6）；新增联赛积分榜查看/重算接口；`AdminSyncMatches` 支持指定赛事触发
- **验收**：现有 admin 接口不带参数时行为不变；带参数时可按赛事筛选；可单赛事触发同步
- **预估**：1.5 人日

### M2-6 后端联调与回归
- **状态**：✅ 已完成（2026-08-04：`go build`/`go vet`/`go test ./...` 全绿；新增单元测试 `league_sync_service_test.go`（zone 规则 + 配置解析）；review 子代理审查通过——修复 1 个阻塞问题（FK 外键导致联赛比赛全部插入失败）+ 4 项应修（限速提至 9s 为 WC 留余量、联赛同步错误透出、`UpsertLeagueStanding` 错误判断、重算先清旧数据））
- **涉及文件**：全后端
- **内容**：联赛同步/积分相关单元测试；`go test ./...` 全绿；用历史赛季数据端到端验证"联赛同步 → 积分榜 → 查询"链路；**世界杯链路回归**
- **验收**：`go test ./...` 通过；6 赛事查询正常；WC 数据与 M1 验收时一致
- **预估**：0.5 人日

---

## M3 前端赛事切换与联赛页面叠加（15 人日）

> 本阶段世界杯页面代码**保留不删**，只叠加联赛分支；**默认赛事 = 世界杯**，首次进入界面与改造前一致。

### M3-1 类型与 mock 数据扩展（纯新增）
- **状态**：✅ 已完成（2026-08-04：新增 `types/competition.ts`；`match.ts` 新增 `regular_season` stage 与 `matchday/competition_id`；`team.ts` 新增 `external_code/team_type/country/venue`，fifa_code 可空兼容；`standing.ts` 新增 `ApiLeagueStanding/LeagueStanding/normalizeLeagueStanding`；`mockMatches.ts` 补齐新字段）
- **涉及文件**：`frontend/src/types/match.ts`、`team.ts`、`standing.ts`、`common.ts`；`frontend/src/data/mockMatches.ts`、`mockStandings.ts`、`mockTeams.ts`、`mockUser.ts`
- **内容**：类型新增可选字段 `competition_id/season/matchday`、`LeagueStanding` 类型；mock 数据**追加** 1~2 个联赛示例（现有世界杯 mock 不动）
- **验收**：`npm run build` 通过；联赛 mock 可在 dev 环境展示；世界杯 mock 数据与改造前一致
- **预估**：1 人日

### M3-2 全局赛事状态 store（新增）
- **状态**：✅ 已完成（2026-08-04：新增 `useCompetitionStore`（默认世界杯、localStorage `wm-competition`/`wm-competition-id`、过期 code 自动回退 WC）；新增 `api/competitions.ts`；`request.ts` 拦截器仅非 WC 时附加 **`competitionId`**（camelCase，与后端 form tag 一致；NaN 防护））
- **涉及文件**：`frontend/src/stores/useCompetitionStore.ts`（新建）、`frontend/src/api/competitions.ts`（新建）、`frontend/src/api/request.ts`
- **内容**：Pinia store 保存当前赛事 + 赛事列表（启动时拉取 `GET /api/competitions`）；**默认值 = 世界杯**；localStorage 持久化（key `wm-competition`）；`request.ts`：仅当当前赛事非世界杯时附加 `competition_id` 参数
- **验收**：默认进入不携带 `competition_id` 参数（与旧请求一致）；切换赛事后请求带对应参数；刷新保持选择
- **预估**：1 人日

### M3-3 TopBar 赛事切换器（新增入口）
- **状态**：✅ 已完成（2026-08-04：TopBar 新增赛事切换 select（含移动端整行布局与 dark 模式样式）；品牌区文案动态化——世界杯保持原文案，联赛显示“{赛事名} 赛程助手 + 赛季”；世界杯默认态不变）
- **涉及文件**：`frontend/src/components/common/TopBar.vue`、`LoginPage.vue`、`frontend/src/pages/admin/AdminDashboard.vue`
- **内容**：TopBar 品牌区**新增**赛事切换器（下拉/胶囊，含 logo 与赛事名）；默认显示"世界杯/2026 世界杯赛程助手"（与现在一致），切到联赛时动态替换品牌标题；**原品牌文案作为默认态保留**
- **验收**：默认界面与改造前一致；6 个赛事可切换且品牌区跟随；切回世界杯恢复原文案
- **预估**：2 人日

### M3-4 赛程页叠加联赛轮次视图
- **状态**：✅ 已完成（2026-08-04：filterOptions 按赛事模式 computed（联赛 = 全部/今日/明日/未开始/只看关注）；联赛模式新增轮次下拉（1~38 轮，`matchday` 参数）；切换赛事重置筛选并重载；`MatchCard` 新增 `regular_season` → “第 N 轮” 映射；`resetAndLoad` 前置 `await comp.fetchCompetitions()` 修复冷启动竞态）
- **涉及文件**：`frontend/src/pages/user/SchedulePage.vue`、`frontend/src/components/common/ChipFilter.vue`、`MatchCard.vue`
- **内容**：世界杯分支保留小组/淘汰赛筛选（原代码不动）；新增联赛分支：轮次（第 N 轮）筛选 + `matchday` 参数；`MatchCard.vue` 新增联赛 stage 映射（"第 N 轮"），原映射保留
- **验收**：世界杯赛程页与改造前一致；切到英超显示轮次筛选并正确请求 `matchday`；切回世界杯行为不变
- **预估**：2 人日

### M3-5 积分榜页叠加联赛单表
- **状态**：✅ 已完成（2026-08-04：联赛模式渲染 20 队单表（排名/球队/场次/胜平负/进失/净胜/积分）+ `zone` 分区色条与标签（欧冠区/欧战区/降级区）+ 主/客场切换（TOTAL/HOME/AWAY）；世界杯模式（小组榜 + 最佳第三名）原样保留）
- **涉及文件**：`frontend/src/pages/user/StandingsPage.vue`、`frontend/src/components/common/StandingTable.vue`（新增 `LeagueStandingTable.vue` 更佳）
- **内容**：世界杯分支保留小组榜 + 最佳第三名；新增联赛分支：20 队单表（排名/球队/场次/胜平负/进失/净胜/积分），欧战/降级区标记（`zone` 字段），主/客场切换（TOTAL/HOME/AWAY）
- **验收**：世界杯积分榜无回归；英超积分榜正确显示分区标记；主客场切换正常
- **预估**：2 人日

### M3-6 球队页与球队详情叠加俱乐部视图
- **状态**：✅ 已完成（2026-08-04：球队页联赛模式请求 `teamType=club`、筛选简化为全部+搜索；球队详情联赛模式：联赛/国家标签、联赛排名与积分 stat、不加载小组榜；`fifa_code` 可空兼容（`|| external_code`））
- **涉及文件**：`frontend/src/pages/user/TeamsPage.vue`、`TeamDetailPage.vue`、`frontend/src/components/common/TeamCard.vue`、`TeamFlag.vue`；`frontend/src/assets/club-icons/`（新建）
- **内容**：世界杯分支保留国家队视图；新增联赛分支：按赛事筛选俱乐部（国家/主球场/近期战绩）；`TeamDetailPage` 联赛分支显示联赛排名与近 5 场；队徽：五大联赛 ~98 队新增到 `club-icons/`（或 football-data `crest` 远程 URL + 本地缓存），`TeamFlag.vue` 扩展映射（国家队走老表，俱乐部走新表）
- **验收**：世界杯球队页/详情无回归；任意联赛球队列表与详情正常；队徽不破版
- **预估**：3 人日

### M3-7 比赛详情页叠加联赛信息
- **状态**：✅ 已完成（2026-08-04：顶部标签联赛模式显示“第 N 轮”（无小组名时）；“所在小组积分”区块仅在存在小组数据时渲染（联赛比赛自动隐藏）；小组榜加载逻辑不变）
- **涉及文件**：`frontend/src/pages/user/MatchDetailPage.vue`
- **内容**：世界杯分支保留"所在小组积分"；联赛分支显示第 N 轮、该队联赛排名；焦点战标签（可后置）
- **验收**：世界杯比赛详情无回归；联赛比赛详情正常，无"小组"残留文案
- **预估**：1 人日

### M3-8 首页叠加赛事维度
- **状态**：✅ 已完成（2026-08-04：联赛模式各模块按当前赛事取数——下一场/今日比赛/热门推荐/进度卡（赛事名+赛季+已完成场次）/积分速览 Top5（替代小组速览）；关注球队自动加载俱乐部列表；世界杯模式原逻辑不动）
- **涉及文件**：`frontend/src/pages/user/HomePage.vue`、`frontend/src/stores/useMatchStore.ts`
- **内容**：世界杯分支保留现有内容（默认）；联赛分支：倒计时/今日比赛/热门推荐按当前赛事取数，"小组积分速览"替换为联赛积分榜 Top5，"我的关注球队"按赛事过滤
- **验收**：默认（世界杯）首页与改造前一致；切换赛事后各模块数据正确
- **预估**：2 人日

### M3-9 前端回归与构建
- **状态**：✅ 已完成（2026-08-04：`npm run build` 通过（三轮验证）；双轮 review——修复 1 个阻塞问题（拦截器 `competition_id`→`competitionId` camelCase 对齐后端 form tag）+ 冷启动竞态（各页 `await comp.fetchCompetitions()`）+ `TeamDetailPage` 补 `currentCode` watcher；硬编码检查仅剩世界杯默认分支文案（符合保留原则）；`tsconfig.*.tsbuildinfo` 取消 git 跟踪；`git rm --cached .env`（既有密钥泄露隐患，已从索引移除，密钥需轮换））
- **涉及文件**：全前端
- **内容**：检查世界杯页面无回归（与改造前截图/行为对比）；移动端布局检查；`npm run build` 通过
- **验收**：`npm run build` 通过；世界杯 5 页面与改造前一致；6 赛事 × 页面手工回归无报错
- **预估**：1 人日

---

## M4 聚合、打磨与上线（6.5 人日）

### M4-1 跨赛事今日焦点聚合（新增模块）
- **状态**：✅ 已完成（2026-08-04：HomePage 新增“今日焦点”模块——利用 `/matches/today` 不绑定 MatchQuery 的特性天然跨赛事聚合，按焦点标记/开球时间排序取前 6，两种模式均展示）
- **涉及文件**：`frontend/src/pages/user/HomePage.vue`、`frontend/src/api/matches.ts`
- **内容**：首页新增"今日焦点"模块（世界杯模式也可展示）：跨 6 赛事聚合当日焦点比赛（按 ImportanceLevel/焦点标签排序）；请求合并/缓存避免 N+1
- **验收**：同一页面看到英超+西甲+世界杯同日比赛；无重复请求
- **预估**：1.5 人日

### M4-2 关注球队跨赛事追踪（叠加）
- **状态**：✅ 已完成（2026-08-04：**纯前端实现**（后端 favorite_service 无需改动，已在文档注明）——ProfilePage 并行拉取各联赛官方积分榜建立 teamId→联赛名映射，关注列表按赛事分组展示（国家队回退“世界杯”），原 `followedTeamNames` 已移除）
- **涉及文件**：`frontend/src/pages/user/ProfilePage.vue`、`frontend/src/stores/useFavoriteStore.ts`、`backend/internal/services/favorite_service.go`
- **内容**：收藏/关注列表新增按赛事分组展示（现有列表保留）；"我的关注"可聚合展示跨赛事近期比赛
- **验收**：关注英超球队 + 世界杯球队后，列表正确分组；老收藏数据不受影响
- **预估**：1.5 人日

### M4-3 管理后台页面叠加赛事维度
- **状态**：✅ 已完成（2026-08-04：AdminSyncPage 赛事下拉 + 单赛事手动同步；AdminMatchesPage 赛事下拉（联赛可筛，世界杯含于全部）；AdminTeamsPage 类型下拉（national/club）；AdminStandingsPage 小组/联赛双模式 + 联赛重算；修复 `AdminListTeams` 缺失的 `ShouldBindQuery` 绑定（review 发现））
- **涉及文件**：`frontend/src/pages/admin/AdminMatchesPage.vue`、`AdminTeamsPage.vue`、`AdminStandingsPage.vue`、`AdminSyncPage.vue`
- **内容**：后台列表/筛选新增赛事下拉（缺省 = 世界杯）；同步页展示各赛事同步状态并可单赛事触发；新增赛事管理页（可选）
- **验收**：后台默认视图与改造前一致；可按赛事筛选；同步状态按赛事展示
- **预估**：1.5 人日

### M4-4 测试与回归
- **状态**：✅ 已完成（2026-08-04：后端 `go build`/`go vet`/`go test` 全绿；前端 `npm run build` 通过（修复 HomePage 缺失的 `apiGetTodayMatches` 导入）；review 审查通过并修复 3 个契约问题（admin teams 绑定、拦截器显式参数优先、世界杯 NULL 过滤语义）+ 2 个防御性修复）
- **涉及文件**：全项目
- **内容**：后端 `go test ./...`、前端 `npm run build`；世界杯全功能回归清单（赛程/积分榜/最佳第三名/收藏/提醒/通知）；6 赛事抽查；老数据兼容性验证
- **验收**：全部通过；WC 2026 历史数据在所有页面正常展示；表结构 diff 仍符合"纯新增"
- **预估**：1 人日

### M4-5 部署与上线
- **状态**：✅ 已完成（2026-08-04：`backend/.env.example` 新增 `SYNC_COMPETITIONS`/`LEAGUE_SYNC_INTERVAL_MINUTES`；`docker-compose.yml` 修复 `MYSQL_DSN`（xxladmin→root:password，与 `MYSQL_ROOT_PASSWORD` 对齐）并新增联赛同步环境变量；README 更新项目简介/功能概览/环境变量说明）
- **涉及文件**：`docker-compose.yml`、`backend/.env.example`、`frontend/nginx.conf`、README.md
- **内容**：新增 `SYNC_COMPETITIONS` 环境变量说明；修复 docker-compose 中 `MYSQL_DSN` 与 MySQL 用户不一致问题（README 已记录，属独立修复）；部署文档更新
- **验收**：`docker compose up -d --build` 一键启动；线上 `/api/competitions` 返回 6 赛事；默认首页为世界杯；联赛同步任务按调度运行
- **预估**：1 人日

---

## 里程碑验收清单

| 里程碑 | 验收口径 |
|---|---|
| M1 完成 | `SHOW CREATE TABLE` 与基线 diff 仅含"新增表/新增可空列/新索引"；`go test ./...` 通过；世界杯全部接口行为与改造前一致；`GET /api/competitions` 可用 |
> 2026-08-04 已达成（本机无 MySQL，表结构 diff 以基线文件 + dump 证据 + review 审查核验） |
| M2 完成 | 五大联赛各自完成一次全量同步且 WC 同步零影响；联赛积分榜与官方一致；`/api/sync/status` 按赛事展示；`group_standings`/best-third 代码零改动 |
> 2026-08-04 已达成（代码层；真实数据同步需配置 `FOOTBALL_DATA_API_KEY` + `SYNC_COMPETITIONS` 后在联网环境执行） |
| M3 完成 | 前端默认世界杯、首次进入界面与改造前一致；6 赛事切换全链路可用；`npm run build` 通过；世界杯页面代码未删 |
> 2026-08-04 已达成（构建 + 双轮 review；真机联调需后端 + 联赛数据） |
| M4 完成 | 跨赛事今日聚合、关注分组上线；Docker 一键部署；回归全绿 |
> 2026-08-04 已达成（代码层；`docker compose up` 与真实数据同步需在具备 Docker/API Key 的环境执行） |

---

## 备注

- **回退方案**：联赛代码全部收敛在新文件/新列/新表中（新增 service、handler、store、组件），回退 = 停用 `SYNC_COMPETITIONS` + 前端默认赛事保持世界杯，即可回到纯世界杯版本，无需回滚数据库
- 可并行项：M3 内部页面任务（M3-4 ~ M3-8）在 M3-2 完成后可并行；M3 与 M2 在 M1 完成后可并行（前端依赖 mock 数据即可联调）
- P2/P3 功能（射手榜、微信提醒、预测竞猜、欧冠等）不在本计划内，属后续迭代
