# WorldCup Mate 球队球员大名单前端开发方案

## 0. 实施进度

### 2026-06-05

- 已完成 P0.1：新增 `frontend/src/types/player.ts`，包含 `ApiPlayer`、`Player` 和 `normalizePlayer`，并提供位置中文兜底映射。
- 已完成 P0.2：在 `frontend/src/api/teams.ts` 新增 `apiGetTeamPlayers(id)`，请求 `/teams/:id/players`。
- 已完成 P0.3：新增 `frontend/src/components/common/PlayerRoster.vue`，支持名单展示、位置筛选、号码排序、照片兜底、图片加载失败兜底、loading、error 和 empty 状态。
- 已完成 P0.4/P0.5：修改 `frontend/src/pages/user/TeamDetailPage.vue`，局部加载球员名单，并将 `PlayerRoster` 插入球队 Hero 后、下一场比赛前。
- 已完成稳定性处理：切换球队时清空旧球员状态，并通过请求序号避免旧接口响应覆盖新球队名单。

## 1. 前端目标

在球队详情页展示该球队的世界杯球员大名单，让用户进入球队详情后可以快速查看球队阵容。

第一版前端需要展示：

- 球员名字
- 球衣号码
- 球员照片
- 球员位置

同时需要支持：

- 球员照片缺失时的兜底头像。
- 球员名单为空时的空状态。
- 球员名单接口失败时不影响球队详情主内容。
- 移动端优先的紧凑列表展示。

## 2. 当前前端现状

球队详情页：

```text
frontend/src/pages/user/TeamDetailPage.vue
```

当前已展示：

- 球队 Hero
- 关注球队
- 下一场比赛
- 球队赛程
- 小组积分

当前球队类型：

```text
frontend/src/types/team.ts
```

当前球队 API：

```text
frontend/src/api/teams.ts
```

当前尚未有：

- `Player` 类型
- 球员 API
- 球员名单组件
- 球队详情页加载球员名单逻辑

## 3. 页面信息结构

球员大名单建议放在球队 Hero 后、下一场比赛前。

最终结构：

```text
球队信息
球员大名单
下一场比赛
球队赛程
小组积分
```

这样用户进入球队详情后，会先看到球队身份和阵容，再看赛程与积分。

## 4. 前端类型设计

新增文件：

```text
frontend/src/types/player.ts
```

类型定义：

```ts
export interface ApiPlayer {
  id: number
  team_id: number
  name: string
  name_en?: string
  shirt_number?: number
  position?: string
  position_label?: string
  photo_url?: string
  club?: string
}

export interface Player {
  id: number
  teamId: number
  name: string
  nameEn: string
  shirtNumber?: number
  position: string
  positionLabel: string
  photoUrl: string
  club: string
}

export function normalizePlayer(value: ApiPlayer): Player {
  return {
    id: value.id,
    teamId: value.team_id,
    name: value.name,
    nameEn: value.name_en || '',
    shirtNumber: value.shirt_number,
    position: value.position || '',
    positionLabel: value.position_label || value.position || '',
    photoUrl: value.photo_url || '',
    club: value.club || '',
  }
}
```

位置编码建议：

| 编码 | 中文 |
| --- | --- |
| `GK` | 门将 |
| `DF` | 后卫 |
| `MF` | 中场 |
| `FW` | 前锋 |

## 5. API 设计

推荐扩展现有：

```text
frontend/src/api/teams.ts
```

新增方法：

```ts
import type { ApiPlayer } from '@/types/player'

export function apiGetTeamPlayers(teamId: number) {
  return request.get<ApiPlayer[]>(`/teams/${teamId}/players`)
}
```

如果后续球员 API 增多，也可以新增：

```text
frontend/src/api/players.ts
```

第一版建议放在 `teams.ts`，因为当前接口是球队详情的附属数据。

## 6. 球队详情页状态设计

修改：

```text
frontend/src/pages/user/TeamDetailPage.vue
```

新增状态：

```ts
const players = ref<Player[]>([])
const playersLoading = ref(false)
const playersError = ref('')
```

加载逻辑：

```ts
async function loadPlayers(teamId: number) {
  playersLoading.value = true
  playersError.value = ''
  players.value = []

  try {
    const res = await apiGetTeamPlayers(teamId) as ApiPlayer[]
    players.value = (res || []).map(normalizePlayer)
  } catch {
    playersError.value = '球员名单加载失败'
  } finally {
    playersLoading.value = false
  }
}
```

在 `loadTeamDetail` 中建议并行加载：

```ts
const [detail] = await Promise.all([
  teamStore.fetchTeamDetail(id),
  loadPlayers(id),
])
```

也可以在拿到球队详情后再调用 `loadPlayers(id)`。第一版推荐并行，减少等待。

注意：

- 球员接口失败不应该让球队详情页进入整体错误状态。
- `playersError` 只传给球员名单组件展示。

## 7. 球员名单组件

新增组件：

```text
frontend/src/components/common/PlayerRoster.vue
```

Props：

```ts
defineProps<{
  players: Player[]
  loading?: boolean
  error?: string
}>()
```

组件能力：

- 展示球员列表。
- 按位置筛选。
- 照片兜底。
- 空状态。
- 错误状态。

位置筛选：

```ts
const tabs = [
  { label: '全部', value: 'all' },
  { label: '门将', value: 'GK' },
  { label: '后卫', value: 'DF' },
  { label: '中场', value: 'MF' },
  { label: '前锋', value: 'FW' },
]
```

筛选逻辑：

```ts
const filteredPlayers = computed(() => {
  if (activePosition.value === 'all') return props.players
  return props.players.filter((player) => player.position === activePosition.value)
})
```

## 8. 展示设计

### 8.1 移动端列表

推荐紧凑列表，不使用大卡片。

单行结构：

```text
[头像]  #10  Kylian Mbappe
        前锋 · Real Madrid
```

视觉重点：

- 头像 42px 左右。
- 号码使用小徽标。
- 姓名加粗。
- 位置和俱乐部使用弱文本。
- 行间使用细分隔线。

### 8.2 桌面端布局

桌面端可以两列展示：

```css
.player-list {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0 14px;
}
```

如果后续信息更多，可以改成表格或按位置分组。

### 8.3 照片兜底

如果 `photoUrl` 存在：

```vue
<img :src="player.photoUrl" :alt="player.name" />
```

如果不存在：

```vue
<span class="avatar-fallback">{{ getInitial(player.name) }}</span>
```

兜底规则：

- 中文名取第一个字。
- 英文名取首字母。
- 空名时显示 `person` 图标。
- 不展示破图。

## 9. 组件模板建议

```vue
<template>
  <section class="section roster-section">
    <div class="section-head">
      <h2>球员大名单</h2>
      <span>{{ players.length }} 人</span>
    </div>

    <div class="position-tabs">
      <button
        v-for="tab in tabs"
        :key="tab.value"
        :class="{ active: activePosition === tab.value }"
        type="button"
        @click="activePosition = tab.value"
      >
        {{ tab.label }}
      </button>
    </div>

    <div v-if="loading" class="roster-state">正在加载球员名单...</div>
    <div v-else-if="error" class="roster-state error">{{ error }}</div>
    <div v-else-if="!players.length" class="roster-state">球员大名单暂未同步</div>

    <div v-else class="player-list">
      <article v-for="player in filteredPlayers" :key="player.id" class="player-row">
        <img v-if="player.photoUrl" :src="player.photoUrl" :alt="player.name" />
        <span v-else class="avatar-fallback">{{ getInitial(player.name) }}</span>

        <span class="player-number">#{{ player.shirtNumber || '-' }}</span>
        <div class="player-copy">
          <strong>{{ player.name }}</strong>
          <span>{{ player.positionLabel || player.position }}<template v-if="player.club"> · {{ player.club }}</template></span>
        </div>
      </article>
    </div>
  </section>
</template>
```

## 10. 样式原则

延续当前项目视觉：

- 使用 `var(--card)`、`var(--line)`、`var(--muted)`、`var(--blue)`。
- 卡片圆角不超过现有系统。
- 移动端优先。
- 不用 emoji 作为 UI 图标。
- 行高、间距保持紧凑，避免球员列表让页面过长。

建议样式片段：

```css
.roster-section {
  display: grid;
  gap: 12px;
}

.position-tabs {
  display: flex;
  gap: 6px;
  overflow-x: auto;
}

.position-tabs button {
  min-height: 32px;
  padding: 0 12px;
  border: 1px solid var(--line);
  border-radius: 999px;
  color: var(--muted);
  background: var(--card);
  white-space: nowrap;
}

.position-tabs button.active {
  color: var(--blue);
  border-color: color-mix(in srgb, var(--blue) 32%, var(--line));
  background: color-mix(in srgb, var(--blue) 7%, var(--card));
}

.player-list {
  display: grid;
  border-top: 1px solid var(--line);
}

.player-row {
  display: grid;
  grid-template-columns: 42px auto minmax(0, 1fr);
  align-items: center;
  gap: 10px;
  min-height: 62px;
  padding: 9px 0;
  border-bottom: 1px solid var(--line);
}
```

## 11. TeamDetailPage 接入

模板中放在 Hero 后面：

```vue
<PlayerRoster
  :players="players"
  :loading="playersLoading"
  :error="playersError"
/>
```

导入：

```ts
import PlayerRoster from '@/components/common/PlayerRoster.vue'
import { apiGetTeamPlayers } from '@/api/teams'
import { normalizePlayer, type Player } from '@/types/player'
```

数据重置：

在切换球队时，需要清空旧球员数据：

```ts
players.value = []
playersError.value = ''
playersLoading.value = false
```

## 12. 空状态和错误状态

组件内处理：

- `loading`：正在加载球员名单...
- `error`：球员名单加载失败
- `players.length === 0`：球员大名单暂未同步

注意：

- 这些状态只影响球员名单区域。
- 不要让球队详情页整体白屏。
- 不要隐藏赛程和积分。

## 13. 开发步骤

P0 前端最小展示：

1. 新增 `frontend/src/types/player.ts`。
2. 在 `frontend/src/api/teams.ts` 新增 `apiGetTeamPlayers`。
3. 新增 `frontend/src/components/common/PlayerRoster.vue`。
4. 修改 `TeamDetailPage.vue` 加载球员名单。
5. 在 Hero 后插入 `PlayerRoster`。
6. 实现 loading、error、empty 状态。
7. 实现照片兜底。

P1 体验增强：

1. 增加位置筛选。
2. 增加桌面两列布局。
3. 增加按号码排序。
4. 增加俱乐部展示。

P2 后续扩展：

1. 增加球员详情弹层。
2. 增加队长标识。
3. 增加年龄、身高、国家队出场等字段。
4. 增加 AI 阵容解读入口。

## 14. 前端验收标准

功能验收：

- 进入球队详情页能看到“球员大名单”区域。
- 每名球员展示名字、号码、位置。
- 有照片时展示照片。
- 无照片时展示兜底头像。
- 可以按位置筛选。
- 名单为空时展示空状态。
- 接口失败时展示局部错误，不影响球队详情其他内容。

视觉验收：

- 移动端 375px 无横向滚动。
- 球员姓名不会溢出容器。
- 头像尺寸稳定，图片加载失败不破坏布局。
- 位置筛选按钮不挤压页面。
- 与现有球队详情页设计风格一致。

技术验收：

- `npm run build` 通过。
- TypeScript 类型无错误。
- 切换不同球队时不会残留上一支球队的球员名单。

## 修订记录：移除前端兜底数据

更新时间：2026-06-05

根据最新要求，前端已调整为只展示接口真实返回的数据：

- `frontend/src/types/player.ts` 不再提供位置中文兜底映射。
- `normalizePlayer` 不再根据 `GK/DF/MF/FW` 自动生成 `positionLabel`。
- `PlayerRoster` 不再用姓名首字母或 `person` 图标生成照片兜底内容。
- `PlayerRoster` 不再显示“位置待定”等合成字段。
- 没有照片时只保留空的布局占位，不展示伪造头像内容。
- 球员名单为空时只展示空状态，不生成任何球员数据。

本记录覆盖上方旧方案中“位置中文兜底映射”“照片兜底”等描述。
