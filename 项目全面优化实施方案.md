# WorldCup Mate 项目全面优化实施方案

## 1. 文档目的

WorldCup Mate 当前已经完成 Vue 3 用户端、管理端基础页面、Go API、MySQL/Redis、赛事数据同步、多联赛主题、球队中文名称与队徽展示等主要能力。

下一阶段不再以增加演示页面为核心，而是将项目从“可运行、可展示”提升到“数据可信、权限安全、功能完整、可持续维护”的正式产品状态。

本方案重点解决以下问题：

1. 补齐后台管理中尚未真正落库的接口和操作。
2. 修复用户数据越权、认证强度和敏感信息处理问题。
3. 消除静态主题数据与真实赛事数据之间的不一致。
4. 提升数据同步、比赛提醒和多实例运行时的可靠性。
5. 建立统一的接口类型、错误规范、测试体系和质量检查流程。
6. 加强实时赛况、时区、国际化、无障碍与移动端体验。

---

## 2. 当前项目基线

### 2.1 已具备的能力

- 用户端：首页、赛程、比赛详情、球队、球队详情、积分榜、个人中心、登录注册。
- 用户功能：关注球队、收藏比赛、比赛提醒、站内通知、头像上传、个人设置。
- 赛事范围：世界杯、英超、西甲、德甲、意甲、法甲。
- 视觉能力：联赛主题切换、深浅色模式、移动端与桌面端响应式布局。
- 后端能力：Gin API、JWT、GORM、MySQL、Redis、邮件提醒、赛事同步。
- 数据能力：球队中文本地化、队徽、比赛、积分榜和同步状态。
- 工程状态：前端生产构建通过，后端现有测试通过。

### 2.2 当前主要短板

- 管理端部分接口只是返回成功，没有真正修改数据库。
- 通知单条已读接口缺少用户归属校验。
- 赛季、轮次和部分首页赛事阶段仍来自静态主题配置。
- 用户选择的时区和语言没有完整作用于全部界面。
- 前端没有直播状态自动刷新机制。
- 同步和提醒任务缺少数据库级幂等与原子认领机制。
- 前端 API 层大量使用 `any`，缺少稳定的接口契约。
- 自动化测试、代码规范检查和持续集成覆盖不足。
- 上传文件、构建产物和临时备份文件的仓库管理需要清理。

---

## 3. 优化原则

### 3.1 数据与视觉分离

联赛主题配置只负责：

- 色彩
- 字体
- 图形语言
- 品牌文案
- 动效节奏

以下内容必须由后端真实数据提供：

- 当前赛季
- 当前轮次
- 已完成场次
- 总场次
- 比赛状态
- 比分与直播分钟
- 球队、积分和排名

### 3.2 后端权限不能依赖前端

- 前端路由守卫只用于改善体验。
- 所有个人资源必须在后端校验 `资源 ID + 当前用户 ID`。
- 所有管理接口必须在后端校验管理员角色。
- 不允许根据客户端传入的用户 ID 操作个人数据。

### 3.3 所有“成功”都必须可验证

- 新增接口成功后必须能重新查询到数据。
- 修改接口成功后必须返回最新数据。
- 删除接口必须具备明确的不存在、被引用和删除成功状态。
- 禁止保留仅返回成功文案但不执行持久化的占位实现。

### 3.4 同步和提醒必须可重复执行

- 重复同步不能产生重复比赛或球队。
- 同一提醒不能被重复发送。
- 任务中断后能够恢复。
- 所有任务需要留下可查询的执行状态和错误摘要。

---

## 4. 优先级总览

| 优先级 | 优化方向 | 核心目标 |
| --- | --- | --- |
| P0 | 用户数据权限 | 修复越权操作，确保个人数据隔离 |
| P0 | 后台管理补全 | 所有管理操作真正落库并可验证 |
| P0 | 认证与敏感信息 | 提升账号安全，清理仓库中的运行数据 |
| P1 | 赛事数据动态化 | 取消静态赛季、轮次和赛事进度 |
| P1 | 时区与直播状态 | 确保比赛时间、分钟和状态准确更新 |
| P1 | 同步与提醒可靠性 | 保证幂等、原子性和失败恢复 |
| P1 | API 规范与类型 | 统一状态码、错误码、DTO 和前端类型 |
| P1 | 自动化测试 | 覆盖核心业务、权限和接口联调 |
| P2 | 性能与无障碍 | 优化请求、缓存、键盘操作和焦点管理 |
| P2 | 产品增强 | 增加推送通知、实时赛况和个性化能力 |

---

## 5. P0：用户数据权限与认证安全

### 5.1 修复通知越权

#### 当前问题

单条通知标记已读时只根据通知 ID 更新，没有同时验证通知所属用户。已登录用户如果猜到其他通知 ID，可能修改其他用户的通知状态。

#### 改造方案

- Handler 获取当前 JWT 中的 `user_id`。
- Service 和 Repository 接口修改为：
  - `MarkNotificationRead(notificationID, userID uint)`
- SQL 条件必须包含：
  - `WHERE id = ? AND user_id = ?`
- 更新结果 `RowsAffected = 0` 时返回资源不存在或无权访问。
- 增加越权访问测试。

#### 涉及文件

- `backend/internal/handlers/notification_handler.go`
- `backend/internal/services/notification_service.go`
- `backend/internal/repositories/notification_repo.go`

#### 验收标准

- 用户只能操作自己的通知。
- 访问其他用户通知时返回 404 或 403。
- 接口测试覆盖正常、越权、不存在三种场景。

### 5.2 登录、注册和密码策略

#### 改造方案

- 登录和注册接口增加基于 IP 与账号的限流。
- 密码最小长度由 6 位提升到至少 8 位，推荐 10 位。
- 密码必须包含至少两类字符。
- 连续登录失败达到阈值后短时间冻结尝试。
- 登录日志不记录密码、Token 和完整敏感请求体。
- 增加忘记密码和邮件重置密码流程。
- 修改密码后使旧登录凭证失效。

#### 验收标准

- 暴力请求会收到 429。
- 弱密码注册和修改密码会被拒绝。
- 密码重置链接具有有效期且只能使用一次。
- 修改密码后旧登录状态无法继续访问个人接口。

### 5.3 Token 生命周期

#### 建议方案

- Access Token 设置较短有效期，例如 30 分钟至 2 小时。
- Refresh Token 使用独立随机值并保存服务端状态。
- Refresh Token 轮换使用，旧 Token 使用后立即作废。
- 退出登录时撤销 Refresh Token。
- JWT 增加 `issuer`、`audience`、`token_version` 等校验。
- 管理员 Token 与普通用户 Token 使用相同签名体系，但必须严格验证角色。

### 5.4 前端路由守卫

#### 改造方案

- 为 `/profile` 增加登录守卫。
- 为 `/admin` 及子路由增加管理员守卫。
- 普通用户访问管理端时跳转首页并提示无权限。
- 未登录访问需要认证的页面时跳转登录页，并保留安全的站内回跳地址。
- 禁止接受协议相对地址和站外重定向地址。

#### 涉及文件

- `frontend/src/router/index.ts`
- `frontend/src/stores/useAuthStore.ts`
- `frontend/src/pages/user/LoginPage.vue`

### 5.5 上传与仓库隐私

#### 改造方案

- 上传目录不再纳入 Git 跟踪。
- 清理已提交的头像和临时运行文件。
- 增加上传文件随机名，避免暴露用户 ID。
- 保留当前扩展名、文件大小和 MIME 检查。
- 增加图片解码校验，拒绝损坏或伪造图片。
- 对图片重新编码，移除 EXIF 等隐私元数据。
- 用户更换头像后清理旧头像。
- 上传路径通过配置指定，后续可平滑切换到对象存储。

#### 验收标准

- Git 状态中不再出现用户上传文件。
- 伪造扩展名和超限文件无法上传。
- 用户无法通过文件名推测其他用户信息。

---

## 6. P0：后台管理功能补全

### 6.1 后端管理接口

| 模块 | 当前状态 | 目标状态 |
| --- | --- | --- |
| 球队 | 查询、删除可用；新增和部分更新不完整 | 完整新增、编辑、删除、队徽与赛事关联 |
| 分组 | 查询可用；新增和更新未完整落库 | 完整 CRUD，并校验分组名称唯一性 |
| 城市 | 查询可用；增删改为占位实现 | 完整 CRUD，并校验球场引用 |
| 球场 | 查询可用；增删改为占位实现 | 完整 CRUD，并校验城市关联 |
| 比赛 | 新增、比分、状态部分可用；普通编辑未保存 | 完整编辑、删除、状态流转和操作记录 |
| 积分榜 | 查询和重算部分可用；手动更新未实现 | 支持重算、人工修正和修正原因 |
| 用户 | 查询可用；状态更新未实现 | 支持启用、停用和角色安全管理 |
| 赛事 | 已有基础接口 | 增加赛季、状态、排序和同步开关管理 |

### 6.2 接口实现要求

- 使用明确的 Input DTO，禁止使用任意 `map[string]interface{}` 接收核心更新。
- 所有 URL ID 必须校验转换错误。
- 所有数据库写操作必须检查返回错误。
- 关键操作使用事务。
- 删除前检查外键引用并返回可理解的业务提示。
- 比赛状态只允许合法流转，例如：
  - `scheduled -> live -> finished`
  - `scheduled -> postponed/cancelled`
- 更新比分与完赛时触发积分重算。
- 管理操作记录管理员、对象、动作、变更前后数据和时间。

### 6.3 管理端前端

#### 页面补全

- 独立管理员登录入口。
- 后台首页 KPI 改为真实接口数据，不再使用固定数字。
- 球队新增/编辑抽屉。
- 比赛新增/编辑、比分修改和状态修改弹窗。
- 城市、球场、分组和赛事管理页面。
- 用户启用/停用页面。
- 同步日志详情与失败重试。

#### 交互规范

- 新增、更新、删除均显示加载状态。
- 删除操作必须二次确认。
- 成功后重新拉取数据并展示成功提示。
- 失败时保留表单输入并展示具体业务错误。
- 表格支持后端分页、筛选和排序。
- 移动端使用卡片或横向滚动表格，顶部导航改为可收起形式。

#### 验收标准

- 每个管理按钮都有实际动作。
- 页面刷新后修改结果仍然存在。
- 普通用户无法访问管理接口。
- 管理接口核心路径具备集成测试。

---

## 7. P1：赛事数据全面动态化

### 7.1 清理静态业务数据

#### 当前问题

`leagueTheme.ts` 同时保存视觉主题和静态业务数据，例如：

- 赛季
- 当前轮次
- 已比赛数
- 总比赛数
- 示例球队
- 示例球场

这会导致真实接口数据更新后，首页仍显示旧轮次或旧赛季。

#### 改造方案

将主题结构收敛为：

```ts
interface LeagueTheme {
  code: CompetitionCode
  slug: string
  name: string
  en: string
  mark: string
  tagline: string
  description: string
}
```

删除或停止使用：

- `stage`
- `played`
- `total`
- `venue`
- `teams`
- `getDemoMatches`

新增或扩展后端聚合接口：

```text
GET /api/competitions/:code/overview
```

建议返回：

```json
{
  "competition": {},
  "season": 2026,
  "current_matchday": 12,
  "total_matches": 380,
  "finished_matches": 108,
  "live_matches": 1,
  "next_match": {},
  "available_matchdays": [1, 2, 3]
}
```

### 7.2 动态轮次筛选

- 联赛赛程页从数据中生成可选轮次。
- 默认选择全部或当前轮次。
- 支持上一轮、当前轮、下一轮快捷切换。
- 世界杯继续使用小组赛、淘汰赛等阶段筛选。
- 赛季或赛事切换后清空不适用的旧筛选条件。

### 7.3 直播分钟与比赛状态

- 前端 `ApiMatch` 增加 `live_minute`。
- `normalizeMatch()` 正确映射直播分钟。
- 完整区分：未开始、直播、完场、延期、取消。
- 延期和取消比赛不能显示为普通未开始状态。
- 比赛详情和卡片统一使用一个状态映射函数。

### 7.4 首页数据聚合

将首页当前多个请求逐步收敛为聚合接口，减少往返请求：

```text
GET /api/home?competition_code=PL
```

建议返回：

- 赛事概览
- 下一场焦点
- 今日比赛
- 推荐比赛
- 积分榜前五
- 登录用户的关注赛程摘要

#### 验收标准

- 后台修改赛事赛季后，前端无需改代码即可显示新赛季。
- 轮次筛选不再固定为第 11、12 轮。
- 首页进度与数据库比赛数量一致。
- 直播分钟能随接口数据变化正常显示。

---

## 8. P1：时区与国际化

### 8.1 统一时区处理

#### 当前问题

- 部分代码固定使用北京时间。
- 部分代码使用浏览器本地时区。
- 用户保存的时区没有贯穿所有比赛时间显示。

#### 改造方案

- 后端继续统一保存和返回 UTC 时间。
- 前端只通过一个时间工具模块格式化时间。
- 所有比赛时间显式传入用户选择的时区。
- 未登录用户默认使用浏览器时区。
- 登录用户默认使用账号保存时区。
- 用户修改时区后，当前页面所有时间立即重新计算。
- 日期分组、今日比赛判断和倒计时使用相同时区。
- 增加跨日期、夏令时和无效时间测试。

#### 涉及文件

- `frontend/src/utils/timezone.ts`
- `frontend/src/types/match.ts`
- `frontend/src/data/themeAdapters.ts`
- `frontend/src/stores/useSettingStore.ts`
- 所有比赛卡片和详情页面

### 8.2 国际化决策

建议采用以下二选一方案：

#### 方案 A：正式支持中英文

- 引入 Vue I18n。
- 将页面文案、状态、轮次和时间格式迁移为语言包。
- 语言切换后立即生效并同步账号设置。
- 后端返回稳定枚举，前端负责本地化显示。

#### 方案 B：当前版本只支持中文

- 暂时移除 English 选项。
- 保留数据库 language 字段，为后续国际化预留。

不建议继续保留“可选择英文但界面不变化”的状态。

---

## 9. P1：实时赛况与前端请求策略

### 9.1 直播轮询

第一阶段使用轮询即可满足当前规模：

- 存在直播比赛时，每 30 秒请求直播接口。
- 比赛即将开始的时间窗口内，每 60 秒刷新。
- 没有直播时降低刷新频率。
- 页面不可见时暂停轮询。
- 页面重新可见时立即刷新。
- 离开页面和组件卸载时清理定时器。

后续访问量增加后，可升级为 SSE 或 WebSocket。

### 9.2 请求取消与竞态处理

- 赛事快速切换时取消旧请求。
- 搜索输入使用防抖。
- 相同参数的并发请求进行合并。
- 旧请求晚返回时不能覆盖新赛事的数据。
- 网络失败保留上一次成功数据，并显示轻量错误状态。

### 9.3 赛程加载方式

- 不再一次并发拉取所有分页。
- 移动端采用按日期加载或“加载更多”。
- 桌面端可采用后端分页。
- 收藏筛选优先由后端完成，避免先下载全部比赛再前端过滤。

---

## 10. P1：同步与提醒可靠性

### 10.1 比赛同步幂等

- 为比赛增加数据库唯一约束：
  - `external_provider + external_id`
- 同步使用 Upsert 或事务化查询更新。
- 冲突时更新已存在记录，不创建重复记录。
- 对历史重复数据提供一次性清理脚本。
- 球队、积分榜也使用稳定外部标识建立唯一约束。

### 10.2 外部 API 重试策略

- 对超时、429、502、503、504 进行有限次数重试。
- 使用指数退避并加入随机抖动。
- 尊重供应商返回的限流等待信息。
- 4xx 参数错误不自动重试。
- 记录状态码、赛事、赛季、重试次数和错误摘要。
- 错误日志不记录 API Key。

### 10.3 同步任务锁

- 当前进程内互斥继续保留。
- 增加 Redis 分布式锁，防止多个实例同时同步同一赛事。
- 锁包含过期时间和唯一持有者标识。
- 只有锁持有者可以释放锁。
- 同步超时后锁能自动过期。

### 10.4 提醒原子认领

#### 当前风险

任务先查询待发送提醒，发送完成后才更新状态。并发扫描或中途崩溃可能重复发送。

#### 改造方案

- 提醒状态扩展为：
  - `pending`
  - `processing`
  - `sent`
  - `failed`
- 扫描时通过事务原子认领任务。
- 认领成功后再生成通知和发送邮件。
- 记录发送次数、最后错误和下一次重试时间。
- 邮件发送失败不影响站内通知状态判断。
- 超过最大重试次数进入失败状态，支持后台人工重试。
- 增加用户、比赛、提醒分钟和渠道的唯一约束。

#### 验收标准

- 同一赛事重复同步不会增加比赛数量。
- 两个任务实例同时扫描时，同一提醒只发送一次。
- 外部 API 临时错误后能够自动恢复。
- 后台可以查看每次同步和提醒失败原因。

---

## 11. P1：API 规范化

### 11.1 正确使用 HTTP 状态码

建议映射：

| 场景 | HTTP 状态码 |
| --- | --- |
| 查询或修改成功 | 200 |
| 新增成功 | 201 |
| 删除成功且无返回体 | 204 |
| 参数错误 | 400 |
| 未登录或 Token 失效 | 401 |
| 已登录但无权限 | 403 |
| 资源不存在 | 404 |
| 数据冲突或重复 | 409 |
| 请求过多 | 429 |
| 服务内部错误 | 500 |
| 上游数据源异常 | 502/503 |

可以继续保留统一 JSON 结构，但 HTTP 状态码必须准确。

### 11.2 业务错误码

示例：

```json
{
  "code": "REMINDER_ALREADY_EXISTS",
  "message": "该提醒已经存在",
  "data": null,
  "request_id": "..."
}
```

要求：

- 前端根据稳定错误码处理业务分支。
- 用户可见文案与内部日志分离。
- 数据库错误和堆栈不直接返回前端。
- 每个请求生成 `request_id`，便于排查问题。

### 11.3 DTO 与接口文档

- 为请求和响应定义独立 DTO。
- 不直接把 GORM Model 作为所有接口的最终响应。
- 增加 OpenAPI 文档。
- 前端类型由接口文档生成或统一维护。
- 明确字段是否可空、时间格式、枚举范围和分页结构。

### 11.4 REST 行为一致性

- POST 关注资源只负责新增或保证已存在。
- DELETE 只负责删除或保证已删除。
- 不再让 POST 和 DELETE 都执行 Toggle。
- 更新接口使用 PUT 或 PATCH，并定义清晰字段语义。
- 批量操作提供部分失败结果和失败原因。

---

## 12. P1：前端工程质量

### 12.1 消除 `any`

优先顺序：

1. `frontend/src/api/admin.ts`
2. 登录、注册和个人资料 API
3. 比赛、球队、积分榜 API
4. Pinia Store
5. 管理端页面

建议定义：

```ts
interface ApiResponse<T> {
  code: string | number
  message: string
  data: T
  request_id?: string
}

interface PaginatedData<T> {
  list: T[]
  total: number
  page: number
  page_size: number
}
```

### 12.2 API 层职责

- API 文件只负责请求和响应类型。
- Store 负责缓存、加载状态和跨页面共享。
- 页面负责展示和局部交互。
- 数据归一化逻辑集中管理，禁止各页面重复转换。
- 统一处理 401，但要保留安全的登录回跳地址。

### 12.3 代码质量工具

- 增加 ESLint。
- 增加 Prettier 或统一格式化规则。
- 增加 Stylelint 或 CSS 基础检查。
- 增加提交前检查。
- 删除不再引用的 mock 数据。
- 清理临时备份文件和已跟踪构建产物。

---

## 13. P1：后端工程质量与数据库管理

### 13.1 数据库迁移

- 开发环境可以保留便捷初始化能力。
- 正式环境改用版本化迁移文件。
- 每个迁移具有明确版本和回滚说明。
- 唯一索引、状态字段和审计表通过迁移创建。
- Seed 只负责开发示例数据，不在正式启动流程中自动写入。
- 球队中文名和球场映射改为显式数据修复任务。

### 13.2 索引优化

建议确认或新增组合索引：

- 比赛：`competition_id + season + kickoff_time_utc`
- 比赛：`competition_id + season + matchday`
- 比赛：`external_provider + external_id` 唯一索引
- 提醒：`status + remind_at`
- 提醒：`user_id + match_id + minutes + channel` 唯一索引
- 通知：`user_id + is_read + created_at`
- 积分榜：`competition_id + season + type + position`

所有索引需要通过真实查询计划验证，避免无效或重复索引。

### 13.3 服务关闭与连接管理

- HTTP 服务增加读取、写入和空闲超时。
- 支持系统信号触发优雅关闭。
- 停止接收新请求后等待进行中的请求完成。
- 停止后台任务并释放 ticker。
- 关闭数据库和 Redis 连接。

---

## 14. P1：自动化测试与持续集成

### 14.1 后端测试

#### 单元测试

- 密码和 JWT。
- 球队中文本地化。
- 比赛状态映射。
- 时区与提醒时间计算。
- 积分榜重算。
- 同步重试和限流处理。

#### Repository 测试

- 用户数据隔离。
- 通知已读权限。
- 提醒唯一约束。
- 比赛同步 Upsert。
- 分页、筛选和组合索引查询。

#### Handler 集成测试

- 注册、登录、刷新和退出。
- 用户、管理员权限。
- 后台 CRUD。
- 收藏、提醒和通知。
- 参数错误和标准 HTTP 状态码。

### 14.2 前端测试

#### 组件测试

- 比赛状态和比分展示。
- 队徽失败回退。
- 提醒设置。
- 联赛切换弹窗。
- 时区切换后时间更新。

#### 页面测试

- 登录与登录回跳。
- 联赛切换后数据范围更新。
- 赛程轮次筛选。
- 关注球队和收藏比赛。
- 管理端权限守卫。

#### 端到端测试

- 注册登录 → 关注球队 → 收藏比赛 → 设置提醒。
- 管理员登录 → 修改比赛 → 用户端查看变化。
- 切换六项赛事并检查移动端无溢出。
- Token 失效后的重新登录流程。

### 14.3 持续集成检查

每次提交至少执行：

```text
前端类型检查
前端生产构建
前端单元测试
后端格式检查
后端静态检查
后端单元与集成测试
数据库迁移检查
敏感信息扫描
```

---

## 15. P2：性能优化

### 15.1 前端加载性能

- 保持页面级路由懒加载。
- 对后台组件继续按页面拆包。
- 字体继续使用 WOFF2 和 Unicode Range。
- 对哈希静态资源配置长期缓存。
- HTML 使用短缓存或不缓存。
- 队徽和头像使用懒加载、固定尺寸和错误回退。
- 大型列表使用分页或虚拟列表。
- 使用性能预算监控首屏资源大小。

建议性能预算：

| 指标 | 目标 |
| --- | --- |
| 移动端首屏主要 JS（gzip） | 不高于 200 KB |
| 首屏 CSS（gzip） | 不高于 70 KB |
| LCP | 2.5 秒内 |
| CLS | 小于 0.1 |
| INP | 200 毫秒内 |

### 15.2 后端性能

- 首页使用聚合接口减少请求数量。
- 热门赛事、积分榜和赛事概览可使用短时缓存。
- 比赛详情和实时数据使用较短缓存策略。
- 管理接口不使用公共缓存。
- 检查 GORM Preload 是否产生不必要查询。
- 为慢查询设置阈值并记录请求 ID。

---

## 16. P2：无障碍与交互完善

### 16.1 赛事切换弹窗

- 打开时焦点移动到弹窗标题或当前赛事。
- Tab 焦点限制在弹窗内。
- Escape 关闭。
- 关闭后焦点返回触发按钮。
- 补充弹窗打开和赛事切换的读屏提示。

### 16.2 键盘和语义

- 可点击卡片优先使用 `<button>` 或 `<a>`。
- 如果必须使用 article，补充正确的 role 和键盘行为。
- 所有图标按钮提供可读名称。
- 错误信息与表单输入通过 `aria-describedby` 关联。
- 加载状态使用 `aria-busy` 或礼貌播报。

### 16.3 动效

- 保留现有 `prefers-reduced-motion` 支持。
- 页面切换不阻塞操作。
- 比分变化可以使用轻量高亮，不使用持续闪烁。
- 深浅色和联赛切换避免大面积突兀闪白。

---

## 17. P2：产品能力增强

### 17.1 Web Push 与 PWA

- 支持添加到主屏幕。
- 支持系统级比赛提醒。
- 用户可以控制提醒渠道和时间。
- 通知点击后进入对应比赛详情。
- 处理通知权限被拒绝和浏览器不支持的情况。

### 17.2 关注球队自动提醒

- 用户可以开启“关注球队全部比赛提醒”。
- 新赛程同步后自动为关注球队创建提醒。
- 支持仅焦点比赛、全部比赛或关闭。
- 赛程变化后自动调整提醒时间。

### 17.3 更丰富的实时比赛页

外部数据源支持时可逐步增加：

- 比赛事件时间线
- 进球、红黄牌、换人
- 阵容与首发
- 技术统计
- 近期交锋
- 球队近期状态

这些能力必须以数据源授权和字段稳定性为前提。

---

## 18. 可观测性与运营支持

### 18.1 日志

- 使用结构化日志。
- 每个请求包含 request ID。
- 记录接口、状态码、耗时、用户 ID 和管理员 ID。
- 敏感字段统一脱敏。
- 同步和提醒任务使用独立日志字段。

### 18.2 健康检查

建议增加：

```text
GET /health/live
GET /health/ready
```

- `live`：进程是否正常响应。
- `ready`：数据库和 Redis 是否可用。
- 外部赛事供应商不可用不应直接导致基础查询服务不可用。

### 18.3 管理运营视图

- 最近同步时间。
- 下一次同步时间。
- 各赛事比赛和球队数量。
- 同步失败次数。
- 待发送、失败提醒数量。
- 用户、关注和提醒增长数据。
- 数据缺失和异常比赛列表。

---

## 19. 分阶段实施计划

### 阶段一：安全与真实性

优先完成：

1. 修复通知越权。
2. 增加登录限流和密码策略。
3. 增加前端用户与管理员路由守卫。
4. 清理上传文件、临时备份文件和已跟踪构建产物。
5. 补全后台所有占位接口的数据库操作。
6. 后台 KPI 改为真实数据。

阶段验收：

- 不存在已知个人数据越权。
- 后台所有可见操作都能真正生效。
- 普通用户无法访问后台数据。
- 仓库中不包含用户运行数据和敏感配置。

### 阶段二：数据正确性与实时性

优先完成：

1. 主题与业务数据分离。
2. 动态赛季、轮次和进度。
3. 统一时区。
4. 修复直播分钟和比赛状态。
5. 增加直播轮询。
6. 赛程改为后端分页和动态轮次。

阶段验收：

- 前端不再显示固定轮次和固定进度。
- 所有时间都遵循用户选择时区。
- 直播状态能在合理时间内更新。
- 赛事快速切换不会出现旧数据覆盖。

### 阶段三：同步、提醒与 API 基础设施

优先完成：

1. 比赛外部标识唯一约束。
2. 同步重试、限流和任务锁。
3. 提醒任务原子认领和重试。
4. 正确 HTTP 状态码与业务错误码。
5. DTO、OpenAPI 和前端类型统一。
6. 数据库版本化迁移。

阶段验收：

- 重复同步不产生重复数据。
- 同一提醒不会重复发送。
- API 错误能被监控和前端正确识别。
- 接口变更能够在编译或测试阶段发现。

### 阶段四：测试、性能与体验增强

优先完成：

1. 后端权限、同步、提醒集成测试。
2. 前端组件和端到端测试。
3. 自动化质量检查。
4. 静态资源缓存和列表性能优化。
5. 弹窗焦点管理与无障碍完善。
6. 推送通知和 PWA 能力评估与开发。

阶段验收：

- 核心用户链路具备自动化回归测试。
- 六项赛事在主流移动端尺寸无横向溢出。
- 核心性能指标达到预算。
- 键盘用户能够完成主要操作。

---

## 20. 建议优先修改的文件

### 后端

- `backend/internal/handlers/admin_handler.go`
- `backend/internal/handlers/notification_handler.go`
- `backend/internal/repositories/notification_repo.go`
- `backend/internal/services/auth_service.go`
- `backend/internal/utils/jwt.go`
- `backend/internal/utils/response.go`
- `backend/internal/models/match.go`
- `backend/internal/models/reminder.go`
- `backend/internal/services/sync_service.go`
- `backend/internal/services/league_sync_service.go`
- `backend/internal/services/reminder_service.go`
- `backend/internal/jobs/reminder_scanner.go`
- `backend/internal/routes/router.go`
- `backend/cmd/server/main.go`

### 前端

- `frontend/src/router/index.ts`
- `frontend/src/api/request.ts`
- `frontend/src/api/admin.ts`
- `frontend/src/types/match.ts`
- `frontend/src/utils/timezone.ts`
- `frontend/src/data/leagueTheme.ts`
- `frontend/src/data/themeAdapters.ts`
- `frontend/src/stores/useAuthStore.ts`
- `frontend/src/stores/useSettingStore.ts`
- `frontend/src/pages/user/HomePage.vue`
- `frontend/src/pages/user/SchedulePage.vue`
- `frontend/src/pages/user/MatchDetailPage.vue`
- `frontend/src/pages/user/ProfilePage.vue`
- `frontend/src/pages/admin/`
- `frontend/src/components/theme/CompetitionDialog.vue`

### 项目配置与文档

- `.gitignore`
- `README.md`
- 数据库迁移目录
- 自动化检查配置目录

---

## 21. 总体验收清单

### 安全

- [ ] 用户只能访问和修改自己的通知、提醒、收藏和个人资料。
- [ ] 管理接口只有管理员能够访问。
- [ ] 登录和注册具备限流。
- [ ] 密码策略达到正式产品要求。
- [ ] 敏感错误和内部结构不会返回前端。
- [ ] 仓库中不包含用户上传文件和密钥。

### 功能

- [ ] 后台所有按钮均有真实功能。
- [ ] 所有 CRUD 操作刷新页面后仍然生效。
- [ ] 赛事赛季、轮次、进度均来自后端。
- [ ] 时区设置对全部比赛时间生效。
- [ ] 直播分钟和比赛状态正确显示。
- [ ] English 选项已正式实现或暂时移除。

### 数据

- [ ] 重复同步不会产生重复比赛或球队。
- [ ] 同一提醒不会重复发送。
- [ ] 同步错误支持重试并可在后台查看。
- [ ] 积分榜重算结果具备自动化测试。
- [ ] 数据库变更通过版本化迁移执行。

### 工程质量

- [ ] 前端核心 API 不再使用 `any`。
- [ ] API 使用正确 HTTP 状态码和稳定错误码。
- [ ] 核心用户链路具备端到端测试。
- [ ] 后端权限和任务并发具备集成测试。
- [ ] 每次提交自动执行类型、构建、测试和敏感信息检查。

### 体验与性能

- [ ] 六项赛事移动端和桌面端均无布局溢出。
- [ ] 赛事切换不会出现旧数据覆盖。
- [ ] 直播期间页面能够自动更新。
- [ ] 弹窗具备完整焦点管理。
- [ ] 首屏资源和 Web Vitals 达到性能预算。

---

## 22. 最终目标

完成本方案后，WorldCup Mate 应达到以下状态：

1. 用户端展示的赛季、轮次、比分、进度和时间全部可信。
2. 用户关注、收藏、提醒和通知形成稳定的个人观赛闭环。
3. 后台能够真正维护球队、比赛、积分和同步任务。
4. 数据同步与提醒任务可以安全重复执行并从失败中恢复。
5. 权限边界清晰，不存在跨用户数据操作。
6. 前后端接口有稳定契约，核心流程有自动化测试保护。
7. 项目可以持续迭代，而不依赖演示数据、静态轮次和人工修库。
