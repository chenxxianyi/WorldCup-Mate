# WorldCup Mate V2 自用版 AI 助手前端开发方案

## 1. 文档目标

本文档从 `AI_DEVELOPMENT_PLAN.md` 和 `AI_DEVELOPMENT_TASK_PLAN.md` 中拆出前端部分，并结合当前 `frontend/` 目录结构和已实现代码，作为 V2 自用版 AI 看球助手的前端开发执行方案。

V2 前端目标不是重做现有应用，而是在已有用户端页面和管理端基础上，增加一组面向个人使用的 AI 观赛能力：

- 比赛详情页展示 AI 看点。
- 首页或 `/ai` 页面展示“今天看什么”AI 推荐。
- 积分榜页展示小组出线 AI 解读。
- 提供 AI 聊天助手。
- 提供分享文案生成页面。

前端不直接调用模型供应商 API，不保存 API Key，不负责 Prompt 拼装和事实上下文构建。所有 AI 能力统一通过后端 `/api/ai/*` 接口访问。

## 2. 当前前端基础

当前项目已经具备：

- Vue 3 + TypeScript + Vite。
- Pinia 状态管理。
- Vue Router 双布局：`UserLayout` 和 `AdminLayout`。
- Axios 请求封装：`frontend/src/api/request.ts`，统一处理 `{ code, message, data }`。
- 用户端页面：`HomePage`、`SchedulePage`、`MatchDetailPage`、`TeamsPage`、`TeamDetailPage`、`StandingsPage`、`ProfilePage`、`LoginPage`。
- 管理端页面：`AdminDashboard`、`AdminTeamsPage`、`AdminMatchesPage`、`AdminStandingsPage`、`AdminSyncPage`。
- 通用组件：`MatchCard`、`StandingTable`、`ReminderControl`、`NotificationList`、`PageHeader` 等。
- store：`useMatchStore`、`useTeamStore`、`useFavoriteStore`、`useReminderStore`、`useNotificationStore`、`useSettingStore`、`useAuthStore`。

需要注意：

- 当前部分前端中文文案存在历史编码异常，AI 前端新增文案必须使用正常 UTF-8 中文。
- 新增 AI 页面和组件应沿用 `frontend/src/styles/main.css` 中的主题变量、卡片、按钮和响应式风格。
- AI 接口失败不能影响现有比赛详情、赛程、积分榜等主功能展示。

## 3. 前端范围边界

### 必须实现

- `frontend/src/types/ai.ts`
- `frontend/src/api/ai.ts`
- `frontend/src/stores/useAIStore.ts`
- `frontend/src/components/ai/MatchInsightCard.vue`
- `frontend/src/pages/user/AIHomePage.vue`
- `frontend/src/pages/user/AIChatPage.vue`
- `frontend/src/pages/user/AIShareCopyPage.vue`
- 比赛详情页接入 AI 看点。
- 积分榜页接入小组出线 AI 解读。
- 路由新增 `/ai`、`/ai/chat`、`/ai/match/:id`、`/ai/share-copy`。

### 暂缓实现

- 流式输出。
- 语音问答。
- 完整 AI 历史管理中心。
- AI 后台运营页面。
- Token 报表和复杂统计。
- 复杂 RAG、新闻聚合和投注建议。
- 公网多用户商业化功能。

## 4. 建议目录结构

在当前 `frontend/src/` 下新增：

```text
frontend/src/
├── api/
│   └── ai.ts
├── components/
│   └── ai/
│       ├── AIChatPanel.vue
│       ├── AIInputBox.vue
│       ├── AIMessageBubble.vue
│       ├── AIThinking.vue
│       ├── GroupAnalysisCard.vue
│       ├── MatchInsightCard.vue
│       ├── PromptSuggestionCard.vue
│       ├── ShareCopyCard.vue
│       └── TodayRecommendationCard.vue
├── pages/
│   └── user/
│       ├── AIChatPage.vue
│       ├── AIHomePage.vue
│       ├── AIMatchInsightPage.vue
│       └── AIShareCopyPage.vue
├── stores/
│   └── useAIStore.ts
└── types/
    └── ai.ts
```

第一阶段最小闭环只需要：

- `types/ai.ts`
- `api/ai.ts`
- `stores/useAIStore.ts`
- `components/ai/MatchInsightCard.vue`
- 修改 `pages/user/MatchDetailPage.vue`

## 5. 路由设计

修改文件：

```text
frontend/src/router/index.ts
```

新增用户端子路由：

```text
/ai                 AI 首页，今日推荐和快捷入口
/ai/chat            AI 聊天助手
/ai/match/:id       独立比赛 AI 看点页，可选
/ai/share-copy      分享文案生成
```

路由建议：

- `/ai` 放在 `UserLayout` 下，保持移动端底部导航和桌面侧栏一致。
- `/ai/match/:id` 可以第二阶段再做；第一阶段先在 `MatchDetailPage.vue` 内嵌 `MatchInsightCard`。
- AI 页面可不强制登录，但如果后端要求登录，前端应显示登录引导，不阻塞其他页面。

## 6. API 封装

新增文件：

```text
frontend/src/api/ai.ts
```

所有请求继续使用 `request.ts`，不要绕过 `/api` 代理。

建议方法：

```ts
export function apiGenerateMatchInsight(payload: MatchInsightRequest) {
  return request.post('/ai/match-insight', payload)
}

export function apiGenerateTodayRecommendations(payload: TodayRecommendationRequest) {
  return request.post('/ai/today-recommendations', payload)
}

export function apiGenerateGroupAnalysis(payload: GroupAnalysisRequest) {
  return request.post('/ai/group-analysis', payload)
}

export function apiExplainFootball(payload: ExplainRequest) {
  return request.post('/ai/explain', payload)
}

export function apiGenerateShareCopy(payload: ShareCopyRequest) {
  return request.post('/ai/share-copy', payload)
}

export function apiSendAIChat(payload: AIChatRequest) {
  return request.post('/ai/chat', payload)
}

export function apiListAIConversations() {
  return request.get('/ai/conversations')
}

export function apiGetAIConversation(id: number) {
  return request.get(`/ai/conversations/${id}`)
}

export function apiDeleteAIConversation(id: number) {
  return request.delete(`/ai/conversations/${id}`)
}
```

## 7. 类型定义

新增文件：

```text
frontend/src/types/ai.ts
```

第一阶段核心类型：

```ts
export interface MatchInsightRequest {
  match_id: number
  force_refresh?: boolean
}

export interface MatchInsight {
  summary: string
  watch_rating: number
  reasons: string[]
  team_comparison: string[]
  beginner_tips: string[]
  qualification_impact: string
  should_stay_up: string
  suitable_for: string[]
  generated_at?: string
}
```

后续扩展类型：

- `TodayRecommendationRequest`
- `TodayRecommendation`
- `GroupAnalysisRequest`
- `GroupAnalysis`
- `ExplainRequest`
- `ShareCopyRequest`
- `ShareCopyResult`
- `AIChatRequest`
- `AIChatMessage`
- `AIConversation`

类型命名规则：

- 前端字段尽量直接跟后端 JSON 字段一致，减少转换成本。
- 如需展示友好字段，可在组件内计算，不污染接口类型。
- 可复用 `frontend/src/types/match.ts`、`team.ts`、`standing.ts` 中已有类型。

## 8. Pinia Store

新增文件：

```text
frontend/src/stores/useAIStore.ts
```

第一阶段 state：

- `currentMatchInsight`
- `matchInsightLoading`
- `matchInsightError`

第一阶段 action：

- `generateMatchInsight(matchId: number, forceRefresh?: boolean)`
- `clearMatchInsight()`

后续扩展 state：

- `todayRecommendations`
- `groupAnalysisMap`
- `chatMessages`
- `conversations`
- `shareCopyResult`

实现要求：

- 每个 AI 功能有独立 loading 和 error，不要共用单个全局 loading。
- API 失败时保留页面原有数据，不清空比赛详情或积分榜主数据。
- 错误提示使用正常中文，并避免暴露后端内部错误细节。

## 9. 组件设计

### MatchInsightCard

新增文件：

```text
frontend/src/components/ai/MatchInsightCard.vue
```

展示内容：

- 一句话总结。
- 看点指数，使用 1 到 5 或 0 到 100 的视觉表达。
- 推荐理由。
- 双方对比。
- 小白观赛重点。
- 出线影响。
- 是否值得熬夜。
- 适合人群。

状态：

- 空状态：提示可生成 AI 看点。
- Loading：展示简洁加载状态。
- Error：展示失败信息和重试按钮。
- Success：展示结构化内容。

交互：

- “生成看点”按钮。
- “重新生成”按钮，传 `force_refresh=true`。
- 可选“复制摘要”按钮。

### TodayRecommendationCard

用于 `/ai` 首页展示今日推荐：

- 今日推荐比赛列表。
- “只看一场”推荐。
- 推荐理由。
- 跳转比赛详情。

### GroupAnalysisCard

用于积分榜页：

- 小组出线形势总结。
- 前二规则和最佳第三名规则解释。
- 风险提示：数据不足时明确说明。

### AIChatPanel

用于 `/ai/chat`：

- 消息列表。
- 输入框。
- 快捷问题。
- Loading 状态。
- 复制回答。

第一版不做流式输出，提交后等待完整响应。

### ShareCopyCard

用于 `/ai/share-copy`：

- 比赛选择。
- 平台选择。
- 语气选择。
- 长度选择。
- 文案结果展示。
- 复制按钮。

## 10. 页面接入方案

### 10.1 比赛详情页 AI 看点

修改文件：

```text
frontend/src/pages/user/MatchDetailPage.vue
```

接入方式：

- 在比赛基础信息卡片下方增加 AI 看点区域。
- 引入 `MatchInsightCard` 和 `useAIStore`。
- 使用当前路由 `match.id` 调用 `generateMatchInsight`。
- AI 区域失败时不影响比赛基础信息、小组积分和提醒收藏。

验收标准：

- 打开比赛详情页，基础信息正常展示。
- 点击生成后出现 loading。
- Mock 后端可返回结构化 AI 看点。
- 后端失败时显示可重试错误。
- `npm run build` 不因新增类型失败。

### 10.2 AI 首页

新增文件：

```text
frontend/src/pages/user/AIHomePage.vue
```

功能：

- 显示“今天看什么”AI 推荐入口。
- 展示 1 到 3 场推荐比赛。
- 提供快捷问题入口：规则解释、小组出线、分享文案。
- 跳转 `/ai/chat`、`/ai/share-copy`、比赛详情页。

验收标准：

- `/ai` 可打开。
- 无比赛时显示空状态。
- API 失败时不白屏。
- 移动端布局可读。

### 10.3 积分榜页 AI 解读

修改文件：

```text
frontend/src/pages/user/StandingsPage.vue
```

功能：

- 每个小组增加“AI 解读”入口。
- 调用 `/api/ai/group-analysis`。
- 展示 `GroupAnalysisCard`。

验收标准：

- 积分榜原表格不受影响。
- 每个小组可单独 loading。
- 无积分数据时 AI 明确说明数据不足。

### 10.4 AI 聊天页

新增文件：

```text
frontend/src/pages/user/AIChatPage.vue
```

功能：

- 支持输入自然语言问题。
- 支持快捷问题。
- 支持上下文类型：`general`、`match`、`team`、`group`。
- 登录用户可读取历史会话。

验收标准：

- 能发送消息并显示回答。
- 非流式返回即可。
- 聊天失败不清空历史。
- 复制回答可用。

### 10.5 分享文案页

新增文件：

```text
frontend/src/pages/user/AIShareCopyPage.vue
```

功能：

- 选择比赛。
- 选择平台：朋友圈、微信群、小红书、微博、通用。
- 选择语气：轻松、热血、专业、小白友好。
- 选择长度：短、中、长。
- 生成并复制文案。

验收标准：

- 文案生成成功后可复制。
- 失败提示友好。
- 不出现投注、侵权媒体内容或虚构事实。

## 11. 开发阶段拆分

### F0 基线检查

目标：

- 确认当前前端构建状态。
- 记录现有失败，不和 AI 改动混在一起。

命令：

```powershell
cd frontend
npm run build
```

验收：

- 构建通过，或明确记录现有失败原因。
- 当前中文乱码问题已标注为独立数据/文案治理任务。

### F1 比赛 AI 看点最小闭环

任务：

- 新增 `types/ai.ts`。
- 新增 `api/ai.ts`。
- 新增 `stores/useAIStore.ts`。
- 新增 `components/ai/MatchInsightCard.vue`。
- 修改 `MatchDetailPage.vue` 接入 AI 看点。

依赖后端：

- `POST /api/ai/match-insight`

验收：

- Mock Provider 下可生成比赛看点。
- 比赛详情主内容不受 AI 接口失败影响。
- 前端构建通过或记录无关既有失败。

### F2 AI 首页与今日推荐

任务：

- 新增 `AIHomePage.vue`。
- 新增 `TodayRecommendationCard.vue`。
- 修改 `router/index.ts` 增加 `/ai`。
- `useAIStore` 增加今日推荐状态和 action。

依赖后端：

- `POST /api/ai/today-recommendations`

验收：

- `/ai` 可展示今日推荐。
- 可跳转比赛详情。
- 无比赛和接口失败都有空状态。

### F3 积分榜小组 AI 解读

任务：

- 新增 `GroupAnalysisCard.vue`。
- 修改 `StandingsPage.vue`。
- `useAIStore` 增加 group analysis map。

依赖后端：

- `POST /api/ai/group-analysis`

验收：

- 每个小组可独立生成出线解读。
- 积分榜原有功能不受影响。

### F4 AI 聊天助手

任务：

- 新增 `AIChatPage.vue`。
- 新增 `AIChatPanel.vue`、`AIMessageBubble.vue`、`AIInputBox.vue`、`AIThinking.vue`。
- 修改路由增加 `/ai/chat`。
- `useAIStore` 增加聊天状态和会话操作。

依赖后端：

- `POST /api/ai/chat`
- `GET /api/ai/conversations`
- `GET /api/ai/conversations/:id`
- `DELETE /api/ai/conversations/:id`

验收：

- 可以发送问题并显示回答。
- 可以查看会话列表和会话详情。
- 可以删除会话。

### F5 分享文案生成

任务：

- 新增 `AIShareCopyPage.vue`。
- 新增 `ShareCopyCard.vue`。
- 修改路由增加 `/ai/share-copy`。

依赖后端：

- `POST /api/ai/share-copy`

验收：

- 可以选择比赛、平台、语气、长度。
- 可以生成文案并复制。
- 失败提示友好。

### F6 文档与最终检查

任务：

- 更新 README 中 AI 前端入口说明。
- 记录 Mock Provider 使用方式。
- 执行前端构建。

验收：

- `npm run build` 通过，或记录明确的既有失败。
- 文档能指导下一次继续开发。

## 12. 前后端接口契约

前端依赖后端统一响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

前端显示策略：

- `code === 0`：展示 `data`。
- `401`：由 `request.ts` 统一处理登录状态。
- `403`：提示没有权限或需要登录。
- `500` 或网络错误：提示“AI 服务暂时不可用，请稍后再试”。

AI 功能不要在前端写死模型返回内容。Mock 内容由后端 Mock Provider 提供。

## 13. UI 与交互要求

- 移动端优先，兼容桌面端。
- AI 模块视觉上应融入现有卡片和页面，不做独立营销落地页。
- 不使用大段说明文堆满页面，优先用按钮、卡片、状态和结果引导。
- AI 结果要结构化展示，避免整段长文。
- Loading、空状态、错误状态必须完整。
- 复制按钮要有成功反馈。
- 所有新增中文文案必须正常 UTF-8。

## 14. 质量要求

- 不直接访问 OpenAI 或其他模型供应商。
- 不把 API Key、模型名等敏感信息写入前端。
- 新增类型必须和后端返回字段一致。
- 新增 store 不能污染现有 match/team/standing store。
- AI 页面失败不能造成主应用白屏。
- 不在前端编造比分、赛程、伤病、新闻、投注建议。

## 15. 最小闭环清单

如果只想最快看到效果，前端按这个顺序做：

1. 新增 `frontend/src/types/ai.ts`。
2. 新增 `frontend/src/api/ai.ts`。
3. 新增 `frontend/src/stores/useAIStore.ts`。
4. 新增 `frontend/src/components/ai/MatchInsightCard.vue`。
5. 修改 `frontend/src/pages/user/MatchDetailPage.vue`。
6. 等后端 Mock `/api/ai/match-insight` 可用后联调。
7. 运行 `npm run build`。

## 16. 开发进度记录

### F1.1 AI 类型、API 和 Store

状态：已完成。

修改文件：

- `frontend/src/types/ai.ts`
- `frontend/src/api/ai.ts`
- `frontend/src/stores/useAIStore.ts`

实现内容：

- 新增比赛 AI 看点、今日推荐、小组分析、解释、分享文案、聊天会话等类型。
- 新增 `/api/ai/*` 请求封装，继续复用现有 `request.ts`。
- 新增 `useAIStore`，按功能拆分 loading/error 状态，避免 AI 接口失败影响现有核心页面。

### F1.2 AI 通用组件

状态：已完成。

修改文件：

- `frontend/src/components/ai/AIThinking.vue`
- `frontend/src/components/ai/AIMessageBubble.vue`
- `frontend/src/components/ai/AIInputBox.vue`
- `frontend/src/components/ai/PromptSuggestionCard.vue`
- `frontend/src/components/ai/MatchInsightCard.vue`
- `frontend/src/components/ai/TodayRecommendationCard.vue`
- `frontend/src/components/ai/GroupAnalysisCard.vue`
- `frontend/src/components/ai/AIChatPanel.vue`
- `frontend/src/components/ai/ShareCopyCard.vue`
- `frontend/src/api/index.ts`

实现内容：

- 新增 AI 加载、空状态、错误、复制、聊天输入和结构化展示组件。
- 组件样式复用现有 `card`、`pill-btn`、`tag`、`icon-action` 和主题变量，保持与现有项目风格一致。
- 导出 AI API，方便后续页面和 store 统一引用。

### F2 AI 页面与路由入口

状态：已完成。

修改文件：

- `frontend/src/pages/user/AIHomePage.vue`
- `frontend/src/pages/user/AIChatPage.vue`
- `frontend/src/pages/user/AIShareCopyPage.vue`
- `frontend/src/pages/user/AIMatchInsightPage.vue`
- `frontend/src/router/index.ts`
- `frontend/src/components/common/BottomTabBar.vue`
- `frontend/src/components/common/DesktopRail.vue`

实现内容：

- 新增 `/ai`、`/ai/chat`、`/ai/match/:id`、`/ai/share-copy` 路由。
- 新增 AI 首页、聊天页、分享文案页和独立比赛看点页。
- 移动端底部导航和桌面侧栏增加 AI 入口，并使用现有 Material Symbols 图标风格。

### F3 业务页面 AI 接入

状态：已完成。

修改文件：

- `frontend/src/pages/user/MatchDetailPage.vue`
- `frontend/src/pages/user/StandingsPage.vue`
- `frontend/src/components/common/StandingTable.vue`
- `frontend/src/types/standing.ts`

实现内容：

- 比赛详情页接入 `MatchInsightCard`，支持生成和重新生成比赛 AI 看点。
- 积分榜页接入 `GroupAnalysisCard`，每个小组可独立生成 AI 出线解读。
- 整理积分状态类型和积分表中文展示，避免新增 AI 展示继续依赖历史乱码状态。

### F4 前端构建验证

状态：已完成。

验证命令：

```powershell
cd frontend
npm run build
```

验证结果：

- `vue-tsc -b` 通过。
- `vite build` 通过。
- 已生成生产构建产物到 `frontend/dist/`。
