# WorldCup Mate 前端开发方案

## 1. 项目定位

WorldCup Mate 是一个面向普通球迷的「2026 世界杯赛程助手」Web App。前端以移动端优先为核心，同时兼容平板和桌面端，提供赛程查询、球队关注、本地时间换算、比赛提醒、小组积分榜、最佳第三名榜、热门比赛推荐以及管理后台界面。

前端交付目标是完成一个可运行的 MVP，包含用户端、管理后台、PWA、深色模式、响应式适配和基础测试。

## 2. 技术栈

- Vue 3
- Vite
- TypeScript
- Vue Router
- Pinia
- Element Plus
- Tailwind CSS
- Day.js + timezone 插件
- ECharts
- PWA 支持

## 3. 推荐目录结构

```text
frontend/
├── public/
│   ├── icons/
│   └── manifest.json
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   │   ├── common/
│   │   ├── match/
│   │   ├── team/
│   │   ├── standing/
│   │   └── admin/
│   ├── layouts/
│   ├── pages/
│   │   ├── user/
│   │   └── admin/
│   ├── router/
│   ├── stores/
│   ├── styles/
│   ├── types/
│   ├── utils/
│   ├── App.vue
│   └── main.ts
├── package.json
├── tailwind.config.ts
├── tsconfig.json
└── vite.config.ts
```

## 4. 页面范围

### 用户端页面

- `/` 首页：今日比赛、明日比赛、下一场倒计时、我关注球队、热门推荐比赛。
- `/schedule` 赛程页：支持日期、球队、小组、阶段、城市、状态和关键词筛选。
- `/matches/:id` 比赛详情页：展示球队、比分、状态、本地时间、UTC 时间、城市、球场、小组、收藏、提醒、相关比赛。
- `/teams` 球队列表页：展示 48 支球队，支持大洲、小组、关键词筛选和关注操作。
- `/teams/:id` 球队详情页：展示球队资料、小组排名、积分、赛程、结果和晋级状态。
- `/standings` 积分榜页：展示 Group A 到 Group L 积分榜和最佳第三名榜。
- `/favorites` 我的关注页：展示关注球队、收藏比赛和关注球队的下一场比赛。
- `/reminders` 我的提醒页：展示、修改和删除比赛提醒。
- `/profile` 我的页面：用户资料、关注、收藏、提醒、时区、主题和退出登录。
- `/settings` 设置页：时区、语言、主题等偏好设置。
- `/login` 登录页。
- `/register` 注册页。

### 管理后台页面

- `/admin/login` 后台登录。
- `/admin/dashboard` 数据看板。
- `/admin/teams` 球队管理。
- `/admin/groups` 小组管理。
- `/admin/cities` 城市管理。
- `/admin/stadiums` 球场管理。
- `/admin/matches` 比赛管理、比分更新、CSV 导入。
- `/admin/standings` 积分榜管理和重算。
- `/admin/recommendations` 推荐比赛管理。
- `/admin/users` 用户管理。
- `/admin/settings` 系统设置。

## 5. 路由与布局

前端采用用户端和管理后台双布局：

- 用户端使用 `UserLayout`，移动端底部 Tab 导航，桌面端顶部或左侧导航。
- 管理后台使用 `AdminLayout`，桌面端侧边栏导航，适合高密度数据管理。
- 需要登录的用户端页面通过路由守卫校验 JWT。
- `/admin/*` 页面通过路由守卫校验管理员身份。

移动端底部导航包含：

- 首页
- 赛程
- 球队
- 积分榜
- 我的

## 6. 状态管理

使用 Pinia 建立以下 store：

### useAuthStore

- `token`
- `user`
- `login`
- `register`
- `logout`
- `fetchProfile`
- `isAdmin`

### useMatchStore

- `matches`
- `todayMatches`
- `tomorrowMatches`
- `upcomingMatches`
- `recommendedMatches`
- `fetchMatches`
- `fetchTodayMatches`
- `fetchTomorrowMatches`
- `fetchRecommendedMatches`
- `fetchMatchDetail`

### useTeamStore

- `teams`
- `teamDetail`
- `fetchTeams`
- `fetchTeamDetail`
- `fetchTeamMatches`

### useFavoriteStore

- `favoriteTeams`
- `favoriteMatches`
- `fetchFavoriteTeams`
- `fetchFavoriteMatches`
- `addFavoriteTeam`
- `removeFavoriteTeam`
- `addFavoriteMatch`
- `removeFavoriteMatch`

### useReminderStore

- `reminders`
- `fetchReminders`
- `createReminder`
- `updateReminder`
- `deleteReminder`

### useSettingStore

- `theme`
- `timezone`
- `language`
- `setTheme`
- `setTimezone`
- `setLanguage`

## 7. 通用组件设计

需要优先封装以下可复用组件：

- `MatchCard`：比赛卡片，支持比分、状态、时间、收藏、提醒。
- `TeamCard`：球队卡片，支持国旗、名称、小组、大洲、关注按钮。
- `StandingTable`：积分榜表格。
- `DateFilter`：日期筛选器。
- `StageFilter`：阶段筛选器。
- `TeamFilter`：球队筛选器。
- `ReminderButton`：提醒按钮和提醒设置弹窗入口。
- `FavoriteButton`：收藏按钮，兼容球队和比赛。
- `Countdown`：下一场比赛倒计时。
- `EmptyState`：空状态组件。
- `LoadingSkeleton`：加载骨架屏。
- `PageHeader`：页面标题和辅助操作区。
- `BottomTabBar`：移动端底部导航。
- `AdminTable`：后台通用表格。
- `AdminFormDialog`：后台通用表单弹窗。

## 8. API 接入规范

在 `src/api` 中按领域拆分 API 模块：

- `auth.ts`
- `matches.ts`
- `teams.ts`
- `groups.ts`
- `standings.ts`
- `favorites.ts`
- `reminders.ts`
- `notifications.ts`
- `admin.ts`

统一使用请求封装：

- 自动附带 JWT。
- 统一处理 `code/message/data` 响应结构。
- 统一处理 401、403、404、500。
- 支持分页参数 `page`、`pageSize`。
- 管理后台接口统一走 `/api/admin/*`。

## 9. 时间与时区

后端只返回 UTC 时间字段 `kickoff_time_utc`。前端负责将其转换为本地显示时间：

- 默认使用浏览器时区。
- 登录用户优先使用用户设置的 `timezone`。
- 设置页允许修改时区。
- 使用 Day.js timezone 插件生成 `local_kickoff_time`。

比赛时间展示需要同时兼顾清晰性和移动端可读性，比赛详情页可同时展示本地时间和 UTC 时间。

## 10. UI 与交互规范

整体风格：

- 现代体育数据风格。
- 清爽、简洁、适合手机浏览。
- 首页突出今日比赛、关注球队和热门推荐。
- 避免新闻门户式拥挤信息。
- 避免大面积渐变和过度装饰。

布局要求：

- 移动端优先。
- 桌面端内容最大宽度控制在 1200px。
- 比赛卡片适合单手浏览。
- 按钮高度不小于 40px。
- 列表和卡片间距清晰。
- 删除、导入、比分更新等重要操作需要二次确认。

主题要求：

- 浅色模式：白色背景、浅灰卡片、深色文字。
- 深色模式：深色背景、深灰卡片、白色文字。
- 主色可采用克制的世界杯绿色或蓝色。

## 11. PWA 要求

需要配置：

- `manifest.json`
- service worker
- app icon
- `name`
- `short_name`
- `theme_color`
- `background_color`
- `display: standalone`

MVP 支持：

- 添加到桌面。
- 基础离线缓存。
- 首页和赛程页缓存。

## 12. 管理后台前端方案

后台使用 Element Plus Admin 风格，强调清晰、高效和桌面端操作效率。

核心能力：

- 登录后进入数据看板。
- 列表统一支持分页、搜索、筛选、编辑、新增和删除。
- 比赛管理支持比分更新、状态更新、推荐等级设置和 CSV 导入。
- 积分榜管理支持查看、手动修正和一键重算。
- 表单统一使用校验规则。
- 删除操作统一二次确认。

## 13. 前端测试

至少提供以下测试：

- 关键页面正常渲染。
- 登录状态切换。
- 赛程筛选。
- 收藏按钮状态变化。
- 深色模式切换。

如果时间有限，优先保证首页、赛程页、比赛详情页、登录状态和收藏交互可测试。

## 14. 开发顺序

1. 初始化 Vite + Vue 3 + TypeScript 项目。
2. 配置 Vue Router、Pinia、Element Plus、Tailwind CSS。
3. 建立 API 请求封装和基础类型定义。
4. 完成用户端布局、底部导航和主题基础。
5. 完成核心组件：比赛卡片、球队卡片、积分榜、筛选器、收藏、提醒、倒计时。
6. 完成用户端页面：首页、赛程、比赛详情、球队、积分榜、我的。
7. 完成登录、注册、用户资料、收藏和提醒流程。
8. 完成管理后台布局和后台登录。
9. 完成后台核心页面：Dashboard、球队管理、比赛管理、积分榜管理。
10. 完成 PWA、深色模式、响应式适配、骨架屏、空状态和错误提示。
11. 补充前端测试并完善 README 前端部分。

## 15. MVP 边界

第一版不做新闻资讯、视频集锦、社区评论、真钱竞猜、支付系统、原生 App、复杂 AI 解说、直播流和第三方体育数据自动同步。

前端需要为后续能力预留空间：

- AI 比赛看点。
- 赛程日历订阅。
- Web Push。
- 邮件提醒。
- 多语言。
- 第三方数据 API。
