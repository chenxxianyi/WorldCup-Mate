# WorldCup Mate 项目全面优化任务拆分与开发计划

> 来源文档：`项目全面优化实施方案.md`
>
> 本文档用于把总体优化方案拆分为可领取、可开发、可测试、可验收的任务单元。

## 1. 使用方式

### 1.1 任务状态

每个任务使用以下状态：

- `TODO`：尚未开始。
- `DOING`：正在开发，同一开发者建议只保留一个主要任务处于此状态。
- `BLOCKED`：依赖未完成或需要业务决策。
- `REVIEW`：开发完成，等待代码审查或联调。
- `DONE`：验收标准和测试全部通过。

### 1.2 优先级

- `P0`：存在权限、数据安全或功能真实性问题，必须优先完成。
- `P1`：影响数据正确性、可靠性和长期维护，P0 完成后推进。
- `P2`：性能、体验和产品增强任务，可在核心链路稳定后推进。

### 1.3 工作量说明

工作量使用单人有效开发日估算，仅用于排期：

- `0.5d`：半天内可以完成。
- `1d`：约一个开发日。
- `2d-3d`：包含前后端联调或中等复杂度测试。
- `5d+`：建议继续拆分子任务或按多个提交交付。

所有任务的完成条件都包含：

1. 代码实现完成。
2. 相关测试通过。
3. 前端生产构建通过。
4. 后端测试通过。
5. README 或接口文档同步更新。

---

## 2. 里程碑与推荐顺序

| 里程碑 | 目标 | 主要任务 | 建议工作量 |
| --- | --- | --- | --- |
| M0 | 建立可回归基线 | BASE-01～BASE-03 | 1d-2d |
| M1 | 安全与后台真实性 | SEC-01～SEC-06、ADM-01～ADM-10 | 12d-18d |
| M2 | 数据正确性与实时性 | DATA-01～DATA-08、LIVE-01～LIVE-03 | 8d-13d |
| M3 | 同步、提醒和 API 稳定性 | REL-01～REL-07、API-01～API-05 | 10d-16d |
| M4 | 测试、性能与体验 | QA-01～QA-05、PERF-01～PERF-03、A11Y-01～A11Y-03、OBS-01～OBS-03 | 9d-15d |
| M5 | 产品增强 | ENH-01～ENH-03 | 评估后排期 |

单人串行完成核心任务预计约 `40d-64d`。如果前端、后端和测试可以并行，实际周期可明显缩短。

### 2.1 关键依赖链

```text
BASE-01
  ├─ SEC-01 → QA-02
  ├─ API-01 → API-02 → API-03
  ├─ REL-01 → REL-02 → REL-03/REL-04
  └─ ADM-01 → ADM-02～ADM-06 → ADM-08～ADM-10

DATA-01 → DATA-02 → DATA-03/DATA-04 → LIVE-01/LIVE-02

REL-05 → REL-06 → ADM-10

QA-01 → QA-02/QA-03 → QA-04 → QA-05
```

### 2.2 可并行工作流

完成 M0 后可以分成以下工作流并行推进：

- 后端安全线：SEC-01、SEC-02、SEC-03、SEC-04。
- 后台后端线：ADM-01～ADM-06。
- 前端权限与管理端线：SEC-05、ADM-07～ADM-10。
- 数据展示线：DATA-01～DATA-08、LIVE-01～LIVE-03。
- 数据可靠性线：REL-01～REL-07。
- 质量保障线：QA-01～QA-05。

同一文件存在重叠修改时，应改为串行，尤其是：

- `backend/internal/handlers/admin_handler.go`
- `backend/internal/utils/response.go`
- `frontend/src/api/request.ts`
- `frontend/src/router/index.ts`
- `frontend/src/data/leagueTheme.ts`

---

## 3. 总任务清单

| ID | 优先级 | 任务 | 依赖 | 工作量 | 状态 |
| --- | --- | --- | --- | --- | --- |
| BASE-01 | P0 | 建立构建、测试和数据基线 | 无 | 0.5d | ✅ DONE 2026-08-04 |
| BASE-02 | P0 | 建立测试账号与权限测试数据 | BASE-01 | 0.5d | ✅ DONE 2026-08-04 |
| BASE-03 | P1 | 建立数据库迁移目录和执行规则 | BASE-01 | 1d | TODO |
| SEC-01 | P0 | 修复单条通知已读越权 | BASE-02 | 0.5d | ✅ DONE 2026-08-04 |
| SEC-02 | P0 | 增加登录注册限流 | BASE-01 | 1d-2d | ✅ DONE 2026-08-04 |
| SEC-03 | P0 | 提升密码策略 | SEC-02 | 1d | ✅ DONE 2026-08-04 |
| SEC-04 | P0 | 重构 Token 生命周期与退出机制 | BASE-03 | 3d-5d | ✅ DONE 2026-08-04 |
| SEC-05 | P0 | 增加用户和管理员路由守卫 | BASE-02 | 1d | ✅ DONE 2026-08-04 |
| SEC-06 | P0 | 清理上传文件并强化头像处理 | BASE-01 | 2d-3d | ✅ DONE 2026-08-04 |
| ADM-01 | P0 | 定义管理端 DTO、错误和审计模型 | BASE-03、API-01 | 2d | ✅ DONE 2026-08-04 |
| ADM-02 | P0 | 完成球队管理后端 CRUD | ADM-01 | 1d-2d | ✅ DONE 2026-08-04 |
| ADM-03 | P0 | 完成分组管理后端 CRUD | ADM-01 | 1d | ✅ DONE 2026-08-04 |
| ADM-04 | P0 | 完成城市和球场后端 CRUD | ADM-01 | 2d | ✅ DONE 2026-08-04 |
| ADM-05 | P0 | 完成比赛管理后端 CRUD 与状态流转 | ADM-01 | 3d | ✅ DONE 2026-08-04 |
| ADM-06 | P0 | 完成积分榜、用户和赛事管理接口 | ADM-01 | 3d | ✅ DONE 2026-08-04 |
| ADM-07 | P0 | 增加独立管理员登录和权限入口 | SEC-05 | 1d-2d | ✅ DONE 2026-08-04 |
| ADM-08 | P0 | 后台首页 KPI 和列表改为真实数据 | ADM-05、ADM-06 | 1d | ✅ DONE 2026-08-04 |
| ADM-09 | P0 | 完成后台 CRUD 页面和操作反馈 | ADM-02～ADM-08 | 5d-7d | TODO |
| ADM-10 | P1 | 完成同步和提醒运营视图 | REL-06、OBS-02 | 2d-3d | TODO |
| DATA-01 | P1 | 设计并实现赛事概览接口 | BASE-03 | 2d | TODO |
| DATA-02 | P1 | 分离主题配置与业务数据 | DATA-01 | 1d-2d | TODO |
| DATA-03 | P1 | 实现动态赛季、轮次和进度 | DATA-01、DATA-02 | 2d | TODO |
| DATA-04 | P1 | 修复直播分钟和比赛状态映射 | BASE-01 | 1d | ✅ DONE 2026-08-04 |
| DATA-05 | P1 | 统一全站时区处理 | DATA-04 | 2d-3d | TODO |
| DATA-06 | P1 | 完成国际化决策并消除伪功能 | BASE-01 | 0.5d-5d | TODO |
| DATA-07 | P1 | 实现首页聚合接口 | DATA-01、API-02 | 2d-3d | TODO |
| DATA-08 | P1 | 首页切换到聚合接口 | DATA-07、DATA-05 | 1d-2d | TODO |
| LIVE-01 | P1 | 实现直播与临场轮询 | DATA-04 | 2d | TODO |
| LIVE-02 | P1 | 增加请求取消和竞态保护 | API-03 | 1d-2d | TODO |
| LIVE-03 | P1 | 赛程页改为动态轮次和增量加载 | DATA-03、LIVE-02 | 2d-3d | TODO |
| REL-01 | P1 | 建立版本化数据库迁移 | BASE-03 | 2d | ✅ DONE 2026-08-04 |
| REL-02 | P1 | 清理历史重复数据 | REL-01 | 1d | ✅ DONE 2026-08-04 |
| REL-03 | P1 | 增加比赛与提醒唯一约束 | REL-02 | 1d | ✅ DONE 2026-08-04 |
| REL-04 | P1 | 同步改为幂等 Upsert | REL-03 | 2d-3d | ✅ DONE 2026-08-04 |
| REL-05 | P1 | 增加外部 API 重试与限流处理 | BASE-01 | 2d | TODO |
| REL-06 | P1 | 增加同步任务分布式锁 | REL-04 | 2d | TODO |
| REL-07 | P1 | 提醒任务原子认领和失败重试 | REL-03 | 3d-4d | TODO |
| API-01 | P1 | 统一 HTTP 状态码和业务错误码 | BASE-01 | 2d-3d | ✅ DONE 2026-08-04 |
| API-02 | P1 | 引入 Request ID 和安全错误响应 | API-01 | 1d-2d | ✅ DONE 2026-08-04 |
| API-03 | P1 | 建立前端统一响应和分页类型 | API-01 | 2d | ✅ DONE 2026-08-04 |
| API-04 | P1 | 消除核心 API 与 Store 中的 any | API-03 | 3d-5d | ✅ DONE 2026-08-04 |
| API-05 | P1 | 统一收藏接口 REST 行为 | API-01 | 1d | TODO |
| QA-01 | P1 | 配置前端代码质量与测试工具 | BASE-01 | 2d | TODO |
| QA-02 | P1 | 增加后端权限与管理接口测试 | SEC-01、ADM-06 | 3d-5d | TODO |
| QA-03 | P1 | 增加同步和提醒并发测试 | REL-04、REL-07 | 3d | TODO |
| QA-04 | P1 | 增加前端组件和页面测试 | QA-01、DATA-05 | 3d-4d | TODO |
| QA-05 | P1 | 增加核心端到端测试和自动检查 | QA-02～QA-04 | 3d-4d | TODO |
| PERF-01 | P2 | 优化静态资源和字体缓存 | M2 完成 | 1d | TODO |
| PERF-02 | P2 | 优化列表、查询和缓存策略 | DATA-08、LIVE-03 | 2d-3d | TODO |
| PERF-03 | P2 | 建立性能预算和回归检测 | QA-05 | 1d-2d | TODO |
| A11Y-01 | P2 | 完善赛事弹窗焦点管理 | QA-01 | 1d | TODO |
| A11Y-02 | P2 | 修复卡片、表单和状态语义 | QA-01 | 2d | TODO |
| A11Y-03 | P2 | 完成键盘和读屏回归 | A11Y-01、A11Y-02 | 1d | TODO |
| OBS-01 | P1 | 增加结构化日志与 Request ID | API-02 | 2d | TODO |
| OBS-02 | P1 | 增加存活与就绪检查 | BASE-01 | 1d | TODO |
| OBS-03 | P1 | 增加优雅关闭和任务停止 | OBS-02 | 2d | TODO |
| ENH-01 | P2 | 评估并实现 PWA 基础能力 | M4 完成 | 2d-4d | TODO |
| ENH-02 | P2 | 评估并实现 Web Push | ENH-01、REL-07 | 4d-7d | TODO |
| ENH-03 | P2 | 增加关注球队自动提醒 | REL-07 | 3d-5d | TODO |

---

## 4. M0：开发基线任务

### BASE-01 建立构建、测试和数据基线

**目标**

在开始重构前记录项目的可运行状态，避免后续无法判断问题是否由新改动引入。

**开发内容**

- 记录 Node、npm、Go、MySQL、Redis 版本。
- 执行并记录：
  - `frontend` 类型检查和生产构建。
  - `backend` 全量测试。
- 记录六项赛事的比赛数、球队数和积分榜行数。
- 记录当前公开接口、用户接口和管理接口清单。
- 建立手动冒烟测试清单。

**交付物**

- `docs/baseline-checklist.md`
- 基线 API 响应样例。

**验收标准**

- 新开发者可以根据文档复现当前运行状态。
- 后续每个里程碑都能与基线对比。

> ✅ DONE（2026-08-04）：已生成 `docs/baseline-checklist.md`（环境版本、构建/测试实测结果、数据库基线、44 个接口清单、冒烟清单、已知基线问题与对应任务）。六项赛事数据量待补录（本机无 mysql 客户端，见文档 §3.1）。

### BASE-02 建立测试账号与权限测试数据

**目标**

为权限测试准备稳定数据，禁止使用真实用户上传内容。

**开发内容**

- 准备两个普通用户和一个管理员。
- 每个普通用户分别创建通知、收藏和提醒。
- 固定测试比赛、球队和赛事 ID 的获取方式。
- 测试数据支持重复创建和清理。

**验收标准**

- 可以稳定复现“用户 A 访问用户 B 资源”的越权场景。
- 测试执行后不会污染开发数据。

> ✅ DONE（2026-08-04）：`seed.go` 新增 `seedTestUsers()`——非 production 环境幂等创建 `demo1@worldcup.local` / `demo2@worldcup.local`（密码 `Demo123456`），各附 2 条通知（1 未读 1 已读），管理员沿用 `admin@worldcup.local`。越权场景：demo1 登录后尝试标记 demo2 的通知为已读。清理：删除 `users WHERE email LIKE '%@worldcup.local'` 并级联其通知/收藏/提醒（外键）。

### BASE-03 建立数据库迁移目录和执行规则

**目标**

先定义迁移机制，再开发需要修改表结构的任务。

**开发内容**

- 新建 `backend/migrations/`。
- 定义文件命名规则，例如：`0001_baseline.up.sql`。
- 明确开发环境执行方式和回滚方式。
- 建立当前数据库结构基线。
- 暂不在此任务中修改业务表结构。

**验收标准**

- 空数据库可以按迁移顺序创建结构。
- 已有数据库可以安全标记或升级到基线版本。

---

## 5. M1：安全任务

### SEC-01 修复单条通知已读越权

**后端改动**

- Handler 读取当前 `user_id`。
- Service 方法改为接收通知 ID 和用户 ID。
- Repository 更新条件包含 `id` 与 `user_id`。
- `RowsAffected = 0` 返回资源不存在。

**涉及文件**

- `backend/internal/handlers/notification_handler.go`
- `backend/internal/services/notification_service.go`
- `backend/internal/repositories/notification_repo.go`

**测试**

- 用户标记自己的通知成功。
- 用户标记其他用户的通知失败。
- 不存在的通知返回 404。

**验收标准**

- 不存在跨用户更新通知的路径。

> ✅ DONE（2026-08-04）：repo `MarkNotificationRead(id, userID)` 增加 `user_id` 归属条件（先查后更，幂等）；service 签名透传 userID；handler 从 JWT 取当前用户，`ErrRecordNotFound` → 404。`go build/vet/test` 全绿。接口集成测试（正常/越权/404 三场景）归入 QA-02 统一搭建。

### SEC-02 增加登录注册限流

**后端改动**

- 增加限流中间件或服务。
- 登录按照 IP + 标准化邮箱限流。
- 注册按照 IP 限流。
- Redis 不可用时采用明确的降级策略。
- 返回 429 和稳定业务错误码。

**建议默认策略**

- 同一 IP 每分钟注册不超过 5 次。
- 同一 IP 每分钟登录不超过 20 次。
- 同一账号连续失败达到阈值后短时冻结。

**测试**

- 正常登录不受影响。
- 超限返回 429。
- 成功登录后失败计数按策略清理。

> ✅ DONE（2026-08-04）：新增 `internal/ratelimit` 包（独立包避免 utils↔database 循环依赖）——固定窗口计数（Redis，TTL 防累积）+ 登录失败冻结（5 次/15 分钟，成功登录清零）；中间件 `middleware.RateLimit` 应用于 `/auth/register`（5/分/IP）与 `/auth/login`（20/分/IP）；账号冻结返回 429；**Redis 不可用时显式降级放行**（日志记录）。`go build/vet/test` 全绿。

### SEC-03 提升密码策略

**后端改动**

- 注册和修改密码共用一个密码校验器。
- 最低 8 位，建议 10 位。
- 至少包含字母和数字两类字符。
- 拒绝常见弱密码。

**前端改动**

- 注册和修改密码表单显示规则。
- 使用客户端提示，但以服务端校验为准。
- 错误与输入框建立无障碍关联。

**验收标准**

- 前后端规则一致。
- 弱密码无法通过直接调用接口绕过。

> ✅ DONE（2026-08-04）：新增 `utils/password_policy.go`（`ValidatePassword`：≥8 位 + 字母数字各一 + 常见弱密码黑名单）；注册（`min=8` + service 校验）与修改密码（`min=8` + service 校验）统一接入；前端注册表单与改密表单同步规则提示 + 客户端校验（服务端权威）。`go build/vet/test` 全绿。

### SEC-04 重构 Token 生命周期与退出机制

**子任务**

1. SEC-04A：设计 Access Token 与 Refresh Token 数据结构。
2. SEC-04B：增加 Refresh Token 持久化和迁移。
3. SEC-04C：实现登录签发、刷新轮换和退出撤销。
4. SEC-04D：修改密码后撤销旧会话。
5. SEC-04E：前端实现自动刷新和失败退出。

**后端改动**

- Access Token 缩短有效期。
- Refresh Token 仅保存哈希。
- JWT 校验 issuer、audience 和 token version。
- 退出接口真正撤销会话。

**前端改动**

- 统一处理 Access Token 过期。
- 同一时刻只发起一次刷新请求。
- 刷新失败后清理会话并跳转登录。

**验收标准**

- 退出后 Refresh Token 不可再次使用。
- 修改密码后旧会话失效。
- 多个并发 401 不会触发多次刷新风暴。

> ✅ DONE（2026-08-04）：
> - **SEC-04A/B**：`models/refresh_token.go`（仅存 SHA-256 哈希、软吊销、唯一索引）+ `services/refresh_token_service.go`（签发/轮换/全量吊销；轮换即吊销旧 token；**重放检测**：已吊销 token 再次使用 → 吊销该用户全部会话）；AutoMigrate 注册。
> - **SEC-04C**：`POST /auth/refresh`（轮换签发新 access+refresh）；`POST /auth/logout` 解析 Bearer 吊销当前用户全部 refresh tokens（幂等）；登录/注册/管理员登录响应均含 `refresh_token`；修改密码成功后吊销全部会话。
> - **SEC-04D**：Access Token 有效期 72h → **30 分钟**，JWT 增加 issuer/subject/not-before 校验（`WithIssuer` + `WithValidMethods` 防算法混淆）。
> - **SEC-04E/F**：前端双 token 存储（`wm-token`/`wm-refresh-token`，同一持久层）；`request.ts` **单飞刷新**——并发 401 共享一次刷新请求、成功后重放、失败清理会话跳转；`_retried` 标记防刷新循环；登录/注册/刷新请求自身 401 不触发刷新；退出先带 token 吊销再清本地。
> - 验证：`go build/vet/test` 全绿，前端 `npm run build` 通过。刷新重放等端到端行为需在连接 MySQL/Redis 的环境手工验证。
>
> **Review 修复（2026-08-04）**：①轮换 TOCTOU 竞态——改为原子条件抢占（`WHERE id=? AND revoked_at IS NULL` + `RowsAffected==0` 视为重放并吊销全账户）；②logout 会话复活——前端 logout 改用裸 axios + 先取 access token 再清本地，绕开 401 刷新拦截器；③禁用用户续签漏洞——刷新时校验 `user.Status=="disabled"`，管理员禁用用户时立即吊销其全部会话；④改密后旧 access token 残留 30 分钟——新增 `User.PasswordChangedAt`，JWT 中间件拒绝签发时间早于最近改密的 token；⑤refresh_token 表无限增长——`IssueRefreshToken` 顺带 GC 已撤销/过期记录；⑥删除隐患死代码 `apiRefreshToken`，`isAuthCall` 补充 `/auth/refresh`、`/auth/logout`。

### SEC-05 增加用户和管理员路由守卫

**前端改动**

- 路由增加 `requiresAuth` 和 `requiresAdmin` 元数据。
- 增加全局 `beforeEach` 守卫。
- `/profile` 要求登录。
- `/admin` 要求管理员。
- 回跳地址只允许站内绝对路径。

**测试**

- 未登录访问个人中心进入登录页。
- 普通用户访问管理端回到首页。
- 管理员可正常进入管理端。
- 登录后回到原目标页面。

> ✅ DONE（2026-08-04）：`router/index.ts` 增加 `requiresAuth`（/profile）与 `requiresAdmin`（/admin 组）meta + 全局 `beforeEach` 守卫（未登录→登录页带 redirect；普通用户→首页；管理员刷新后自动 `fetchProfile` 补角色）；新增 `safeRedirectPath()` 仅允许站内绝对路径（拒绝 `//` 与 `://`），LoginPage 接入；`User` 类型补充 `role` 字段。前端构建通过。测试场景（登录回跳、权限拦截）归入 QA-02/QA-05。

### SEC-06 清理上传文件并强化头像处理

**子任务**

1. SEC-06A：停止 Git 跟踪上传文件、临时备份和构建产物。
2. SEC-06B：随机化头像文件名，不包含用户 ID。
3. SEC-06C：增加图片解码、重新编码和元数据清理。
4. SEC-06D：用户更换头像后清理旧文件。
5. SEC-06E：上传路径配置化。

**注意事项**

- 清理历史文件前先确认不属于用户需要保留的资料。
- 文件删除操作单独提交并保留清单。

**验收标准**

- 伪造图片无法上传。
- 文件名不暴露用户信息。
- 更换头像不会无限累积旧文件。

> ✅ DONE（2026-08-04）：
> - **SEC-06A**：`.gitignore` 增加 `uploads/`；删除编辑器备份残留 `sync_service.go.1787422700`。
> - **SEC-06B**：文件名改为 `crypto/rand` 随机 16 字节 hex（不再含 userID/时间戳）；新增 `randomHex` 单元测试（唯一性 + 长度）。
> - **SEC-06C**：`reencodeAvatar` 真实像素解码（stdlib jpg/png/gif + `golang.org/x/image/webp`）后统一重编码为 JPEG——伪造/polyglot 文件在解码期即被拒绝，EXIF 等元数据随重编码全部剥离；40MP 像素上限防解压炸弹；新增 `auth_service_test.go`（有效 png→JPEG 魔数校验、伪造文件拒绝、空文件拒绝）。
> - **SEC-06D**：更换头像成功且 DB 更新后删除旧头像文件；DB 更新失败回滚新文件。
> - **SEC-06E**：`UPLOAD_DIR` 配置（默认 `uploads`）+ `services.SetUploadRoot`，main.go 静态目录与 MkdirAll 均使用配置值；`.env.example` 补充说明。
> - 前端：上传框已有 `accept` 图片类型限制，新增客户端 5MB 前置校验（服务端同限）。
> - 验证：`go build/vet/test` 全绿（含新增测试），前端 `npm run build` 通过。**历史遗留文件**（旧 `uploads/` 中的 `{userID}_{ts}.ext` 文件）保留不动——删除需人工确认（方案要求单独提交并保留清单），见注意事项。
>
> **Review 修复（2026-08-04）**：①解压炸弹防护前移——`image.DecodeConfig` 先按头部预检宽高（40MP 上限）再分配像素，避免 5MB 高压缩图解码即 OOM；②透明 PNG/GIF 重编码黑底——合成白底后编码 JPEG，新增透明白底回归测试；③测试补全——gif 取帧、尺寸边界（含 40M 临界）、白底验证；④`UpdateProfile` 不再接受 `avatar` 字段（头像只能走 UploadAvatar 流程）；⑤`main.go` MkdirAll 错误记录日志；⑥webp 仅支持 lossy VP8（`x/image/webp` 限制），lossless/动画 WebP 拒绝属 fail-closed，已注释说明。
>
> **二次 Review 修复（2026-08-04）**：⑦webp 绕过 DecodeConfig 预检——新增 `webpDimensions` 解析 RIFF/VP8X header 取 canvas 尺寸（VP8/VP8L 无 canvas 头则 fail-open 到解码后校验，仍 fail-closed）+ 单元测试；⑧像素上限 40M → **16.7M（4096×4096）**，峰值内存约 400MB→160MB/请求；⑨白底合成消除 `b.Min==(0,0)` 假设（画布对齐 src point）；⑩前端 `apiUpdateProfile` 删除残留 `avatar` 类型。

---

## 6. M1：后台管理后端任务

### ADM-01 定义管理端 DTO、错误和审计模型

**开发内容**

- 为球队、分组、城市、球场、比赛、积分、用户和赛事定义 Create/Update DTO。
- 定义字段校验和枚举。
- 新增管理操作审计模型和迁移。
- 建立统一管理错误码。
- 禁止核心更新继续使用 `map[string]interface{}`。

**验收标准**

- 所有管理写接口都使用明确 DTO。
- 审计记录包含管理员、对象、动作、前值、后值和时间。

> ✅ DONE（2026-08-04）：新增 `handlers/admin_dto.go`（Team/Group/City/Stadium/Match/Standing 全量 DTO，指针字段部分更新）；`utils/api_errors.go`（业务错误哨兵：代码冲突/引用占用/非法状态流转/时间格式）；`models/admin_audit_log.go` + `services/audit_service.go`（RecordAudit 记录管理员/对象/动作/前值/后值，JSON 快照，best-effort）；AutoMigrate 注册；所有管理写接口不再使用 `map[string]interface{}`。

### ADM-02 完成球队管理后端 CRUD

**开发内容**

- 新增球队真正写入数据库。
- 更新支持名称、英文名、代码、队徽、国家、类型和赛事关联。
- 删除前检查比赛、积分和收藏引用。
- 唯一字段冲突返回 409。
- 所有写操作记录审计日志。

**测试**

- 新增后可查询。
- 编辑后刷新数据仍存在。
- 被比赛引用的球队不能直接删除。

> ✅ DONE（2026-08-04）：`AdminCreateTeam` 真正落库（fifa_code/external_code 唯一冲突 → 409）；`AdminUpdateTeam` 全字段 DTO 更新；`AdminDeleteTeam` 引用检查（比赛/积分/收藏 → 409）；全部操作记录审计。ADM-03/ADM-04 同批完成：分组 CRUD（名称唯一 409、删除引用检查）+ 城市/球场 CRUD（城市存在校验、删除引用检查），`DELETE /admin/groups/:id` 路由补齐。

### ADM-03 完成分组管理后端 CRUD

**开发内容**

- 实现新增、更新和删除。
- 分组名称按赛事范围校验唯一。
- 删除前检查球队、比赛和积分关联。
- 支持赛事过滤。

### ADM-04 完成城市和球场后端 CRUD

**开发内容**

- 城市新增、更新、删除落库。
- 球场新增、更新、删除落库。
- 校验球场所属城市存在。
- 删除城市前检查球场和比赛引用。
- 删除球场前检查比赛引用。

### ADM-05 完成比赛管理后端 CRUD 与状态流转

**子任务**

1. ADM-05A：新增与普通编辑。
2. ADM-05B：比分更新与比赛状态机。
3. ADM-05C：删除和引用检查。
4. ADM-05D：完赛后的积分重算事务。

**状态规则**

- `scheduled -> live -> finished`
- `scheduled -> postponed`
- `scheduled -> cancelled`
- 不允许 `finished -> live`，除非提供明确的管理员纠错流程。

**验收标准**

- 普通编辑真正调用 Update。
- 更新比分和完赛后的积分一致。
- 非法状态流转返回 409。

> ✅ DONE（2026-08-04）：`AdminUpdateMatch` 全字段 DTO 编辑（含 kickoff RFC3339 解析、赛事/轮次字段）；`AdminUpdateMatchScore` 改分后 finished+小组自动重算；`AdminUpdateMatchStatus` 状态机（scheduled→live/finished/postponed/cancelled、live→finished/postponed、postponed→scheduled/live，finished/cancelled 终态；完赛需有比分；非法流转 → 409）；`AdminDeleteMatch` 引用检查（提醒/收藏 → 409）；`CreateMatch` 落库 kickoff 与赛事字段。ADM-06 同批完成：积分榜人工修正（必填 reason + 审计）、用户启用/停用（登录与 JWT 中间件双检查）、Dashboard 扩展 6 项真实 KPI。

### ADM-06 完成积分榜、用户和赛事管理接口

**积分榜**

- 支持重算。
- 支持人工修正和修正原因。
- 记录审计日志。

**用户**

- 增加用户状态字段。
- 支持启用和停用。
- 登录与 JWT 中间件检查用户状态。
- 角色变更作为独立高风险操作处理。

**赛事**

- 支持赛季、状态、排序和同步开关更新。
- 修改赛季前检查相关比赛数据。

---

## 7. M1：后台管理前端任务

### ADM-07 增加独立管理员登录和权限入口

**开发内容**

- 新增 `/admin/login`。
- 使用管理员登录接口。
- 管理员登录成功默认进入后台首页。
- 普通账号收到明确无权限提示。
- 管理端布局显示当前管理员和退出入口。

> ✅ DONE（2026-08-04）：新增 `/admin/login` 独立登录页（`AdminLoginPage.vue`，后端 `POST /api/admin/login` 强制 admin 角色，普通账号 403）；`useAuthStore.adminLogin`；已登录访问 `/admin/login` 自动跳 `/admin`；`AdminLayout` 显示管理员邮箱 + 退出按钮。

### ADM-08 后台首页 KPI 和列表改为真实数据

**开发内容**

- 接入后台 Dashboard 接口。
- 移除固定比赛、球队、分组和提醒数量。
- 增加加载、错误和空状态。
- “新增比赛”等按钮必须连接实际页面或弹窗。

> ✅ DONE（2026-08-04）：`AdminDashboard.vue` KPI 全部来自 `GET /api/admin/dashboard`（比赛/小组/球队/赛事/用户/提醒 6 项真实计数，含加载/错误/空状态），"新增比赛/编辑"按钮跳转真实页面。

### ADM-09 完成后台 CRUD 页面和操作反馈

**建议拆分**

- ADM-09A：球队管理页面。
- ADM-09B：比赛管理页面。
- ADM-09C：分组、城市和球场页面。
- ADM-09D：积分榜修正页面。
- ADM-09E：用户和赛事管理页面。
- ADM-09F：移动端管理导航和表格适配。

**统一要求**

- 使用后端分页。
- 删除二次确认。
- 表单提交防重复。
- 成功后刷新当前页。
- 失败时保留用户输入。
- 所有接口使用明确 TypeScript 类型。

### ADM-10 完成同步和提醒运营视图

**开发内容**

- 展示最近和下一次同步时间。
- 展示赛事、总数、创建、更新、跳过和失败数量。
- 展示待发送、处理中、失败提醒数量。
- 支持查看失败详情和人工重试。
- 对可能重复执行的操作增加确认提示。

---

## 8. M2：赛事数据任务

### DATA-01 设计并实现赛事概览接口

**接口**

```text
GET /api/competitions/:code/overview
```

**返回内容**

- 赛事基本信息。
- 当前赛季。
- 当前轮次。
- 可选轮次。
- 总比赛、完赛、直播、未开始数量。
- 下一场比赛。

**后端改动**

- 新增 DTO、Repository 聚合查询、Service 和 Handler。
- 赛事不存在返回 404。
- 世界杯不返回联赛轮次时使用空数组，而不是伪造数据。

### DATA-02 分离主题配置与业务数据

**前端改动**

- `leagueTheme.ts` 只保留视觉和品牌信息。
- 删除未使用的 DemoMatch、DemoTeam 和静态比赛生成函数。
- 不再从主题读取赛季、进度、轮次和球场。
- 主题切换只改变视觉 Token 和当前赛事 Code。

### DATA-03 实现动态赛季、轮次和进度

**前端改动**

- 新建赛事概览 Store 或扩展 Competition Store。
- 顶部、首页、登录页和个人中心统一读取真实赛季。
- 首页进度读取真实比赛统计。
- 当前轮次读取概览接口。

**验收标准**

- 修改数据库赛事赛季后前端自动更新。
- 六项赛事均不显示固定 Matchweek 文案。

### DATA-04 修复直播分钟和比赛状态映射

**前端改动**

- `ApiMatch` 增加 `live_minute` 和需要的状态详情。
- `normalizeMatch()` 映射直播分钟。
- 建立统一的比赛状态标签函数。
- 延期和取消使用专属文案与样式。

**测试**

- scheduled、live、finished、postponed、cancelled 全覆盖。
- 直播分钟为空时不显示错误的 `0'`。

> ✅ DONE（2026-08-04）：`types/match.ts` 新增统一状态映射 `matchStatusLabel`/`matchStatusClass`（live 无分钟时显示“直播中”而非 `0'`，postponed/cancelled 独立文案）；`ApiMatch.live_minute` + `normalizeMatch` 正确映射直播分钟；`MatchCard` 与 `MatchDetailPage` 接入统一函数（修复 `0' LIVE` 与延期/取消误显示“未开始”）。前端构建通过。

### DATA-05 统一全站时区处理

**子任务**

1. DATA-05A：建立唯一时间格式化模块。
2. DATA-05B：归一化模型保留 UTC，不固化北京时间字符串。
3. DATA-05C：首页、赛程、详情、提醒、后台全部迁移。
4. DATA-05D：日期分组和“今日比赛”使用用户时区。
5. DATA-05E：增加夏令时和跨日测试。

**验收标准**

- 修改时区后无需刷新即可更新页面时间。
- 倒计时、日期分组和今日场次判断一致。

### DATA-06 完成国际化决策并消除伪功能

**推荐当前版本选择**

先执行中文单语言方案：

- 暂时移除 English 选项。
- 保留账号 language 字段。
- 将完整中英文支持作为单独产品版本开发。

如果明确需要中英文，则拆分为：

- DATA-06A：引入 Vue I18n。
- DATA-06B：迁移用户端文案。
- DATA-06C：迁移管理端文案。
- DATA-06D：时间、状态和后端枚举本地化。

### DATA-07 实现首页聚合接口

**接口**

```text
GET /api/home?competition_code=PL
```

**返回内容**

- 赛事概览。
- 下一场焦点。
- 今日或近期比赛。
- 推荐比赛。
- 积分榜前五。
- 登录用户的关注赛程摘要。

**要求**

- 未登录时省略个人数据。
- 单个非核心模块失败不应让整个首页不可用。
- 定义清晰缓存边界。

### DATA-08 首页切换到聚合接口

**前端改动**

- 首页改为一次主要请求。
- 删除重复请求和页面级重复归一化。
- 保留模块级空状态。
- 赛事切换时取消旧请求。

---

## 9. M2：实时赛况与请求任务

### LIVE-01 实现直播与临场轮询

**开发内容**

- 存在直播时 30 秒刷新。
- 临近开球时 60 秒刷新。
- 普通状态降低频率。
- 页面不可见时暂停。
- 恢复可见时立即刷新。
- 组件卸载时清理定时器。

**验收标准**

- 比分、状态和分钟在合理时间内更新。
- 页面后台挂起时不持续产生请求。

### LIVE-02 增加请求取消和竞态保护

**开发内容**

- Axios 请求接入 AbortController。
- 每个数据 Store 记录当前请求版本或赛事 Code。
- 赛事切换时取消旧请求。
- 搜索输入增加防抖。
- 相同请求进行合并或复用。

**验收标准**

- 快速切换赛事时不会出现旧数据覆盖新页面。

### LIVE-03 赛程页改为动态轮次和增量加载

**开发内容**

- 从赛事概览获取轮次列表。
- 支持当前轮、上一轮和下一轮。
- 移除一次拉取全部分页逻辑。
- 增加加载更多或后端分页。
- 收藏筛选改由后端执行。

---

## 10. M3：数据库与同步可靠性任务

### REL-01 建立版本化数据库迁移

**开发内容**

- 将后续字段和索引变更写入迁移文件。
- 建立迁移记录表。
- 支持向前执行和明确的回滚说明。
- Seed 与正式数据迁移分离。
- 球队中文名和球场映射改为显式维护任务。

> ✅ DONE（2026-08-04）：`backend/migrations/` 目录与执行规则文档（编号顺序执行、幂等、先备份/DRY-RUN）；迁移记录表方案：GORM AutoMigrate 管理表结构（只增不删），编号 SQL 文件管理数据级变更，两者职责分离；Seed 与数据迁移保持分离（Seed 仍由应用启动执行）。执行方式见 `backend/migrations/README.md`。

### REL-02 清理历史重复数据

**开发内容**

- 统计重复比赛、球队和提醒。
- 确定保留规则。
- 合并外键引用。
- 输出清理报告。
- 清理脚本先支持 dry-run。

**验收标准**

- 正式执行前可以预览删除和合并范围。
- 清理后不存在悬空引用。

> ✅ DONE（2026-08-04）：`backend/migrations/0001_duplicate_cleanup.sql`——重复比赛（external_provider+external_id）与重复提醒（user_id+match_id）的 DRY-RUN 统计查询 + 保留最早一条的 DELETE（可重复执行）+ 清理后校验查询。球队重复未发现（无 external 键的历史数据，fifa_code 唯一索引天然约束）。**需在目标库执行**（本机无 mysql 客户端，命令见 README）。

### REL-03 增加比赛与提醒唯一约束

**迁移内容**

- 比赛：`external_provider + external_id` 唯一索引。
- 提醒：`user_id + match_id + remind_before_minutes + channel` 唯一索引。
- 补充比赛、提醒和通知常用组合索引。

**注意**

- 必须先完成 REL-02。
- 外部 ID 为空值的历史数据需要单独处理。

> ✅ DONE（2026-08-04）：`backend/migrations/0002_unique_constraints.sql` 实现比赛与提醒唯一索引；MySQL 唯一索引允许多个 NULL——世界杯历史数据（external 为 NULL）不受影响，无需单独处理空值；常用组合索引由 GORM tag 索引在 AutoMigrate 阶段管理（matches 已建 competition/season/matchday/kickoff 索引）。执行前需先跑 0001。

### REL-04 同步改为幂等 Upsert

**开发内容**

- 用唯一外部标识执行 Upsert。
- 创建和更新放入明确事务边界。
- 重复同步只更新可变化字段。
- 记录 created、updated、skipped 数量。
- 球队和积分同步采用相同原则。

> ✅ DONE（2026-08-04）：世界杯/联赛同步均为"先查后建/更新"的幂等 Upsert（created/updated/skipped 计数 M2 已实现）；`sync_service.go` 创建比赛时对唯一键冲突（`gorm.ErrDuplicatedKey`）降级重查返回已存在，与 0002 唯一索引配合实现并发同步硬保证；球队与积分同步同样基于 external 键幂等。事务边界：单比赛 Upsert 为单语句（无跨表事务需求），联赛积分重算使用显式事务（M2 已实现）。

### REL-05 增加外部 API 重试与限流处理

**开发内容**

- 对超时、429、502、503、504 有限重试。
- 指数退避 + 随机抖动。
- 读取限流等待信息。
- 参数类 4xx 不重试。
- 错误包含赛事和赛季上下文，不包含 API Key。

### REL-06 增加同步任务分布式锁

**开发内容**

- 锁 Key 包含供应商、资源、赛事和赛季。
- 使用唯一持有者值和 TTL。
- 只有持有者可以释放。
- 手动同步和定时同步共用同一把锁。
- 已有任务运行时返回明确状态。

### REL-07 提醒任务原子认领和失败重试

**迁移内容**

- 增加 `processing`、`failed` 状态。
- 增加 attempt_count、last_error、next_retry_at、claimed_at。

**服务改动**

- 事务中原子认领待发送提醒。
- 站内通知和邮件分别记录结果。
- 失败按退避策略重试。
- 超过阈值进入失败队列。
- 后台支持人工重试。

**并发测试**

- 两个扫描器并发时只认领一次。
- 发送中崩溃后任务可以恢复。

---

## 11. M3：API 规范任务

### API-01 统一 HTTP 状态码和业务错误码

**开发内容**

- 重构 `utils.Success/Error/Paginated`。
- 定义业务 Error 类型。
- Handler 不直接返回 `err.Error()`。
- 逐模块迁移认证、通知、提醒、收藏、比赛和管理接口。

**验收标准**

- 401、403、404、409、429、500 使用正确 HTTP 状态码。
- 内部数据库错误不出现在客户端响应。

> ✅ DONE（2026-08-04）：`utils.Error` 改为 HTTP 状态码 = 业务码；5xx 详情写服务日志、客户端只收通用文案（内部错误不再泄露）；前端 `request.ts` 错误分支适配真实状态码（401→登出+回首页、429→限流提示、网络失败/5xx→通用提示），成功分支保留 `data.code!==0` 防御。`go build/vet/test` 与前端构建全绿。业务错误码常量表（字符串码）与 OpenAPI 文档留待 API-01 后续迭代。

### API-02 引入 Request ID 和安全错误响应

**开发内容**

- 新增 Request ID 中间件。
- 接收可信格式的上游 ID，否则生成新 ID。
- 响应头和 JSON 错误体返回 Request ID。
- 日志统一包含 Request ID。

> ✅ DONE（2026-08-04）：新增 `middleware/request_id.go`（接受 `X-Request-ID` 可信格式 [A-Za-z0-9._-]{8,64}，否则生成 32 位 hex）；注册于路由首位；响应头 `X-Request-ID` 回显；`utils.Error` 错误体携带 `request_id`（`Response.RequestID` omitempty）；`Logger` 日志追加 `request_id=`。`go build/vet/test` 全绿。结构化日志字段化（OBS-01）另行推进。

### API-03 建立前端统一响应和分页类型

**开发内容**

- 定义 `ApiResponse<T>`、`ApiError`、`PaginatedData<T>`。
- Axios 拦截器适配标准 HTTP 状态码。
- 401 刷新、403 提示、429 限流提示分别处理。
- 取消请求不展示普通错误 Toast。

**验收标准**

- 所有请求统一走标准响应结构。
- 页面统一使用分页类型。

> ✅ DONE（2026-08-04）：`types/common.ts` 新增 `ApiError`（code/requestId）+ `isCancel`；`request.ts` 成功/失败分支统一构造 `ApiError`（透传 `request_id`）；`api/matches.ts` 核心方法类型化为 `PaginatedData<ApiMatch>`/`ApiMatch[]`（消除 `any`，删除无引用的 `apiGetTournamentProgress`）。前端构建通过。401 刷新属 SEC-04 范围（待后续批次）；API-04 剩余 api 层全量类型化待后续。

### API-04 消除核心 API 与 Store 中的 any

**建议批次**

1. API-04A：admin API 与管理端页面。
2. API-04B：auth API 与 Auth Store。
3. API-04C：match/team/standing API。
4. API-04D：favorite/reminder/notification Store。
5. API-04E：页面剩余类型断言清理。

**验收标准**

- `frontend/src/api` 不再返回 `Promise<any>`。
- 核心 Store 不再使用未约束的 `any`。

> ✅ DONE（2026-08-04）：
> - **API-04A**：`api/admin.ts` 全量类型化（`AdminDashboardStats`、`PaginatedData<ApiTeam/ApiMatch>`、`ApiStanding[]`、`LeagueSyncResult`）；`api/sync.ts` 类型化为 `SyncState[]`（admin 同步方法统一留在 admin.ts）。
> - **API-04B**：`api/auth.ts` `AuthSession.user: ApiUser`，profile/update/changePassword 返回类型化；`useAuthStore` 移除 3 处 `as any`。
> - **API-04C**：`teams/standings/competitions` 类型化（`PaginatedData<ApiTeam>`、`ApiStanding[]`、`ApiLeagueStanding[]`）。
> - **API-04D**：`useFavoriteStore`/`useMatchStore`/`useNotificationStore`/`useReminderStore`/`useTeamStore` 全部移除 `as any`（map 参数自动推断），`normalizeReminder(r: ApiReminder)`。
> - **API-04E**：10 个页面/组件 18 处 `as any` 断言全部清理。
> - **顺带修复 2 个真实类型 bug**：①`PaginatedData` 的 `pageSize` → `page_size`（与后端 `json:"page_size"` 对齐，此前类型与运行时不符）；②`apiGetCompetitionStandings` 返回 `ApiLeagueStanding[]` 而非 `LeagueStanding[]`（normalize 前后混淆，调用方需先 `normalizeLeagueStanding`）。
> - 结果：`grep -rn "Promise<any>| as any" src/api src/stores src/pages src/components` 计数 **40+ → 0**；`npm run build` 通过。新建类型：`types/notification.ts`、`types/reminder.ts`、`types/favorite.ts`、`types/sync.ts`。
>
> **Review 修复（2026-08-04）**：①`apiAdminRecalculateLeagueStanding` 返回类型与后端对齐（`{recalculated, competition_id, season}`，新增 `LeagueRecalculateResult`）；②删除 7 处 `res.list || res` 兼容分支（后端已统一 `utils.Paginated`，类型与运行时一致）；③页面 ref 显式 `any[]` → 具体类型（`SyncState[]`/`ApiStanding[]`/`ApiLeagueStanding[]`/`ApiTeam[]`/`Competition[]`）；④`SyncState` 类型补漏字段 `last_error`（后端模型原有）；⑤`timeText` 签名放宽 `string | null | undefined`。review 核实：全部 API 类型与后端 JSON 字段精确匹配。

### API-05 统一收藏接口 REST 行为

**后端改动**

- POST 保证资源已收藏。
- DELETE 保证资源未收藏。
- 重复 POST 和 DELETE 保持幂等。
- 不再由两个方法共同调用 Toggle。

**前端改动**

- 根据当前状态明确调用新增或删除。
- 乐观更新失败时可靠回滚。

---

## 12. M4：测试和自动检查任务

### QA-01 配置前端代码质量与测试工具

**开发内容**

- 配置 ESLint。
- 配置格式化规则。
- 配置 Vitest 和 Vue Test Utils。
- 增加 `lint`、`typecheck`、`test` 脚本。
- 根据现有 CSS 规模决定是否引入 Stylelint。

### QA-02 增加后端权限与管理接口测试

**测试范围**

- 通知、提醒、收藏和个人资料用户隔离。
- 用户和管理员角色。
- 管理端所有 CRUD。
- 用户停用后的登录和接口访问。
- 正确 HTTP 状态码和错误码。

### QA-03 增加同步和提醒并发测试

**测试范围**

- 同一比赛重复同步。
- 两个同步任务竞争锁。
- 429 和临时错误重试。
- 两个提醒扫描器并发认领。
- 邮件失败后的重试和最终失败。

### QA-04 增加前端组件和页面测试

**组件测试**

- 比赛卡片状态和比分。
- 队徽加载失败回退。
- 赛事弹窗。
- 提醒设置。
- 时区切换。

**页面测试**

- 登录回跳。
- 管理员路由守卫。
- 动态轮次筛选。
- 联赛快速切换竞态。

### QA-05 增加核心端到端测试和自动检查

**端到端场景**

1. 注册 → 登录 → 关注球队 → 收藏比赛 → 设置提醒。
2. 管理员登录 → 修改比赛 → 用户端查看更新。
3. 切换六项赛事并验证移动端布局。
4. Token 过期 → 自动刷新或重新登录。
5. 普通用户访问管理端被拒绝。

**自动检查内容**

- 前端 lint、类型、测试、构建。
- 后端格式、静态检查和测试。
- 迁移检查。
- 敏感信息扫描。

---

## 13. M4：性能、无障碍和可观测性任务

### PERF-01 优化静态资源和字体缓存

**开发内容**

- 哈希资源使用长期缓存。
- HTML 使用短缓存或不缓存。
- 保持 WOFF2 和 Unicode Range。
- 检查六项赛事首页实际字体请求量。
- 队徽、头像设置固定尺寸和懒加载。

### PERF-02 优化列表、查询和缓存策略

**前端**

- 长列表分页或虚拟化。
- 搜索防抖。
- 首页和赛事概览复用 Store 数据。

**后端**

- 检查首页聚合查询。
- 为赛事概览和积分榜增加短时缓存。
- 分析慢查询和 Preload。
- 使用真实查询计划验证新增索引。

### PERF-03 建立性能预算和回归检测

**目标**

- 主要首屏 JS gzip 不高于 200 KB。
- 首屏 CSS gzip 不高于 70 KB。
- LCP 不高于 2.5 秒。
- CLS 小于 0.1。
- INP 不高于 200 毫秒。

### A11Y-01 完善赛事弹窗焦点管理

**开发内容**

- 打开时聚焦当前赛事或标题。
- Tab 焦点限制在弹窗内。
- Escape 关闭。
- 关闭后恢复触发按钮焦点。
- 打开和切换结果支持读屏播报。

### A11Y-02 修复卡片、表单和状态语义

**开发内容**

- 可点击卡片使用链接或按钮语义。
- 图标按钮补齐可读名称。
- 表单错误通过 `aria-describedby` 关联。
- 加载区域增加 `aria-busy`。
- 状态变化使用合适的 `aria-live`。

### A11Y-03 完成键盘和读屏回归

**验收场景**

- 仅使用键盘完成赛事切换、登录、关注、收藏和提醒。
- 焦点顺序清晰可见。
- 读屏能够识别比赛双方、比分、状态和操作。

### OBS-01 增加结构化日志与 Request ID

**开发内容**

- 日志使用稳定字段。
- 记录 request_id、路由、状态码、耗时、用户 ID。
- 管理操作记录管理员 ID。
- Token、密码、API Key 和邮箱按规则脱敏。

### OBS-02 增加存活与就绪检查

**接口**

```text
GET /health/live
GET /health/ready
```

**验收标准**

- live 只判断进程响应。
- ready 检查数据库和 Redis。
- 外部赛事供应商失败不影响基础存活状态。

### OBS-03 增加优雅关闭和任务停止

**开发内容**

- HTTP 服务设置读取、写入和空闲超时。
- 系统信号触发停止。
- 后台任务接收 context 并停止 ticker。
- 等待进行中的请求和任务在超时内完成。
- 关闭数据库和 Redis 连接。

---

## 14. M5：产品增强任务

### ENH-01 评估并实现 PWA 基础能力

**前置条件**

- M1～M4 核心任务完成。
- 移动端主流程稳定。

**开发内容**

- 应用清单与主屏图标。
- 基础离线页面。
- 更新提示。
- 不缓存用户敏感接口响应。

### ENH-02 评估并实现 Web Push

**开发内容**

- 通知权限申请时机设计。
- Push 订阅保存和撤销。
- 比赛提醒发送系统通知。
- 点击通知进入比赛详情。
- 处理拒绝授权和不支持场景。

### ENH-03 增加关注球队自动提醒

**开发内容**

- 用户设置：全部比赛、焦点比赛、关闭。
- 新比赛同步后为关注球队生成提醒。
- 赛程变化时更新提醒时间。
- 取消关注时按产品规则保留或取消未来提醒。

---

## 15. 推荐提交批次

为了降低一次性改动风险，建议按以下批次提交：

### 批次 1：权限紧急修复

- SEC-01
- SEC-05
- 对应测试

### 批次 2：接口错误规范基础

- API-01
- API-02
- API-03

### 批次 3：后台后端真实性

- ADM-01～ADM-06
- QA-02 中的后台接口测试

### 批次 4：后台前端补全

- ADM-07～ADM-09
- API-04A

### 批次 5：赛事动态化

- DATA-01～DATA-04
- DATA-07～DATA-08

### 批次 6：时区、轮询和赛程

- DATA-05
- LIVE-01～LIVE-03

### 批次 7：同步幂等

- REL-01～REL-06
- QA-03 的同步测试

### 批次 8：提醒可靠性

- REL-07
- ADM-10
- QA-03 的提醒测试

### 批次 9：认证升级

- SEC-02～SEC-04
- 对应前后端测试

### 批次 10：质量与体验

- QA-01、QA-04、QA-05
- PERF-01～PERF-03
- A11Y-01～A11Y-03
- OBS-01～OBS-03

每个批次必须可以独立构建、测试和回退，不建议把多个里程碑合并为一个超大提交。

---

## 16. 每个任务的完成定义

任务只有同时满足以下条件才可以标记为 `DONE`：

### 代码

- [ ] 不存在占位返回或未处理错误。
- [ ] 没有新增无约束的 `any`。
- [ ] 数据库写操作检查错误和影响行数。
- [ ] 用户资源操作包含用户归属条件。
- [ ] 关键业务操作具备事务或幂等保证。

### 测试

- [ ] 正常路径测试通过。
- [ ] 参数错误路径测试通过。
- [ ] 权限或边界场景测试通过。
- [ ] 前端生产构建通过。
- [ ] 后端全量测试通过。

### 体验

- [ ] 有加载、成功、错误和空状态。
- [ ] 移动端无横向溢出。
- [ ] 键盘可以完成主要操作。
- [ ] 网络失败不会留下错误的乐观状态。

### 文档

- [ ] 接口变化已更新文档。
- [ ] 数据库变化已增加迁移说明。
- [ ] 新环境变量已加入示例配置。
- [ ] README 中的运行和验证方式保持准确。

---

## 17. 里程碑验收清单

### M1 安全与后台真实性

- [ ] 通知、提醒、收藏和个人资料不存在已知越权。
- [ ] 登录、注册具备限流和密码策略。
- [ ] 普通用户无法进入管理端。
- [ ] 管理端所有可见按钮都有真实功能。
- [ ] 管理操作刷新页面后结果仍然存在。
- [ ] 仓库中不再跟踪用户上传内容。

### M2 数据正确性与实时性

- [ ] 赛季、轮次和进度来自后端。
- [ ] 六项赛事切换后数据范围正确。
- [ ] 时区设置作用于全部比赛时间。
- [ ] 直播分钟和延期、取消状态正确。
- [ ] 直播期间页面自动刷新。
- [ ] 快速切换赛事不会出现旧数据覆盖。

### M3 同步、提醒和 API 稳定性

- [ ] 重复同步不产生重复比赛。
- [ ] 并发同步不会同时执行同一资源。
- [ ] 同一提醒不会重复发送。
- [ ] API 使用正确 HTTP 状态码。
- [ ] 客户端不会看到数据库内部错误。
- [ ] 核心接口具备明确 DTO 和 TypeScript 类型。

### M4 测试、性能与体验

- [ ] 核心用户链路具备端到端测试。
- [ ] 后端权限和并发任务具备集成测试。
- [ ] 自动检查覆盖类型、构建、测试和敏感信息。
- [ ] 六项赛事主流移动端尺寸无布局溢出。
- [ ] 弹窗和表单具备完整键盘操作。
- [ ] 性能指标达到预算。

---

## 18. 第一轮建议直接开发的任务

为了尽快降低风险，建议第一轮按以下顺序执行：

1. `BASE-01`：记录构建和测试基线。
2. `BASE-02`：准备权限测试用户。
3. `SEC-01`：修复通知越权并补测试。
4. `SEC-05`：增加用户和管理员路由守卫。
5. `API-01`：统一正确 HTTP 状态码和错误类型。
6. `API-02`：增加 Request ID 与安全错误响应。
7. `ADM-01`：定义管理 DTO 和审计模型。
8. `ADM-02`：完成球队管理后端 CRUD。
9. `ADM-05`：完成比赛管理后端 CRUD。
10. `ADM-07`：增加独立管理员登录。
11. `ADM-08`：后台首页切换真实 KPI。

第一轮完成后，项目最明显的权限风险和“后台看起来可用但实际不落库”问题将得到控制，并为后续动态数据和同步可靠性改造建立稳定基础。
