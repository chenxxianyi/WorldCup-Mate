# WorldCup Mate 后端开发方案

## 1. 项目定位

后端负责 WorldCup Mate 的核心数据、业务规则、认证授权、管理后台接口、积分榜计算、比赛提醒、站内通知、CSV 导入和 API 文档。目标是交付一个可运行、可扩展、可通过 Docker 启动的 MVP 后端服务。

## 2. 技术栈

- Go
- Gin
- MySQL
- Redis
- GORM
- JWT
- bcrypt
- Swagger / OpenAPI
- Docker / Docker Compose

## 3. 推荐目录结构

```text
backend/
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── config/
│   ├── database/
│   ├── middleware/
│   ├── models/
│   ├── routes/
│   ├── handlers/
│   ├── services/
│   ├── repositories/
│   ├── utils/
│   └── jobs/
├── migrations/
├── docs/
├── go.mod
├── go.sum
└── .env.example
```

## 4. 分层职责

- Handler：接收请求、参数校验、调用 Service、返回统一响应。
- Service：处理业务逻辑，如认证、积分计算、提醒、收藏、导入。
- Repository：封装数据库读写。
- Model：定义 GORM 模型。
- Middleware：处理 CORS、日志、异常恢复、JWT、管理员权限等。
- Jobs：处理定时任务，例如提醒扫描。
- Utils：统一响应、分页、错误码、密码加密、JWT 工具、时间工具。

## 5. 环境变量

`.env` 建议配置：

```env
APP_ENV=development
APP_PORT=8080
JWT_SECRET=please_change_me
MYSQL_DSN=root:password@tcp(mysql:3306)/worldcup_mate?charset=utf8mb4&parseTime=True&loc=Local
REDIS_ADDR=redis:6379
REDIS_PASSWORD=
REDIS_DB=0
```

要求：

- 不硬编码密钥。
- 密码必须使用 bcrypt 加密存储。
- 所有比赛时间统一使用 UTC 存储。

## 6. 数据库设计

使用 MySQL + GORM。核心表如下：

### users

- `id`
- `username`
- `email`
- `password_hash`
- `avatar`
- `timezone`
- `language`
- `role`
- `created_at`
- `updated_at`
- `deleted_at`

角色：

- `user`
- `admin`

### groups

- `id`
- `name`
- `stage`
- `created_at`
- `updated_at`

示例：Group A 到 Group L。

### teams

- `id`
- `name`
- `name_en`
- `fifa_code`
- `flag_url`
- `continent`
- `group_id`
- `description`
- `coach`
- `created_at`
- `updated_at`
- `deleted_at`

### cities

- `id`
- `name`
- `name_en`
- `country`
- `timezone`
- `description`
- `created_at`
- `updated_at`
- `deleted_at`

### stadiums

- `id`
- `name`
- `name_en`
- `city_id`
- `capacity`
- `description`
- `created_at`
- `updated_at`
- `deleted_at`

### matches

- `id`
- `match_no`
- `home_team_id`
- `away_team_id`
- `group_id`
- `stage`
- `stadium_id`
- `city_id`
- `kickoff_time_utc`
- `home_score`
- `away_score`
- `status`
- `winner_team_id`
- `importance_level`
- `recommend_tag`
- `recommend_reason`
- `created_at`
- `updated_at`
- `deleted_at`

比赛阶段：

- `group`
- `round_of_32`
- `round_of_16`
- `quarter_final`
- `semi_final`
- `third_place`
- `final`

比赛状态：

- `scheduled`
- `live`
- `finished`
- `postponed`
- `cancelled`

推荐等级：

- `0` 普通比赛
- `1` 推荐比赛
- `2` 热门比赛
- `3` 焦点比赛
- `4` 重大比赛
- `5` 决赛或最高优先级

### user_favorite_teams

- `id`
- `user_id`
- `team_id`
- `created_at`

唯一索引：`user_id + team_id`。

### user_favorite_matches

- `id`
- `user_id`
- `match_id`
- `created_at`

唯一索引：`user_id + match_id`。

### reminders

- `id`
- `user_id`
- `match_id`
- `remind_before_minutes`
- `remind_at`
- `channel`
- `status`
- `created_at`
- `updated_at`

提醒渠道：

- `site`
- `web_push`
- `email`

提醒状态：

- `pending`
- `sent`
- `cancelled`
- `failed`

### group_standings

- `id`
- `group_id`
- `team_id`
- `played`
- `won`
- `drawn`
- `lost`
- `goals_for`
- `goals_against`
- `goal_difference`
- `points`
- `rank`
- `qualification_status`
- `updated_at`

唯一索引：`group_id + team_id`。

晋级状态：

- `unknown`
- `qualified`
- `possible`
- `eliminated`

### notifications

- `id`
- `user_id`
- `title`
- `content`
- `type`
- `is_read`
- `created_at`

## 7. 统一响应与错误码

成功响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

失败响应：

```json
{
  "code": 400,
  "message": "error message",
  "data": null
}
```

分页响应：

```json
{
  "list": [],
  "total": 100,
  "page": 1,
  "pageSize": 20
}
```

错误码：

- `0` success
- `400` bad request
- `401` unauthorized
- `403` forbidden
- `404` not found
- `409` conflict
- `500` server error

## 8. 中间件

需要实现：

- CORS
- Logger
- Recover
- JWT Auth
- Admin Auth
- Request ID
- Rate Limit，可选

鉴权规则：

- 游客可以访问首页数据、赛程、比赛详情、球队、积分榜等公开接口。
- 普通用户可以收藏球队、收藏比赛、设置提醒、查看通知和个人资料。
- 管理员可以访问 `/api/admin/*` 接口。

## 9. RESTful API 设计

### 认证接口

- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/user/profile`
- `PUT /api/user/profile`

### 赛程接口

- `GET /api/matches`
- `GET /api/matches/today`
- `GET /api/matches/tomorrow`
- `GET /api/matches/upcoming`
- `GET /api/matches/recommended`
- `GET /api/matches/:id`
- `GET /api/matches/by-team/:teamId`
- `GET /api/matches/by-group/:groupId`
- `GET /api/matches/by-stage/:stage`

`GET /api/matches` 支持 query：

- `page`
- `pageSize`
- `date`
- `teamId`
- `groupId`
- `stage`
- `cityId`
- `status`
- `keyword`

### 球队接口

- `GET /api/teams`
- `GET /api/teams/:id`
- `GET /api/teams/:id/matches`

支持筛选：

- `continent`
- `groupId`
- `keyword`

### 小组和积分榜接口

- `GET /api/groups`
- `GET /api/groups/:id`
- `GET /api/groups/:id/standings`
- `GET /api/standings`
- `GET /api/standings/best-third`

### 城市和球场接口

- `GET /api/cities`
- `GET /api/stadiums`
- `GET /api/stadiums/:id`

### 收藏接口

需要登录：

- `POST /api/favorites/teams/:teamId`
- `DELETE /api/favorites/teams/:teamId`
- `GET /api/favorites/teams`
- `POST /api/favorites/matches/:matchId`
- `DELETE /api/favorites/matches/:matchId`
- `GET /api/favorites/matches`

### 提醒接口

需要登录：

- `POST /api/reminders`
- `GET /api/reminders`
- `PUT /api/reminders/:id`
- `DELETE /api/reminders/:id`

创建提醒 body：

```json
{
  "matchId": 1,
  "remindBeforeMinutes": 60,
  "channel": "site"
}
```

### 通知接口

- `GET /api/notifications`
- `PUT /api/notifications/:id/read`
- `PUT /api/notifications/read-all`

### 管理后台接口

所有 `/api/admin/*` 接口需要管理员权限。

- `POST /api/admin/login`
- `GET /api/admin/dashboard`
- `GET /api/admin/teams`
- `POST /api/admin/teams`
- `PUT /api/admin/teams/:id`
- `DELETE /api/admin/teams/:id`
- `GET /api/admin/groups`
- `POST /api/admin/groups`
- `PUT /api/admin/groups/:id`
- `GET /api/admin/cities`
- `POST /api/admin/cities`
- `PUT /api/admin/cities/:id`
- `DELETE /api/admin/cities/:id`
- `GET /api/admin/stadiums`
- `POST /api/admin/stadiums`
- `PUT /api/admin/stadiums/:id`
- `DELETE /api/admin/stadiums/:id`
- `GET /api/admin/matches`
- `POST /api/admin/matches`
- `PUT /api/admin/matches/:id`
- `DELETE /api/admin/matches/:id`
- `PUT /api/admin/matches/:id/score`
- `PUT /api/admin/matches/:id/status`
- `POST /api/admin/matches/import`
- `GET /api/admin/standings`
- `POST /api/admin/standings/recalculate`
- `PUT /api/admin/standings/:id`
- `GET /api/admin/users`
- `PUT /api/admin/users/:id/status`

## 10. 核心业务逻辑

### 本地时间换算

数据库统一存储 `kickoff_time_utc`。后端只返回 UTC 时间，前端根据浏览器时区或用户设置的 `timezone` 进行显示转换。

### 小组积分计算

当管理员将小组赛状态更新为 `finished` 后，系统自动重新计算该小组积分榜。

积分规则：

- 胜：3 分
- 平：1 分
- 负：0 分

统计字段：

- `played`
- `won`
- `drawn`
- `lost`
- `goals_for`
- `goals_against`
- `goal_difference`
- `points`

排序规则：

1. `points` 高者靠前。
2. `goal_difference` 高者靠前。
3. `goals_for` 高者靠前。
4. 暂用 `team_id` 排序，后续可扩展公平竞赛积分和相互战绩。

晋级规则：

- 每组前两名标记为 `qualified`。
- 第三名先标记为 `possible`。
- 第四名按情况标记为 `unknown` 或 `eliminated`。

### 最佳第三名榜计算

从 12 个小组中取每组第 3 名，共 12 队。

排序规则：

1. `points`
2. `goal_difference`
3. `goals_for`
4. `team_id`

前 8 名标记为 `qualified`，后 4 名标记为 `eliminated` 或 `possible`。

### 比赛提醒逻辑

用户设置提醒后：

1. 根据 `kickoff_time_utc` 和 `remind_before_minutes` 计算 `remind_at`。
2. 定时任务每分钟扫描 `pending` 状态且 `remind_at <= 当前时间` 的提醒。
3. 创建站内通知 `notification`。
4. 更新 `reminder.status = sent`。

MVP 阶段只做站内通知，Web Push 和 Email 只预留字段。

### 推荐比赛逻辑

首页热门推荐比赛取：

- `importance_level >= 1`
- 比赛状态未结束

排序：

1. `importance_level DESC`
2. `kickoff_time_utc ASC`

### 收藏逻辑

- 收藏球队后，首页“我关注的球队”展示这些球队的下一场比赛。
- 收藏比赛后，“我的关注比赛”展示这些比赛。
- 收藏表需要使用唯一索引避免重复收藏。

## 11. CSV 导入比赛

管理后台支持 CSV 批量导入比赛数据。

CSV 字段：

```text
match_no,
home_team_name,
away_team_name,
group_name,
stage,
stadium_name,
city_name,
kickoff_time_utc,
importance_level,
recommend_tag
```

导入逻辑：

1. 根据球队名查找主队和客队。
2. 根据小组名查找 group。
3. 根据球场名查找 stadium。
4. 根据城市名查找 city。
5. 创建 match。
6. 如果数据错误，返回错误行号和原因。

## 12. 种子数据

需要提供 seed 脚本或初始化逻辑，用于创建：

- 管理员账号。
- Group A 到 Group L。
- 示例球队数据。
- 示例城市数据。
- 示例球场数据。
- 示例比赛数据。

管理员账号：

- email: `admin@worldcup.local`
- password: `admin123456`

真实赛程数据可后续通过后台导入，MVP 阶段先提供示例数据，保证系统可运行。

## 13. Swagger 文档

需要为主要接口提供 Swagger 注释，并生成接口文档。README 中需要说明 API 文档访问方式。

重点覆盖：

- 认证接口。
- 赛程接口。
- 球队接口。
- 积分榜接口。
- 收藏接口。
- 提醒接口。
- 管理后台接口。

## 14. Docker 部署

根目录需要提供 `docker-compose.yml`，包含：

- frontend
- backend
- mysql
- redis

MySQL：

- 数据库名：`worldcup_mate`
- 用户名：`root`
- 密码：`password`

启动方式：

```bash
docker-compose up -d
```

README 需要写清楚：

- 项目介绍。
- 技术栈。
- 本地开发启动方式。
- Docker 启动方式。
- 环境变量说明。
- 管理员账号。
- API 文档访问方式。
- 项目目录说明。

## 15. 后端测试

至少提供以下测试：

- 用户注册登录测试。
- 赛程查询测试。
- 球队查询测试。
- 收藏球队测试。
- 收藏比赛测试。
- 小组积分计算测试。
- 最佳第三名计算测试。
- 提醒创建测试。

如果时间有限，优先完成认证、赛程查询、收藏、提醒和积分计算测试。

## 16. 开发顺序

1. 创建后端目录结构和 Go 项目。
2. 配置读取、MySQL 连接、Redis 连接。
3. 定义 GORM 模型和迁移。
4. 初始化 Gin 路由、统一响应、错误处理和中间件。
5. 实现 JWT、bcrypt、注册、登录、用户资料和管理员登录。
6. 实现基础数据模块：groups、teams、cities、stadiums、matches。
7. 实现用户端核心 API：赛程查询、比赛详情、今日比赛、推荐比赛、球队详情、积分榜。
8. 实现收藏、提醒、站内通知和提醒扫描任务。
9. 实现后台管理接口：球队、城市、球场、比赛、比分、推荐等级、用户管理。
10. 实现积分榜重算和最佳第三名榜计算。
11. 实现 CSV 导入。
12. 补充 Swagger 文档、seed 数据、测试和 Docker 配置。

## 17. MVP 边界

第一版不做：

- 新闻资讯。
- 视频集锦。
- 社区评论。
- 真钱竞猜。
- 支付系统。
- 原生 App。
- 复杂 AI 解说。
- 直播流。
- 第三方体育数据自动同步。

后端需要预留扩展空间：

- AI 比赛看点。
- 赛程日历订阅。
- Web Push。
- 邮件提醒。
- 轻量竞猜。
- 多语言。
- 第三方数据 API。

## 18. 代码质量要求

- 代码模块化，避免所有逻辑堆在单个文件。
- Handler、Service、Repository 分层清晰。
- 数据库字段命名统一。
- 所有时间统一使用 UTC 存储。
- 所有需要登录的接口必须校验 JWT。
- 管理后台接口必须校验 admin 权限。
- 密码必须使用 bcrypt 加密存储。
- 不硬编码密钥。
- 列表接口支持分页。
- 重要操作有错误提示。
- 删除操作需要前端二次确认，后端需要权限和存在性校验。
