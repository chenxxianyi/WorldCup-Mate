# WorldCup Mate V2 自用版 AI 助手后端开发方案

## 1. 文档目标

本文档从 `AI_DEVELOPMENT_PLAN.md` 和 `AI_DEVELOPMENT_TASK_PLAN.md` 中拆出后端部分，并结合当前 `backend/` 代码结构，作为 V2 自用版 AI 看球助手的后端开发执行方案。

V2 后端目标是在现有 Go/Gin/GORM 服务上增加一套轻量 AI 能力：

- 为比赛详情生成 AI 看点。
- 为今日比赛生成观赛推荐。
- 为小组积分生成出线形势解读。
- 提供足球规则和术语解释。
- 提供 AI 聊天助手。
- 提供分享文案生成。
- 提供 Mock Provider 和 OpenAI-compatible Provider。
- 记录 AI 调用日志和生成内容。

后端负责 AI 配置、上下文构建、Prompt、安全边界、Provider 调用、结构化解析、缓存、限流和日志。前端只负责展示和交互。

## 2. 当前后端基础

当前项目已经具备：

- Go 1.23 + Gin + GORM。
- MySQL 和 Redis 初始化。
- JWT、bcrypt、CORS、日志、异常恢复、管理员鉴权中间件。
- 统一响应工具：`internal/utils/response.go`。
- 已有领域模型：用户、球队、比赛、城市、球场、小组、积分榜、收藏、提醒、通知、同步状态。
- 已有分层：
  - `handlers`
  - `services`
  - `repositories`
  - `models`
  - `routes`
  - `jobs`
  - `providers`
- football-data.org 同步能力。
- 启动入口 `backend/cmd/server/main.go` 已执行 `AutoMigrate`、seed、后台任务和路由注册。

需要注意：

- 当前部分 seed 数据和业务文案存在中文编码异常，AI 上下文读取这些字段前必须先治理或兜底。
- 当前 Docker 生产配置需要补齐 `JWT_SECRET`、`CORS_ALLOWED_ORIGINS` 和 MySQL 用户配置。
- AI API Key 只能来自环境变量，不能进入前端、日志或数据库明文字段。

## 3. 后端范围边界

### 必须实现

- AI 配置读取。
- AI 数据模型。
- AI Repository。
- Provider 接口。
- Mock Provider。
- OpenAI-compatible Provider。
- Prompt 和 Safety 规则。
- Context Builder。
- Parser 兜底。
- AI Service。
- AI Handler 和路由。
- usage log 入库。
- generated content 入库。
- 简单 Redis 缓存。
- 登录用户每日限流。

### 暂缓实现

- 流式输出。
- 复杂 RAG。
- 多供应商管理后台。
- Token 成本报表后台。
- AI 反馈系统。
- 新闻聚合。
- 语音问答。
- 投注建议、赔率分析或博彩相关功能。
- 公网高并发商业化方案。

## 4. 建议目录结构

在当前 `backend/internal/` 下新增：

```text
backend/internal/
├── ai/
│   ├── context_builder.go
│   ├── mock_provider.go
│   ├── openai_compatible.go
│   ├── parser.go
│   ├── prompts.go
│   ├── provider.go
│   └── safety.go
├── handlers/
│   └── ai_handler.go
├── models/
│   ├── ai_conversation.go
│   ├── ai_generated_content.go
│   ├── ai_message.go
│   └── ai_usage_log.go
├── repositories/
│   └── ai_repo.go
└── services/
    └── ai_service.go
```

不建议第一版新增 `ai_feedbacks` 表，除非实际使用中确实需要点赞、点踩和 Prompt 迭代数据。

## 5. 配置设计

修改文件：

```text
backend/internal/config/config.go
backend/.env.example
```

新增环境变量：

```env
AI_PROVIDER=mock
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=
AI_MODEL=
AI_TIMEOUT_SECONDS=60
AI_DAILY_LIMIT_USER=50
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=1200
AI_CACHE_ENABLED=true
```

配置结构建议：

```go
type Config struct {
    AIProvider       string
    AIBaseURL        string
    AIAPIKey         string
    AIModel          string
    AITimeoutSeconds int
    AIDailyLimitUser int
    AITemperature    float64
    AIMaxTokens      int
    AICacheEnabled   bool
}
```

要求：

- 默认 `AI_PROVIDER=mock`。
- 未配置 API Key 时不允许真实 Provider 偷偷发起请求。
- 生产环境不能把 API Key 打到日志。
- 配置初始化建议在 `main.go` 中传入 `services.ConfigureAI(...)` 或 `ai.NewProvider(...)`。

## 6. 数据模型设计

新增模型文件：

```text
backend/internal/models/ai_conversation.go
backend/internal/models/ai_message.go
backend/internal/models/ai_generated_content.go
backend/internal/models/ai_usage_log.go
```

修改文件：

```text
backend/cmd/server/main.go
```

在 `AutoMigrate` 中加入四张表。

### ai_conversations

用途：保存聊天会话。

字段建议：

```text
id
user_id
title
context_type
context_id
last_message
created_at
updated_at
deleted_at
```

说明：

- `context_type` 可选：`general`、`match`、`team`、`group`。
- 自用版可要求聊天必须登录，减少匿名历史和限流复杂度。

### ai_messages

用途：保存聊天消息。

字段建议：

```text
id
conversation_id
user_id
role
content
provider
model
prompt_tokens
completion_tokens
total_tokens
created_at
```

说明：

- `role` 可选：`user`、`assistant`、`system`。
- 不建议保存完整系统 Prompt，避免后续维护成本和潜在敏感信息泄露。

### ai_generated_contents

用途：保存结构化生成内容，例如比赛看点、今日推荐、小组分析、分享文案。

字段建议：

```text
id
user_id
type
target_type
target_id
content_json
content_markdown
provider
model
cache_key
created_at
updated_at
```

说明：

- `type` 可选：`match_insight`、`today_recommendations`、`group_analysis`、`explain`、`share_copy`。
- `content_json` 用于前端结构化展示。
- `content_markdown` 用于 parser 兜底或聊天类文本展示。

### ai_usage_logs

用途：记录 AI 调用情况，便于自查成本和错误。

字段建议：

```text
id
user_id
ip
endpoint
provider
model
prompt_tokens
completion_tokens
total_tokens
status
error_message
latency_ms
created_at
```

说明：

- 成功和失败都要写 usage log。
- `error_message` 必须脱敏，不能包含 API Key。
- 自用版可以先不做复杂统计页面，通过数据库查询即可。

## 7. Repository 设计

新增文件：

```text
backend/internal/repositories/ai_repo.go
```

职责：

- 保存 generated content。
- 按 cache key 查询 generated content。
- 保存 usage log。
- 创建 conversation。
- 保存 user message。
- 保存 assistant message。
- 查询 conversation 列表。
- 查询 conversation detail。
- 删除 conversation。

要求：

- Repository 不拼 Prompt。
- Repository 不调用 Provider。
- Repository 不判断安全策略。
- 所有会话查询必须按 `user_id` 过滤，避免串数据。

建议函数：

```go
func SaveGeneratedContent(content *models.AIGeneratedContent) error
func GetGeneratedContentByCacheKey(cacheKey string) (*models.AIGeneratedContent, error)
func SaveUsageLog(log *models.AIUsageLog) error
func CreateConversation(conv *models.AIConversation) error
func SaveMessage(msg *models.AIMessage) error
func ListConversations(userID uint) ([]models.AIConversation, error)
func GetConversationWithMessages(userID uint, conversationID uint) (*models.AIConversation, []models.AIMessage, error)
func DeleteConversation(userID uint, conversationID uint) error
```

## 8. Provider 设计

新增文件：

```text
backend/internal/ai/provider.go
backend/internal/ai/mock_provider.go
backend/internal/ai/openai_compatible.go
```

### Provider 接口

建议：

```go
type Provider interface {
    Name() string
    Chat(ctx context.Context, req ChatRequest) (*ChatResponse, error)
}

type ChatRequest struct {
    SystemPrompt string
    UserPrompt   string
    Messages     []Message
    Model        string
    Temperature  float64
    MaxTokens    int
}

type ChatResponse struct {
    Content          string
    Model            string
    PromptTokens     int
    CompletionTokens int
    TotalTokens      int
}
```

要求：

- Provider 不知道 match、team、group 等业务概念。
- Provider 不写数据库。
- Provider 不做限流。

### Mock Provider

必须实现。

用途：

- 本地开发无需 API Key。
- 前端联调无需外网。
- 后端测试稳定。

要求：

- `AI_PROVIDER=mock` 时可用。
- 覆盖 `match-insight`、`today-recommendations`、`group-analysis`、`explain`、`share-copy`、`chat`。
- 返回结构稳定。
- 不访问外部网络。

### OpenAI-compatible Provider

必须实现。

要求：

- 支持 `AI_BASE_URL`。
- 支持 `AI_API_KEY`。
- 支持 `AI_MODEL`。
- 支持超时。
- 支持 token usage 解析。
- 错误信息脱敏。
- 兼容 `/chat/completions` 风格 API。

第一版可以不支持：

- 流式输出。
- function calling。
- 图片、音频、多模态输入。

## 9. Prompt、Safety 和 Parser

新增文件：

```text
backend/internal/ai/prompts.go
backend/internal/ai/safety.go
backend/internal/ai/parser.go
```

### Prompt

统一系统人设：

```text
你是一个懂球但不装专业的世界杯看球助手。你主要服务项目主人自用，要简洁、清楚、有判断力。你不能编造事实，不能提供投注建议。数据不足时要直接说明。
```

输出原则：

- 先给结论。
- 再给 3 到 5 条理由。
- 尽量短，不写长篇论文。
- 对足球小白友好。
- 涉及预测必须克制。
- 涉及数据必须基于上下文。
- 不说“稳赢”“必中”“100% 晋级”。

### Safety

基础安全规则：

- 禁止投注建议。
- 禁止博彩诱导。
- 禁止非法直播源。
- 禁止编造比分、赛程、伤病、新闻、球员事件。
- 禁止无根据确定性预测。
- 数据不足时必须明确说明。

Safety 第一版可以做两层：

1. Prompt 中写明边界。
2. 输出后处理检查明显违规词，例如投注、稳赢、盘口、赔率、下注等。

### Parser

用途：

- 优先解析模型返回的 JSON。
- JSON 解析失败时返回 Markdown/text 兜底。
- 必要字段缺失时填默认值。
- 保证前端不会因为模型格式漂移白屏。

要求：

- match insight 必须最终返回结构化字段。
- 对数组字段做空数组兜底。
- 对评分做范围限制，例如 1 到 5。

## 10. Context Builder

新增文件：

```text
backend/internal/ai/context_builder.go
```

Context Builder 只负责从数据库组装事实，不负责生成观点。

### MatchContext

用于 `/api/ai/match-insight`。

包含：

- 比赛 ID、编号、阶段、状态。
- 主队、客队、FIFA code、英文名、中文名。
- 开球时间 UTC。
- 城市、球场。
- 小组。
- 比分和胜者，仅当数据存在。
- 小组积分。
- 用户关注球队。
- 用户时区。

要求：

- 比赛不存在返回明确错误。
- 无积分时标记“暂无积分数据”。
- 不包含乱码字段；如中文字段异常，优先使用 `name_en`、`fifa_code`。
- 不包含数据库不存在的事实。

### TodayMatchesContext

用于 `/api/ai/today-recommendations`。

包含：

- 指定日期比赛。
- 用户时区。
- 用户关注球队。
- 比赛阶段。
- 比赛重要度。
- 比赛状态。

要求：

- 当天无比赛时可查找下一比赛日。
- 只推荐数据库存在的比赛。
- 登录用户优先考虑关注球队。

### GroupContext

用于 `/api/ai/group-analysis`。

包含：

- 小组信息。
- 小组球队。
- 当前积分。
- 已结束比赛。
- 未开始比赛。
- 最佳第三名规则说明。

要求：

- 严格基于 `group_standings`。
- 不生成概率。
- 小组赛未开始时输出赛前说明。

### Chat Context

用于 `/api/ai/chat`。

支持：

- `general`
- `match`
- `team`
- `group`

要求：

- 只携带必要事实。
- 只带最近 N 条历史消息。
- 不把整库数据塞进 Prompt。

## 11. Service 设计

新增文件：

```text
backend/internal/services/ai_service.go
```

职责：

- 参数二次校验。
- 用户身份和限流判断。
- 缓存读取。
- 调用 Context Builder。
- 拼装 Prompt。
- 调用 Provider。
- Parser 解析。
- Safety 后处理。
- 保存 generated content。
- 保存 usage log。
- 返回结构化结果。

建议对外函数：

```go
func GenerateMatchInsight(ctx context.Context, req MatchInsightRequest, userID *uint, ip string) (*MatchInsightResponse, error)
func GenerateTodayRecommendations(ctx context.Context, req TodayRecommendationRequest, userID *uint, ip string) (*TodayRecommendationResponse, error)
func GenerateGroupAnalysis(ctx context.Context, req GroupAnalysisRequest, userID *uint, ip string) (*GroupAnalysisResponse, error)
func ExplainFootball(ctx context.Context, req ExplainRequest, userID *uint, ip string) (*ExplainResponse, error)
func GenerateShareCopy(ctx context.Context, req ShareCopyRequest, userID *uint, ip string) (*ShareCopyResponse, error)
func Chat(ctx context.Context, req ChatRequest, userID uint, ip string) (*ChatResponse, error)
```

要求：

- 调用失败也写 usage log。
- 不把 Provider 错误原样暴露给前端。
- `force_refresh=true` 时绕过缓存。
- Redis 不可用时不影响核心生成流程，只跳过缓存和限流。

## 12. Handler 和路由

新增文件：

```text
backend/internal/handlers/ai_handler.go
```

修改文件：

```text
backend/internal/routes/router.go
```

### 第一版接口

```text
POST /api/ai/match-insight
POST /api/ai/today-recommendations
POST /api/ai/group-analysis
POST /api/ai/explain
POST /api/ai/share-copy
POST /api/ai/chat
GET /api/ai/conversations
GET /api/ai/conversations/:id
DELETE /api/ai/conversations/:id
```

### 鉴权建议

自用版建议：

- `match-insight`：可匿名或登录用户可用。
- `today-recommendations`：可匿名或登录用户可用。
- `group-analysis`：可匿名或登录用户可用。
- `explain`：可匿名或登录用户可用。
- `share-copy`：建议登录用户可用。
- `chat` 和 conversations：登录用户可用。

如果为了实现简单，第一版也可以所有 `/api/ai/*` 都要求登录。前端需要配合显示登录引导。

### Handler 要求

- 使用 `ShouldBindJSON` 绑定请求。
- 参数错误返回 `400`。
- 未登录但接口要求登录返回 `401`。
- Service 错误转成友好 message。
- 响应继续使用 `utils.Success` 和 `utils.Error`。

## 13. API 契约

### match-insight

接口：

```text
POST /api/ai/match-insight
```

请求：

```json
{
  "match_id": 1,
  "force_refresh": false
}
```

响应 data：

```json
{
  "summary": "一句话看点",
  "watch_rating": 4,
  "reasons": [],
  "team_comparison": [],
  "beginner_tips": [],
  "qualification_impact": "",
  "should_stay_up": "",
  "suitable_for": [],
  "generated_at": "2026-06-04T12:00:00Z"
}
```

### today-recommendations

接口：

```text
POST /api/ai/today-recommendations
```

请求：

```json
{
  "date": "2026-06-11",
  "timezone": "Asia/Shanghai",
  "limit": 3,
  "force_refresh": false
}
```

响应 data：

```json
{
  "date": "2026-06-11",
  "timezone": "Asia/Shanghai",
  "recommendations": [],
  "only_one_match": {},
  "note": ""
}
```

### group-analysis

接口：

```text
POST /api/ai/group-analysis
```

请求：

```json
{
  "group_id": 1,
  "force_refresh": false
}
```

响应 data：

```json
{
  "summary": "",
  "key_points": [],
  "qualification_rules": "",
  "teams": [],
  "data_note": ""
}
```

### explain

接口：

```text
POST /api/ai/explain
```

请求：

```json
{
  "question": "什么是越位？",
  "context_type": "general",
  "context_id": 0
}
```

### share-copy

接口：

```text
POST /api/ai/share-copy
```

请求：

```json
{
  "match_id": 1,
  "platform": "wechat",
  "tone": "relaxed",
  "length": "short"
}
```

### chat

接口：

```text
POST /api/ai/chat
```

请求：

```json
{
  "conversation_id": 1,
  "message": "今晚看哪场？",
  "context_type": "general",
  "context_id": 0
}
```

响应 data：

```json
{
  "conversation_id": 1,
  "message": {
    "role": "assistant",
    "content": ""
  }
}
```

## 14. 缓存与限流

### 缓存

Redis key 建议：

```text
ai:match_insight:{matchId}:{lang}
ai:today_recommendations:{date}:{timezone}:{userIdOrGuest}
ai:group_analysis:{groupId}
```

建议 TTL：

- 比赛看点：6 小时。
- 今日推荐：30 分钟。
- 小组分析：15 分钟。

要求：

- `force_refresh=true` 绕过缓存并更新缓存。
- Redis 不可用时继续调用 Provider。
- 缓存内容可以同步写入 `ai_generated_contents`。

### 限流

登录用户每日限流 key：

```text
ai:limit:user:{userId}:{date}
```

默认：

```env
AI_DAILY_LIMIT_USER=50
```

要求：

- 达到上限返回友好提示。
- Mock Provider 可以选择不计入真实成本，但建议仍走限流结构，便于真实 Provider 切换。
- Redis 不可用时允许继续使用，并写日志提示。

## 15. 数据治理前置任务

AI 开发前必须先处理会进入上下文的乱码字段，至少覆盖：

```text
teams.name
teams.continent
cities.name
stadia.name
matches.recommend_tag
matches.recommend_reason
```

治理原则：

- 优先保留 `name_en`、`fifa_code`、`external_id` 作为稳定匹配依据。
- 不做大规模复杂合并。
- 不破坏外键关系。
- Context Builder 中要有兜底：中文字段异常时优先使用英文名或 FIFA code。

验收：

- MatchContext 不输出明显乱码。
- 比赛详情页和球队页无明显乱码。
- AI 不会把乱码原样传给模型。

## 16. 开发阶段拆分

### B0 基线与数据治理

任务：

- 运行 `go test ./...` 记录当前状态。
- 检查 AI 上下文字段乱码。
- 修复最小必要数据。

验收：

- AI 上下文无明显乱码。
- 已知构建或测试失败有记录。

### B1 AI 配置与模型

任务：

- 修改 `config.go`。
- 修改 `.env.example`。
- 新增 AI 四个模型。
- 修改 `main.go` AutoMigrate。

验收：

- 后端可启动。
- AI 配置有默认值。
- 数据表可自动创建。

### B2 Provider 和基础安全

任务：

- 新增 Provider 接口。
- 新增 Mock Provider。
- 新增 Prompt。
- 新增 Safety。
- 新增 Parser。

验收：

- `AI_PROVIDER=mock` 可用。
- Mock 不访问外网。
- Parser 能返回稳定结构。

### B3 Match Insight 最小闭环

任务：

- 新增 MatchContext。
- 新增 AI Repository。
- 新增 AI Service。
- 新增 AI Handler。
- 路由接入 `POST /api/ai/match-insight`。
- usage log 和 generated content 入库。

验收：

- 请求 match-insight 能返回结构化结果。
- 比赛不存在返回友好错误。
- 调用失败也写 usage log。
- `go test ./...` 通过或记录既有失败。

### B4 OpenAI-compatible Provider

任务：

- 实现真实 Provider。
- 支持 Base URL、API Key、Model、Timeout。
- 解析 token usage。
- 错误脱敏。

验收：

- Mock 和真实 Provider 可配置切换。
- API Key 不出现在响应或日志。
- Provider 失败有友好错误。

### B5 今日推荐和小组分析

任务：

- 新增 TodayMatchesContext。
- 新增 GroupContext。
- 实现 `/api/ai/today-recommendations`。
- 实现 `/api/ai/group-analysis`。
- 接入缓存。

验收：

- 今日无比赛时能说明或找到下一比赛日。
- 小组分析严格基于积分榜。
- 不生成虚假概率。

### B6 聊天、解释和分享文案

任务：

- 实现 `/api/ai/chat`。
- 实现会话列表、详情、删除。
- 实现 `/api/ai/explain`。
- 实现 `/api/ai/share-copy`。
- 保存聊天消息。

验收：

- 登录用户可保存聊天历史。
- 只携带最近 N 条历史。
- 分享文案不涉及投注和虚构事实。

### B7 限流、文档与最终检查

任务：

- 实现每日限流。
- 完善缓存绕过逻辑。
- 更新 README AI 配置说明。
- 运行 `go test ./...`。

验收：

- 达到限流有友好提示。
- Redis 不可用不阻塞核心功能。
- 文档能指导 Mock 和真实 Provider 启动。

## 17. 测试建议

优先补充后端测试：

- Mock Provider 返回结构测试。
- Parser JSON 和非 JSON 兜底测试。
- Safety 明显违规词过滤测试。
- MatchContext 比赛不存在测试。
- MatchContext 无积分数据测试。
- match-insight handler 参数错误测试。
- usage log 成功和失败写入测试。

如果时间有限，至少保证：

- `go test ./...` 能跑。
- Mock Provider 下 `/api/ai/match-insight` 可用。
- Provider 失败不会泄露 API Key。

## 18. 代码质量要求

- Handler 只做参数、鉴权和响应。
- Service 负责编排限流、缓存、上下文、Provider、日志。
- Context Builder 只组装数据库事实。
- Provider 只负责模型调用。
- Repository 只负责数据库读写。
- Safety 和 Parser 独立可测试。
- 所有时间存 UTC。
- 所有 AI 错误对前端友好。
- API Key 不进入前端、不进入响应、不进入日志。
- 不编造比分、赛程、伤病、新闻、投注建议。

## 19. 最小闭环清单

如果只想最快打通效果，后端按这个顺序做：

1. 修复 AI 上下文会读取的乱码字段。
2. 新增 AI 配置。
3. 新增 AI 数据模型并加入 AutoMigrate。
4. 新增 Provider 接口。
5. 实现 Mock Provider。
6. 新增 Prompt、Safety、Parser。
7. 实现 MatchContext。
8. 实现 AI Repository。
9. 实现 `GenerateMatchInsight`。
10. 实现 `POST /api/ai/match-insight` Handler 和路由。
11. 确认 usage log 入库。
12. 运行 `go test ./...`。
