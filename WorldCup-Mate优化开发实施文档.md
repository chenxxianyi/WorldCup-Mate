# WorldCup Mate 优化开发实施文档

> 文档版本：v1.0  
> 编制日期：2026-08-05  
> 适用范围：`frontend/`、`backend/`、Docker 部署、测试与运营后台

## 1. 文档目标

WorldCup Mate 已具备多赛事切换、赛程、积分榜、球队、收藏、提醒、通知、后台管理、第三方数据同步和赛事主题等基础能力，当前已达到“可用 Beta”阶段。

下一阶段不以继续堆叠联赛和页面为主要目标，而是完成以下升级：

1. 修复影响生产部署、仓库安全和用户数据隐私的问题。
2. 补齐后台运营闭环，使核心赛事数据可在管理端维护。
3. 提升赛程、首页和提醒系统的性能与可靠性。
4. 增加淘汰赛对阵树、丰富比赛中心等核心观赛能力。
5. 建立 CI、端到端测试、健康检查、日志和告警能力。
6. 将产品从“赛事信息展示平台”升级为“个人化观赛助手”。

## 2. 当前项目基线

### 2.1 已具备能力

- Vue 3、TypeScript、Vite、Pinia、Vue Router 用户端。
- Go、Gin、GORM、MySQL、Redis 后端。
- 世界杯与五大联赛多赛事切换。
- 赛程、比赛详情、球队、球队详情、积分榜。
- 用户注册登录、Token 刷新、收藏、关注、提醒和站内通知。
- 管理员认证、赛事配置、焦点赛事配置和同步管理。
- football-data.org 数据同步、重试与同步锁。
- 自适应直播轮询、请求取消与竞态保护。
- 联赛主题、深浅色模式和响应式布局。

### 2.2 质量检查结果

本轮检查结果如下：

| 检查项 | 结果 |
| --- | --- |
| 前端生产构建 | 通过 |
| 前端单元测试 | 8 个测试文件、44 个用例通过 |
| 后端测试 | `go test ./...` 通过 |
| ESLint | 0 个错误、101 个警告 |
| Git 工作区 | 检查时无未提交修改 |

### 2.3 当前主要风险

1. Git 仍跟踪约 12,626 个 `frontend/node_modules` 文件，体积约 140 MB。
2. Git 仍跟踪 `backend/uploads/avatars` 下的 7 个上传头像，存在隐私风险。
3. Git 仍跟踪少量 `frontend/dist` 构建产物。
4. Docker Compose 的生产 JWT 密钥不满足后端最小长度要求。
5. Docker Compose 未配置生产环境必需的 `CORS_ALLOWED_ORIGINS`。
6. 生产 Nginx 未代理 `/uploads/`，Docker 环境中头像可能无法访问。
7. 后台球队、比赛等页面主要是只读列表，尚未形成完整 CRUD。
8. 赛程页联赛轮次硬编码，并会并发拉取全部分页数据。
9. 首页存在一次列表请求加多次关注球队赛程请求的 N+1 模式。
10. 缺少 CI、端到端测试、健康检查、结构化日志、指标和优雅关闭。
11. 赛事弹窗焦点管理、比赛卡键盘语义和减弱动效支持不完整。
12. 用户可以选择 English，但前端尚未真正完成国际化。

## 3. 优化原则

### 3.1 先稳定再扩展

生产部署、数据隐私、提醒可靠性、数据库迁移和自动化测试优先于预测、分享等增长功能。

### 3.2 保留现有视觉方向

保留当前赛事主题化、块状卡片、Barlow Condensed 与 Noto Sans SC 字体组合。优化重点放在状态反馈、响应式、键盘操作、加载体验和视觉一致性，不进行无必要的整体重构。

### 3.3 前端按需加载，后端负责聚合

避免前端为了拼装一个页面请求大量明细接口。首页、我的比赛日、关注赛程等场景应由后端提供聚合接口。

### 3.4 所有后台任务必须可重复执行

同步和提醒任务需要满足幂等、可重试、可观测和多实例安全，不能依赖单进程假设。

### 3.5 所有任务都要有验收证据

每项任务完成时至少提供：

- 代码变更。
- 自动化测试或可重复的验证步骤。
- 构建与测试结果。
- 数据库变更说明。
- 部署与回滚说明（如涉及生产环境）。

## 4. 总体优先级

| 阶段 | 优先级 | 目标 | 推荐工作量 |
| --- | --- | --- | --- |
| M0 | P0 | 仓库与生产部署阻断问题 | 2～4 人日 |
| M1 | P0/P1 | 后台运营、可靠性和工程基础 | 10～16 人日 |
| M2 | P1 | 赛程、首页和世界杯核心体验 | 10～18 人日 |
| M3 | P1/P2 | 比赛中心与个人化观赛闭环 | 12～22 人日 |
| M4 | P2 | 国际化、无障碍、增长与性能深化 | 按需求排期 |

## 5. M0：仓库与生产部署修复

### REPO-01 清理 Git 跟踪产物和用户上传文件

**优先级：P0**

#### 问题

虽然根目录 `.gitignore` 已忽略 `node_modules/`、`dist/` 和 `uploads/`，但部分历史文件已经被 Git 跟踪，新增忽略规则不会自动移除它们。

#### 开发任务

1. 从 Git 索引取消跟踪：
   - `frontend/node_modules/`
   - `frontend/dist/`
   - `backend/uploads/`
2. 不删除开发者本地依赖和本地上传目录，仅取消版本控制跟踪。
3. 检查 7 个头像是否属于真实用户；如属于真实用户，评估是否需要清理 Git 历史。
4. 增加仓库扫描脚本，阻止依赖目录、构建产物、上传文件和密钥重新进入提交。
5. 在 README 中补充上传目录和构建产物管理规则。

#### 验收标准

- `git ls-files frontend/node_modules` 无输出。
- `git ls-files frontend/dist` 无输出。
- `git ls-files backend/uploads` 无输出。
- 重新执行 `npm install` 和 `npm run build` 不产生待提交文件。
- 仓库不包含 `.env`、私钥、Token 或真实用户上传内容。

### DEP-01 修复 Docker Compose 生产配置

**优先级：P0**

#### 问题

后端生产配置要求：

- `JWT_SECRET` 长度至少 32 位且不能使用默认值。
- 必须配置 `CORS_ALLOWED_ORIGINS`。

当前 Compose 配置不能满足上述约束。

#### 开发任务

1. 将密钥、数据库密码、CORS 域名等敏感项改为从根目录 `.env` 注入。
2. 新增根目录 `.env.example`，只保存占位值和说明。
3. Compose 不再内置可用于生产的固定密码。
4. 增加后端健康检查，并让前端依赖后端健康状态，而不是只依赖容器启动。
5. 验证全新环境执行 `docker compose up -d --build` 可以正常启动。

#### 建议环境变量

```env
MYSQL_ROOT_PASSWORD=replace_with_strong_password
JWT_SECRET=replace_with_at_least_32_random_characters
CORS_ALLOWED_ORIGINS=http://localhost:3000
FOOTBALL_DATA_API_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
```

#### 验收标准

- 默认占位密钥不能在生产环境启动。
- 合法 `.env` 配置可启动 MySQL、Redis、后端和前端。
- 前端可访问 `/api`。
- 浏览器无 CORS 错误。
- `.env` 不被 Git 跟踪。

### DEP-02 修复上传资源生产访问链路

**优先级：P0**

#### 开发任务

1. 在 `frontend/nginx.conf` 增加 `/uploads/` 反向代理。
2. 补充 `X-Forwarded-For`、`X-Forwarded-Proto` 等代理头。
3. Docker Compose 为后端上传目录增加持久化卷。
4. 明确单机与多实例方案：
   - 单机：Docker Volume。
   - 多实例：S3、MinIO、OSS 或 COS 对象存储。
5. 为上传文件设置缓存、安全响应头和合理的访问策略。

#### 验收标准

- 开发环境和 Docker 环境都能显示头像。
- 后端容器重建后头像仍存在。
- 非图片文件无法通过头像接口上传。
- 多实例方案中任意实例都能访问同一头像地址。

## 6. M1：后台运营与工程可靠性

### ADM-11 补齐球队管理 CRUD

**优先级：P0**

#### 功能范围

- 服务端分页、搜索和类型筛选。
- 新建国家队或俱乐部。
- 编辑名称、英文名、代码、队徽、大洲、小组和赛事归属。
- 删除前进行关联比赛检查。
- 批量导入和导出。
- 成功、失败、冲突和加载状态反馈。

#### 重点文件

- `frontend/src/pages/admin/AdminTeamsPage.vue`
- `frontend/src/api/admin.ts`
- `backend/internal/handlers/admin_handler.go`
- `backend/internal/services/admin_service.go`
- `backend/internal/repositories/admin_repo.go`

#### 验收标准

- 管理员不操作数据库即可完成球队维护。
- 重复代码返回 409，并在页面显示明确提示。
- 删除被比赛引用的球队时给出阻止原因。
- 所有写操作写入管理员审计日志。

### ADM-12 补齐比赛与积分管理

**优先级：P0**

#### 功能范围

- 新建和编辑比赛。
- 修改开球时间、主客队、场地、阶段、轮次、比分和状态。
- 对比分、状态流转增加二次确认。
- 支持批量导入预览、错误行提示和部分失败报告。
- 积分榜重算前展示影响范围，重算后展示差异。
- 支持人工修正，并保存操作原因。

#### 验收标准

- 管理员可以修复错误赛程、时间、比分和状态。
- 非法状态流转被后端拒绝。
- 比分调整后积分榜可重新计算。
- 所有高风险操作可追踪管理员、时间、对象和变更内容。

### ADM-13 增加同步与提醒运营视图

**优先级：P1**

#### 功能范围

- 同步历史列表与详情。
- 最近成功、失败、跳过和耗时统计。
- 失败原因与重试按钮。
- 数据源限流状态。
- 提醒待发送、发送中、成功、失败和重试数量。
- 按比赛、渠道和时间范围筛选。

#### 验收标准

- 同步失败不需要查服务器日志即可定位原因。
- 管理员可安全触发单赛事重试。
- 可以发现长期停留在 `sending` 状态的提醒。

### REL-08 修复提醒多实例原子认领

**优先级：P0**

#### 问题

当前实现先选出待发送 ID，批量更新为 `sending`，再按 ID 和 `sending` 状态查询。两个扫描器认领集合部分重叠时，后执行者可能查询到其他扫描器已经认领的记录。

#### 推荐方案

优先选择以下一种：

1. MySQL 事务配合 `SELECT ... FOR UPDATE SKIP LOCKED`。
2. 为提醒增加 `claim_token`、`claimed_at`、`worker_id`，更新后只查询本次 Token 对应记录。

同时增加：

- `sending` 超时自动回收。
- 指数退避与下次重试时间。
- 最终失败原因字段。
- 站内通知幂等键，避免重复通知。

#### 验收标准

- 两个或更多扫描器并发运行时，同一提醒只发送一次。
- Worker 异常退出后，超时任务可以被重新认领。
- 邮件失败不会重复生成站内通知。
- 并发测试覆盖完全重叠和部分重叠两种认领集合。

### DB-01 建立正式数据库迁移流程

**优先级：P1**

#### 开发任务

1. 选择并接入版本化迁移工具。
2. 将结构变更从生产 `AutoMigrate` 迁移到显式 SQL 或迁移文件。
3. 每个迁移包含升级说明；高风险变更包含回滚或恢复方案。
4. CI 在空库和历史基线库分别执行迁移测试。
5. 生产启动时不自动执行不可控结构变更。

#### 验收标准

- 空数据库可通过迁移创建完整结构。
- 历史数据库可连续升级到最新版本。
- 同一迁移不会重复执行。
- 失败迁移不会留下不可识别的半完成状态。

### OBS-04 健康检查、日志与优雅关闭

**优先级：P1**

#### 开发任务

- `GET /health/live`：仅表示进程存活。
- `GET /health/ready`：检查 MySQL、Redis 和必要初始化状态。
- 日志改为 JSON，包含 request ID、路径、状态码、耗时和用户/管理员 ID。
- 增加同步次数、同步失败、提醒积压、接口延迟等指标。
- 接收系统退出信号，停止接收请求并等待后台任务安全退出。
- 为同步器和提醒扫描器增加可取消的 Context。

#### 验收标准

- 编排平台能正确判断服务是否可接流量。
- 所有接口错误可通过 request ID 关联日志。
- 服务关闭时不会突然中断正在执行的数据库写入。
- 后台 Ticker 和 Sleep 循环能够停止。

### QA-06 建立 CI 与端到端测试

**优先级：P0/P1**

#### CI 必跑项

```text
前端：npm ci → typecheck → lint → test → build
后端：go test ./...
数据库：空库迁移 → 基线库升级
端到端：启动完整环境 → 执行核心流程
```

#### 核心 E2E 流程

1. 注册、登录、刷新 Token、退出。
2. 切换赛事并查看赛程和积分榜。
3. 关注球队、收藏比赛、创建和取消提醒。
4. 管理员登录、编辑比赛、重算积分。
5. 手动同步并查看同步结果。
6. 头像上传和访问。
7. 普通用户不能访问后台接口。

#### 验收标准

- 主分支合并前所有检查必须通过。
- ESLint 警告逐步收敛，最终 CI 使用 `--max-warnings=0`。
- 关键流程失败时保留截图、日志和网络记录。

## 7. M2：赛程、首页与世界杯核心体验

### DATA-09 动态赛季、轮次与筛选

**优先级：P1**

#### 问题

当前联赛赛程筛选硬编码第 11、12 轮，无法随赛季和数据自动变化。

#### 开发任务

1. 后端增加赛事元数据接口，返回：
   - 当前赛季。
   - 当前轮次。
   - 已有轮次范围。
   - 最近比赛日期。
2. 前端根据接口动态渲染轮次。
3. 增加日期、状态、球队、收藏/关注筛选。
4. 筛选状态同步到 URL Query，支持刷新和分享。
5. 世界杯保持小组赛、淘汰赛和日期视图。

#### 建议接口

```http
GET /api/competitions/:code/overview
GET /api/matches?competitionId=1&season=2025&matchday=12&cursor=...
```

#### 验收标准

- 不修改前端代码即可展示新轮次。
- 筛选刷新后保持。
- 后台新增赛事后前台能加载正确赛季和轮次。

### PERF-04 赛程增量加载

**优先级：P1**

#### 开发任务

- 将一次性并发拉取所有分页改为游标分页或逐页加载。
- 首屏只加载用户当前关注范围和附近日期。
- 增加加载更多、触底加载和返回顶部。
- 长列表达到阈值后评估虚拟滚动。
- 后端为赛事、开球时间、轮次和状态组合查询增加索引。

#### 验收标准

- 首次进入赛程页不请求全部赛季比赛。
- 列表中无重复、丢失和顺序跳动。
- 切换筛选时旧请求不会覆盖新结果。

### DATA-10 首页聚合接口

**优先级：P1**

#### 建议接口

```http
GET /api/home/overview?competition=PL
GET /api/me/matchday?competition=PL
```

#### 返回内容

- 焦点比赛。
- 今日/近期比赛。
- 赛事进度。
- 积分榜 Top 5。
- 关注球队近期赛程。
- 用户提醒和收藏摘要。
- 数据最后同步时间。

#### 验收标准

- 首页不再为每支关注球队分别请求赛程。
- 未登录和已登录返回结构稳定。
- 接口支持缓存，个性化数据与公共数据分层处理。
- 首页首屏请求数量显著下降。

### CUP-01 世界杯淘汰赛对阵树

**优先级：P1**

#### 功能范围

- 32 强/16 强到决赛的对阵路径展示，具体轮次以赛事配置为准。
- 待定席位、晋级球队、比赛状态和比分展示。
- 点击节点进入比赛详情。
- 移动端横向浏览或分轮次切换。
- 支持最佳第三名等世界杯晋级映射。

#### 数据设计

比赛需要能够表达：

- 所属淘汰赛轮次。
- 对阵位置或 Bracket Slot。
- 上游比赛来源。
- 胜者进入的下游 Slot。

#### 验收标准

- 后台更新比分和胜者后，对阵树自动更新。
- 尚未确定球队的比赛显示来源说明，而不是错误队名。
- 桌面和移动端均可完整浏览。

### DATA-11 数据可信度展示

**优先级：P1**

#### 功能范围

- 赛程和积分榜显示最近同步时间。
- 显示数据提供方。
- 演示数据明确标识。
- 同步延迟或失败时显示非阻断警告。
- 后台人工修改的数据标记更新时间和来源。

#### 验收标准

- 用户可以判断数据是否新鲜。
- 数据源不可用时保留最后一次成功数据。
- 不将同步失败误显示为“暂无比赛”。

## 8. M3：比赛中心与个人化观赛闭环

### MATCH-01 丰富比赛中心

**优先级：P1，依赖数据源能力**

#### 第一阶段

- 比分、状态、比赛分钟。
- 近期战绩。
- 双方历史交锋。
- 小组或联赛排名上下文。
- 收藏、提醒和分享。

#### 第二阶段

- 进球、助攻、黄红牌和换人事件流。
- 首发、替补、阵型。
- 控球率、射门、射正、角球等技术统计。
- 最佳球员与赛后数据。

#### 后端建议

- 抽象赛事数据 Provider 接口，避免业务层绑定单一供应商。
- 比赛基础数据与事件/阵容/统计分表保存。
- 对外部事件使用 `provider + external_id` 唯一键幂等写入。

#### 验收标准

- 数据源缺失某项能力时展示合理空状态。
- 直播刷新不闪屏，不清空最后一次成功数据。
- 同一事件不会因重复同步而重复展示。

### PERSONAL-01 我的比赛日

**优先级：P1**

#### 功能范围

- 聚合今日关注球队比赛。
- 聚合收藏比赛和已设置提醒。
- 根据时区生成个人赛程时间线。
- 关注球队自动提醒开关。
- 比赛改期后自动调整提醒。
- 开球、改期、取消和完场通知。

#### 验收标准

- 用户关注球队后无需逐场设置即可启用自动提醒。
- 自动提醒可按球队、赛事和渠道关闭。
- 比赛改期不会保留错误提醒时间。

### CAL-01 添加到日历

**优先级：P1/P2**

#### 功能范围

- 单场比赛下载 `.ics`。
- 关注球队完整赛程订阅。
- 支持 Apple Calendar、Google Calendar 和 Outlook。
- 比赛改期时更新日历事件 UID。

#### 验收标准

- 日历时间使用 UTC，并由客户端日历正确换算。
- 同一比赛重复添加不会产生多个不同 UID 的事件。
- 标题、场地、详情链接和提醒时间正确。

### PWA-01 PWA 与 Web Push

**优先级：P2**

#### 开发任务

- Web App Manifest。
- Service Worker 与静态资源缓存。
- 离线查看最近一次成功赛程。
- Push 订阅管理。
- VAPID 密钥和订阅表。
- 无权限、拒绝权限和订阅失效处理。

#### 验收标准

- 支持安装到桌面或手机主屏幕。
- 离线时不出现白屏。
- 用户明确授权后才创建 Push 订阅。
- 失效订阅可自动清理。

## 9. M4：体验、国际化与增长

### A11Y-04 完成键盘和读屏支持

**优先级：P1/P2**

#### 开发任务

- 将可点击比赛卡改为链接或按钮语义。
- 所有交互元素具备可见焦点样式。
- 赛事选择弹窗实现焦点锁定、初始焦点和关闭后焦点恢复。
- 弹窗打开时背景不可被读屏和键盘访问。
- 状态不只依赖颜色表达。
- 表单错误与字段建立关联。
- 使用 `aria-live` 通知异步操作结果。

#### 验收标准

- 仅使用键盘可完成赛事切换、查看比赛、收藏和提醒。
- 弹窗焦点不会进入背景页面。
- 主流读屏工具能读出比分、状态和按钮用途。

### UI-01 加载、空状态与动效统一

**优先级：P2**

#### 开发任务

- 首屏和列表使用骨架屏，避免布局跳动。
- 区分加载失败、暂无数据、筛选无结果和同步延迟。
- 所有失败状态提供重试入口。
- 统一按钮 loading、disabled、success 和 error 状态。
- 增加 `prefers-reduced-motion`，关闭非必要过渡和背景动效。
- 在 375、768、1024、1440 像素宽度回归。

### I18N-01 完成国际化决策

**优先级：P2**

#### 方案 A：真正支持英文

- 接入 Vue I18n。
- 抽取页面、状态、错误和日期文本。
- 后端返回稳定状态码，前端按语言翻译。
- 球队、城市和球场使用中英文数据字段。

#### 方案 B：暂不支持英文

- 隐藏个人中心语言选择。
- 保留数据库字段，为后续版本预留。

#### 验收标准

- 不允许出现“设置已保存但界面语言不变化”的伪功能。

### GROWTH-01 分享与预测

**优先级：P2，最后实施**

可选功能：

- 比赛分享海报。
- 球队赛程分享卡。
- 比分预测与积分榜。
- 好友或小组竞猜。
- 赛事结果回顾。

实施前必须先明确登录体系、反作弊、内容审核、结算规则和活动运营责任。

## 10. 性能优化要求

### 10.1 前端

- 移除未使用的 Element Plus 和图标依赖，确认无引用后更新锁文件。
- 保留路由级代码分割。
- 对字体、队徽和静态资源配置长期缓存。
- 评估 Noto Sans SC 字体分片的实际网络加载体积。
- 图片设置宽高，避免累计布局偏移。
- 建立首屏 JS、CSS、字体和图片性能预算。

### 10.2 后端

- 为常用比赛查询建立组合索引。
- 对赛事概览、积分榜和公共首页模块使用 Redis 缓存。
- 写操作后精确失效对应赛事缓存。
- 避免按球队循环请求和循环查询。
- 对公开列表设置最大分页大小。
- 慢查询记录查询耗时和筛选条件，但不记录敏感数据。

## 11. 安全优化要求

- 生产密钥全部通过环境变量或密钥管理服务注入。
- Refresh Token 继续保持轮换和重放检测。
- 评估将长期 Token 从 `localStorage` 迁移到 HttpOnly、Secure、SameSite Cookie。
- 对登录、注册、刷新、上传、手动同步增加合理限流。
- 上传文件校验 MIME、文件头、尺寸和重新编码结果。
- 管理员高风险操作要求二次确认并记录审计日志。
- 增加依赖漏洞扫描和敏感信息扫描。
- CORS 只允许明确配置的前端域名。
- Nginx 增加 CSP、`X-Content-Type-Options`、`Referrer-Policy` 等安全响应头。

## 12. 推荐实施顺序

### 批次 1：立即修复

1. `REPO-01` 仓库跟踪清理。
2. `DEP-01` Docker 配置修复。
3. `DEP-02` 上传资源代理和持久化。
4. `REL-08` 提醒原子认领修复。

### 批次 2：上线工程基础

1. `QA-06` CI 基础流程。
2. `OBS-04` 健康检查与优雅关闭。
3. `DB-01` 正式数据库迁移。
4. ESLint 警告清理。

### 批次 3：后台运营闭环

1. `ADM-11` 球队管理 CRUD。
2. `ADM-12` 比赛和积分管理。
3. `ADM-13` 同步与提醒运营视图。

### 批次 4：核心用户体验

1. `DATA-09` 动态赛季和轮次。
2. `PERF-04` 赛程增量加载。
3. `DATA-10` 首页聚合接口。
4. `DATA-11` 数据可信度展示。
5. `CUP-01` 淘汰赛对阵树。

### 批次 5：个人化观赛助手

1. `MATCH-01` 丰富比赛中心。
2. `PERSONAL-01` 我的比赛日。
3. `CAL-01` 添加到日历。
4. `PWA-01` PWA 与 Web Push。

### 批次 6：体验和增长

1. `A11Y-04` 无障碍。
2. `UI-01` 状态和动效统一。
3. `I18N-01` 国际化。
4. `GROWTH-01` 分享与预测。

## 13. 每个任务的完成定义

一个任务只有同时满足以下条件才可标记为完成：

### 代码

- 功能已实现且不存在仅前端隐藏的权限控制。
- 无新增 TypeScript、ESLint 或 Go 编译问题。
- 无调试日志、硬编码密钥和临时 Mock 遗留。

### 测试

- 新增或更新单元测试。
- 核心用户流程有集成测试或 E2E 覆盖。
- 前端构建、测试、Lint 和后端测试通过。

### 数据

- 数据库变更有版本化迁移。
- 写操作具备幂等、唯一约束或事务保护。
- 有生产数据兼容和回滚说明。

### 体验

- 包含 loading、empty、error、disabled 和 success 状态。
- 支持移动端和键盘操作。
- 不因轮询和异步请求出现闪屏或旧数据覆盖。

### 文档

- API、环境变量和部署说明已更新。
- 任务状态和实际代码一致。
- 验收结果记录具体命令和结果。

## 14. 发布前总体验收清单

### 仓库与安全

- [ ] Git 不跟踪 `node_modules`、`dist`、`uploads` 和 `.env`。
- [ ] 不包含真实用户头像和敏感配置。
- [ ] 生产 JWT、数据库和 SMTP 密钥不使用默认值。
- [ ] CORS 和安全响应头配置正确。

### 部署

- [ ] 全新环境可通过 Docker Compose 启动。
- [ ] `/health/live` 和 `/health/ready` 正常。
- [ ] `/api` 和 `/uploads` 代理正常。
- [ ] 数据库迁移可重复执行。
- [ ] 服务可以优雅关闭。

### 功能

- [ ] 多赛事切换、赛程、球队和积分榜正常。
- [ ] 收藏、关注、提醒和通知形成完整闭环。
- [ ] 管理员可以维护球队、比赛、比分和状态。
- [ ] 同步失败可以查看原因和重试。
- [ ] 世界杯淘汰赛路径正确。

### 质量

- [ ] 前端 typecheck、lint、test、build 全部通过。
- [ ] 后端测试全部通过。
- [ ] 核心 E2E 流程通过。
- [ ] ESLint 使用 `--max-warnings=0`。
- [ ] CI 阻止未通过检查的代码合并。

### 体验

- [ ] 375、768、1024、1440 像素布局通过回归。
- [ ] 键盘可完成核心操作。
- [ ] 弹窗焦点管理正确。
- [ ] 减弱动效设置生效。
- [ ] 弱网和接口失败状态可恢复。

## 15. 最终目标

优化完成后，WorldCup Mate 应达到以下状态：

1. 可以通过标准化配置稳定部署到生产环境。
2. 仓库不包含依赖产物、用户文件和敏感信息。
3. 管理员不操作数据库即可完成日常赛事运营。
4. 赛程、同步和提醒在多实例环境下保持正确可靠。
5. 用户可以围绕关注球队形成“查看—收藏—提醒—回访”的完整闭环。
6. 世界杯用户可以通过淘汰赛对阵树直观理解晋级路径。
7. 比赛详情从基础比分页升级为真正的比赛中心。
8. 项目具备 CI、E2E、健康检查、日志、指标和回滚能力。

在资源有限的情况下，优先完成 M0、M1 和 M2；预测、排行榜等增长玩法应在数据正确性、提醒可靠性和生产工程基础稳定后再实施。
