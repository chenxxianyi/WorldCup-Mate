# WorldCup Mate 球队球员大名单开发方案

## 1. 需求目标

在用户点击进入球队详情页时，除现有球队基础信息、赛程和小组积分外，展示该球队的世界杯球员大名单。

第一版核心字段：

- 球员名字
- 球衣号码
- 球员照片
- 球员位置

建议第一版以“名单可稳定展示”为优先级。球员照片容易受数据源授权、缺失、限流和付费影响，因此照片字段允许为空，前端提供兜底头像。

## 2. 当前项目现状

当前球队详情页位于：

```text
frontend/src/pages/user/TeamDetailPage.vue
```

当前球队详情页已经展示：

- 球队 Hero 信息
- 关注球队按钮
- 下一场比赛
- 球队赛程
- 所在小组积分

后端当前球队模型位于：

```text
backend/internal/models/team.go
```

当前 `Team` 模型只包含球队维度字段，例如名称、英文名、FIFA code、旗帜、洲、分组、简介和教练。项目尚未有独立球员模型、球员表和球队球员接口。

## 3. 数据源判断

截至 2026-06-05，2026 美加墨世界杯最终大名单已经公开，FIFA 官方已发布 48 队、1248 名球员名单。官方名单适合作为权威基础数据来源。

但需要注意：

- 官方名单通常适合提供球员姓名、球队、号码、位置、俱乐部等基础字段。
- 球员照片通常不是官方名单的稳定字段。
- 带照片的球员数据一般需要第三方 API、商业体育数据源，或自行维护图片。

可考虑的数据源：

- FIFA 官方名单：适合做权威基础名单来源。
- football-data.org：部分 Team 资源包含 squad 字段，适合补充姓名和位置，但通常不包含照片和球衣号码。
- BALLDONTLIE World Cup API：提供 World Cup players、rosters、lineups 等接口，可能适合作为同步源，但需要 API Key。
- API-Football / SportMonks 等商业体育数据源：更可能提供照片，但需要评估费用、字段完整度和授权。

推荐策略：

1. 第一版使用官方名单或可信汇总导入基础名单。
2. 数据库保留 `photo_url` 字段，但允许为空。
3. 前端对缺失照片使用姓名首字母或默认头像兜底。
4. 后续接入第三方数据源补充照片和更多字段。

## 4. 数据模型设计

建议新增独立 `players` 表，不要把球员名单塞入 `teams` 表。

后端新增模型：

```go
type Player struct {
	ID             uint           `gorm:"primaryKey" json:"id"`
	TeamID         uint           `gorm:"index;not null" json:"team_id"`
	Team           Team           `gorm:"foreignKey:TeamID" json:"team,omitempty"`
	Name           string         `gorm:"size:100;not null;index" json:"name"`
	NameEn         string         `gorm:"size:100;index" json:"name_en"`
	ShirtNumber    int            `gorm:"index" json:"shirt_number"`
	Position       string         `gorm:"size:20;index" json:"position"`
	PositionLabel  string         `gorm:"size:20" json:"position_label"`
	PhotoURL       string         `gorm:"size:500" json:"photo_url"`
	Club           string         `gorm:"size:100" json:"club"`
	Source         string         `gorm:"size:50;index" json:"source"`
	SourcePlayerID string         `gorm:"size:100;index" json:"source_player_id"`
	CreatedAt      time.Time      `json:"created_at"`
	UpdatedAt      time.Time      `json:"updated_at"`
	DeletedAt      gorm.DeletedAt `gorm:"index" json:"-"`
}
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `team_id` | 关联球队 |
| `name` | 球员展示名 |
| `name_en` | 英文名或原始名 |
| `shirt_number` | 球衣号码 |
| `position` | 标准位置编码，如 `GK`、`DF`、`MF`、`FW` |
| `position_label` | 中文位置，如门将、后卫、中场、前锋 |
| `photo_url` | 球员照片 URL，允许为空 |
| `club` | 所属俱乐部，可选 |
| `source` | 数据来源，如 `fifa_official`、`balldontlie` |
| `source_player_id` | 来源系统里的球员 ID，用于去重和同步 |

建议增加唯一索引：

```text
team_id + shirt_number
```

或者：

```text
team_id + name_en
```

实际使用哪种取决于数据源质量。若球衣号码稳定，优先 `team_id + shirt_number`。

## 5. 后端接口设计

新增接口：

```http
GET /api/teams/:id/players
```

返回示例：

```json
[
  {
    "id": 1,
    "team_id": 10,
    "name": "Kylian Mbappe",
    "name_en": "Kylian Mbappe",
    "shirt_number": 10,
    "position": "FW",
    "position_label": "前锋",
    "photo_url": "https://example.com/player.jpg",
    "club": "Real Madrid"
  }
]
```

后端建议新增文件：

```text
backend/internal/models/player.go
backend/internal/repositories/player_repo.go
backend/internal/services/player_service.go
backend/internal/handlers/player_handler.go
```

也可以将 handler 临时放在 `team_handler.go` 中，第一版保持接口少而清晰。

服务层建议方法：

```go
func ListPlayersByTeam(teamID uint) ([]models.Player, error)
```

路由建议：

```go
public.GET("/teams/:id/players", handlers.GetTeamPlayers)
```

## 6. 数据导入方案

第一版建议使用离线导入，避免页面上线依赖外部 API。

可选导入方式：

1. SQL seed：直接维护 `players` 插入语句。
2. JSON 导入：维护一份 `data/players_2026.json`，启动或管理脚本导入。
3. 后台同步任务：后续从第三方 API 拉取并 upsert。

推荐第一版：

```text
backend/internal/database/seed_players.go
```

或者单独脚本：

```text
backend/cmd/import_players/main.go
```

JSON 结构示例：

```json
[
  {
    "team_code": "FRA",
    "name": "Kylian Mbappe",
    "name_en": "Kylian Mbappe",
    "shirt_number": 10,
    "position": "FW",
    "position_label": "前锋",
    "photo_url": "",
    "club": "Real Madrid"
  }
]
```

导入时通过 `team_code` 匹配 `teams.fifa_code`。

## 7. 前端类型和 API

新增类型文件：

```text
frontend/src/types/player.ts
```

类型定义：

```ts
export interface Player {
  id: number
  team_id: number
  name: string
  name_en?: string
  shirt_number?: number
  position: 'GK' | 'DF' | 'MF' | 'FW' | string
  position_label: string
  photo_url?: string
  club?: string
}
```

新增 API：

```text
frontend/src/api/players.ts
```

或者扩展现有：

```text
frontend/src/api/teams.ts
```

建议方法：

```ts
export function apiGetTeamPlayers(teamId: number) {
  return request.get<Player[]>(`/teams/${teamId}/players`)
}
```

## 8. 前端展示设计

建议在球队详情页中新增 `PlayerRoster` 组件：

```text
frontend/src/components/common/PlayerRoster.vue
```

放置位置：

```text
球队 Hero
球员大名单
下一场比赛
球队赛程
所在小组积分
```

设计原则：

- 移动端优先，列表要紧凑可扫读。
- 不使用大面积卡片堆叠，避免页面过长。
- 按位置提供筛选：全部、门将、后卫、中场、前锋。
- 球员照片缺失时显示兜底头像。
- 球衣号码要醒目，但不要压过姓名。

组件结构建议：

```vue
<section class="section roster-section">
  <div class="section-head">
    <h2>球员大名单</h2>
    <span>{{ players.length }} 人</span>
  </div>

  <div class="position-tabs">
    <button>全部</button>
    <button>门将</button>
    <button>后卫</button>
    <button>中场</button>
    <button>前锋</button>
  </div>

  <div class="player-list">
    <article class="player-row">
      <img />
      <strong>球员名</strong>
      <span>#10</span>
      <span>前锋</span>
    </article>
  </div>
</section>
```

移动端单行展示建议：

```text
[头像]  #10  Kylian Mbappe
        前锋 · Real Madrid
```

桌面端可以两列：

```css
.player-list {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
}
```

## 9. 空状态和异常状态

球员名单加载失败：

```text
球员名单加载失败，请稍后再试
```

球员名单为空：

```text
球员大名单暂未同步
```

照片为空：

- 使用姓名首字母
- 或显示默认 person 图标
- 不要使用破图

## 10. 开发步骤

### 阶段 1：后端最小闭环

1. 新增 `Player` 模型。
2. 将 `Player` 加入 `AutoMigrate`。
3. 新增 player repository。
4. 新增 player service。
5. 新增 `GET /api/teams/:id/players` 接口。
6. 准备少量测试数据，至少覆盖 1-2 支球队。
7. 用接口确认可返回名单。

### 阶段 2：前端展示

1. 新增 `types/player.ts`。
2. 新增 `apiGetTeamPlayers`。
3. `TeamDetailPage.vue` 中加载球队详情时并行加载球员。
4. 新增 `PlayerRoster.vue`。
5. 在球队详情页插入 `PlayerRoster`。
6. 实现位置筛选、照片兜底、空状态和 loading 状态。

### 阶段 3：数据完善

1. 整理 48 队大名单 JSON。
2. 导入姓名、号码、位置、俱乐部。
3. 对照片字段先留空或使用可信照片源补充。
4. 增加 `source` 和 `source_player_id`，方便后续同步。

### 阶段 4：体验增强

1. 增加按号码排序和按位置分组。
2. 增加队长标识。
3. 增加球员详情弹层。
4. 增加 AI 阵容解读入口。

## 11. 验收标准

功能验收：

- 进入球队详情页能看到球员大名单区域。
- 每个球员展示名字、号码、位置。
- 有照片时展示照片，没有照片时展示兜底头像。
- 可以按位置筛选。
- 没有名单时不白屏，有明确空状态。
- 接口失败时不影响球队基础信息、赛程和积分展示。

技术验收：

- `npm run build` 通过。
- `go test ./...` 通过，或明确记录现有失败项。
- 球员接口返回结构稳定。
- 数据模型支持后续导入和同步。

## 12. 推荐优先级

P0：

- 新增球员表。
- 新增球队球员接口。
- 球队详情页展示球员名单。
- 照片缺失时前端兜底。

P1：

- 接入照片数据源。
- 增加位置筛选。
- 增加俱乐部展示。

P2：

- 增加球员详情弹层。
- 增加队长、年龄、身高等字段。
- 增加 AI 阵容解读。

## 13. 结论

这个功能建议按“数据可用性优先”的方式推进。先把球队大名单作为稳定列表展示出来，让球队详情页具备更强的世界杯资料价值；照片作为可选增强字段，不阻塞第一版上线。

第一版完成后，球队详情页的信息结构会变成：

```text
球队信息
球员大名单
下一场比赛
球队赛程
小组积分
```

这能明显提升球队详情页的完整度，也为后续 AI 看点、阵容分析和球员卡片功能打好基础。
