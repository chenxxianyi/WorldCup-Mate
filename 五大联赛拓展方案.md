# 五大联赛拓展方案（WorldCup Mate → 足球赛事平台）

> 目标：在现有世界杯应用基础上，**以纯新增方式**拓展为可同时承载世界杯与五大联赛（英超、西甲、德甲、意甲、法甲）的足球赛事平台。
> 状态：方案评审稿。

---

## 一、背景与现状

### 1.1 项目简介

WorldCup Mate 是一个面向 2026 世界杯的赛程助手与赛事数据管理项目：

- **前端**：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus + Tailwind CSS 4
- **后端**：Go 1.23 + Gin + GORM，MySQL 8 + Redis 7
- **数据同步**：football-data.org（当前仅同步 WC 2026）
- **用户能力**：JWT 认证、关注球队、收藏比赛、比赛提醒（站内通知 + 邮件）

### 1.2 现状盘点

**可直接复用的能力（与赛事无关）：**

- 前后端分层架构、REST API 风格、统一响应结构 `{code, message, data}`
- 用户体系（注册/登录/JWT/资料/头像）
- 关注球队、收藏比赛、比赛提醒、站内通知
- 数据同步框架（provider 抽象、SyncState 状态机、定时任务调度）
- 管理后台骨架（admin 路由 + CRUD handler）

**世界杯专属能力（本次一律保留，不做任何改动）：**

| 模块 | 说明 |
|---|---|
| 小组赛/淘汰赛赛制 | `Match.GroupID`、`Stage`（group/round_of_16/…）、`MatchNo` |
| 国家队模型 | `Team.FIFACode` 唯一索引、`Continent`、`GroupID` |
| 小组积分榜 | `GroupStanding` 表 + 晋级状态（qualified/possible/eliminated） |
| 最佳第三名 | `RecalculateBestThird` 逻辑 + `/api/standings/best-third` |
| 主办城市/球场 | `City`/`Stadium` 与比赛关联 |
| 前端世界杯模式 | 小组/淘汰赛筛选、所在小组积分、"48 支球队"等页面 |

---

## 二、扩展原则（Additive-first，兼容性优先）

本次扩展遵循以下硬性原则，**任何改动违反任一原则即不采纳**：

1. **现有表结构与数据零改动**：`matches`、`teams`、`groups`、`group_standings`、`cities`、`stadiums`、`users`、`user_favorite_*`、`reminders`、`notifications`、`sync_states` 等现有表的**列、索引、唯一约束一律不动**；现有数据一行不修改。只允许两类操作：
   - **新增表**（如 `competitions`、`league_standings`）
   - **现有表新增可空列**（如 `matches` 加 `competition_id`、`season`、`matchday`，均为可空、带默认值，NULL 即视为"世界杯/历史数据"）
2. **世界杯功能零改动**：小组积分、最佳第三名、淘汰赛流程、seed 数据、WC 同步路径全部保持原样，只在其外围叠加联赛分支。
3. **API 向后兼容**：所有新查询参数（`competition_id`、`season`、`matchday`）均为可选；**不带参数时行为与现在完全一致**（默认返回世界杯数据），老客户端零感知。
4. **前端默认仍为世界杯**：首次进入应用看到的是与现在完全一致的界面；赛事切换器是新增入口，世界杯页面代码保留不删。
5. **失败可回退**：任何阶段可一键回退到纯世界杯版本（撤掉新表新列即可，老代码路径未被改写）。

---

## 三、可行性结论

**可行，且成本可控。** 理由：

1. 底层架构干净：分层清晰（handler → service → repository → model），同步框架可扩展，用户功能通用。
2. **数据源无需更换、无需付费**：已验证 football-data.org 免费档（Free Tier）完整覆盖五大联赛 + 世界杯：

   | 联赛代码 | 联赛 | 免费档 |
   |---|---|---|
   | `PL` | 英超 Premier League | ✅ |
   | `PD` | 西甲 La Liga (Primera Division) | ✅ |
   | `BL1` | 德甲 Bundesliga | ✅ |
   | `SA` | 意甲 Serie A | ✅ |
   | `FL1` | 法甲 Ligue 1 | ✅ |
   | `WC` | 世界杯 World Cup | ✅ |

   官方还提供 **Standings 子资源**（TOTAL / HOME / AWAY 三张积分榜）与 **Scorers 子资源**（射手榜），联赛积分无需自算。
3. 核心工作量集中在"新增赛事（Competition + Season）维度并叠加联赛数据路径"，不触碰现有世界杯链路，预计 **5~8 周（1 人全职）** 完成 P0。

---

## 四、技术方案

### 4.1 新增表（纯新增，不动任何现有表）

**① `competitions` 表（赛事元数据）**

| 列 | 说明 |
|---|---|
| `id` | 主键 |
| `code` | 唯一，如 `PL`/`PD`/`BL1`/`SA`/`FL1`/`WC` |
| `name` / `name_en` | 中文名/英文名（如 英超 / Premier League） |
| `country` | 所属国家/地区（世界杯 = World） |
| `logo_url` | 赛事 logo |
| `format` | `league`（联赛）/ `cup`（杯赛，世界杯属此类） |
| `season` | 当前赛季起始年（int）：2026 世界杯 = `2026`；2025-26 英超 = `2025` |
| `status` | 启用/停用 |
| `sort_order` | 前端切换器排序 |

**② `league_standings` 表（联赛积分榜，独立于 `group_standings`）**

| 列 | 说明 |
|---|---|
| `id` | 主键 |
| `competition_id` | 关联 `competitions` |
| `season` | 赛季起始年 |
| `team_id` | 关联 `teams`（俱乐部也存 `teams` 表，见 4.2） |
| `type` | `total` / `home` / `away`（官方三张榜） |
| `position` | 排名 |
| `played/won/drawn/lost` | 场次/胜/平/负 |
| `goals_for/goals_against/goal_difference` | 进/失/净胜 |
| `points` | 积分 |
| `zone` | 分区标记：`champions_league` / `europa_league` / `relegation` 等（按位置规则计算，各联赛配置化） |
| `updated_at` | 更新时间 |

**③ （可选，P2 再建）`players`、`scorers` 表**：射手榜/球员数据，后续迭代。

### 4.2 现有表增强（仅新增可空列，原有列/索引/约束/数据全部不动）

**① `matches` 表 — 新增 3 个可空列**

- `competition_id`（可空，NULL = 世界杯/历史数据）
- `season`（可空 int）
- `matchday`（可空 int，联赛轮次）

> `GroupID`、`Stage`、`MatchNo`、`StadiumID`/`CityID` 等现有列**原样保留**；联赛比赛的 `group_id`/`stadium_id`/`city_id` 为 NULL，不强制外键。

**② `teams` 表 — 新增 4 个可空列**

- `external_code`（football-data 的 tla，如 `ARS`，俱乐部唯一键）
- `team_type`（`national` / `club`，默认 `national`）
- `country`（俱乐部所在国家）
- `venue`（俱乐部主球场）

> **`fifa_code` 唯一索引原样保留**：俱乐部该列存 NULL（MySQL 唯一索引允许多个 NULL），不会与 48 支国家队冲突；国家队数据零影响。

**③ 其余表（`groups`、`group_standings`、`cities`、`stadiums`、`sync_states`、用户相关表）一行不改**。

> `sync_states` 的 `resource` 列支持字符串（如 `matches` / `matches:PL`），联赛同步状态作为**新行**写入，无需改表结构。

### 4.3 后端改造（叠加式，不改写现有路径）

1. **同步服务**（`backend/internal/services/sync_service.go`）
   - **现有 WC 同步函数与调度逻辑原样保留**；
   - 新增"联赛同步"函数：遍历配置的联赛列表 `[{code: PL, season: 2025}, {code: PD, ...}, {code: BL1, ...}, {code: SA, ...}, {code: FL1, ...}]`，调用 `CompetitionMatches(ctx, code, season)`；
   - 联赛同步写入时使用 `matches` 表新增列 + 俱乐部走 `teams` 新列，**不触碰** `group_standings`/`best_third`；
   - 每个赛事独立 `sync_state`（resource = `matches:PL` 等新行）；
   - 新增环境变量 `SYNC_COMPETITIONS`（格式 `PL:2025,PD:2025,BL1:2025,SA:2025,FL1:2025`），缺省为空 = 只同步 WC（行为与现在一致）。
2. **联赛积分榜**（新增 `backend/internal/services/league_standing_service.go`）
   - 同步官方 Standings（TOTAL/HOME/AWAY）写入 `league_standings`；
   - `zone` 分区按位置规则计算（英超：1-4 欧冠、5 欧联、18-20 降级；各联赛规则配置化，德甲/意甲/法甲各有差异）；
   - 新增 `RecalculateLeagueStanding(competitionID, season)` 供管理端手动重算（积分→净胜球→进球）；
   - **`RecalculateGroupStanding` / `RecalculateBestThird` 零改动**。
3. **API 层**
   - 新增 `GET /api/competitions`（赛事列表，供前端切换器）；
   - 现有接口（matches/teams/standings/groups）**只增加可选查询参数** `competition_id`、`season`、`matchday`，缺省时走原逻辑返回世界杯数据；
   - 新增 `GET /api/competitions/:code/standings`（联赛积分榜，走 `league_standings`）；
   - 路由结构不变（`router.go` 增补）。
4. **管理后台**：新增赛事管理接口与联赛积分榜查看/重算入口；现有 admin CRUD 不动，比赛/球队管理页增加赛事筛选下拉（可选参数）。

### 4.4 前端改造（世界杯模式完整保留，叠加联赛模式）

1. **全局赛事切换**
   - 新增 `useCompetitionStore`（Pinia），localStorage 持久化（key `wm-competition`）；
   - **默认赛事 = 世界杯**：首次进入界面与现在完全一致；
   - TopBar 品牌区新增赛事切换器（世界杯 / 英超 / 西甲 / 德甲 / 意甲 / 法甲）；
   - `request.ts` 拦截器：仅当用户切换了赛事时才附加 `competition_id` 参数（世界杯 = 不带参数，兼容旧后端）。
2. **页面双模式**（世界杯分支保留原代码，仅新增联赛分支）
   - 赛程页：世界杯 = 现有小组/淘汰赛筛选；联赛 = 轮次（第 N 轮）筛选
   - 积分榜页：世界杯 = 现有小组榜 + 最佳第三名；联赛 = 20 队单表（欧战/降级区标记、主客场切换）
   - 球队页：世界杯 = 现有国家队视图；联赛 = 按赛事筛选的俱乐部（国家、主球场）
   - 比赛详情 / 首页：世界杯 = 现有内容；联赛 = 轮次、联赛排名等
3. **文案处理**：世界杯相关文案（如"2026 世界杯赛程助手"）作为**默认态保留**，仅在切到联赛时动态替换品牌区标题；业务文案按"当前赛事"渲染，不删除世界杯文案分支。
4. **队徽资源**：现有 `src/assets/team-icons/`（48 支国家队）不动；五大联赛 ~98 支俱乐部队徽新增到 `src/assets/club-icons/`（或 football-data `crest` 远程 URL + 本地缓存），`TeamFlag.vue` 扩展 code→图片映射（国家队查老表，俱乐部查新表）。

### 4.5 兼容与回退

- **零数据迁移**：不回溯修改任何老数据；`competitions` 表仅新增一条 `WC` 元数据记录（可选，用于切换器展示），老表老数据原封不动；
- **回退方案**：联赛代码全部收敛在新表/新列/新文件（新增 service、新增 handler、新增 store），回退 = 停用 `SYNC_COMPETITIONS` + 前端默认赛事保持世界杯，即可回到纯世界杯版本；
- 顺带修复 README 记录的 2 个 TS 类型错误（`src/data/mockUser.ts`、`src/pages/user/ProfilePage.vue`），恢复 `npm run build` 通过（仅修类型，不改功能）。

### 4.6 运维与限速

- football-data 免费档限速 **10 次/分钟**（标准版 49€/年可提升至 30/min，非必需）；
- 单个联赛全季约 380 场（约 10+ 次分页请求），5 个联赛初始全量同步约 50~60 次请求（6~10 分钟），需错峰调度；日常增量仅拉取比赛日当天数据；
- 联赛为跨年赛季（如 2025-26），`season` 参数按起始年传。

---

## 五、产品方案

### 5.1 产品定位

从"2026 世界杯赛程助手"升级为**一站式足球赛事伴侣**：跨赛事看赛程、查积分、设提醒、跟球队。**世界杯功能与数据完整保留**（作为历史赛事与默认首页），五大联赛作为新增内容叠加。

核心差异化不是数据，而是现有两个能力：

1. **比赛提醒**（站内通知 + 邮件，可拓展到微信推送）
2. **关注球队 / 收藏比赛跨赛事追踪**

### 5.2 信息架构（改动最小方案）

保持现有 5 个 Tab 不变，只叠加一层全局"赛事切换"：

```
[首页] [赛程] [球队] [积分榜] [我的]
        ↕ 新增：全局赛事切换器（默认=世界杯；可选 英超/西甲/德甲/意甲/法甲）
```

| 页面 | 世界杯模式（现有，原样保留） | 联赛模式（新增） |
|---|---|---|
| 首页 | 现有内容（默认展示） | 当前赛事倒计时/今日比赛/热门推荐 + 跨赛事"今日焦点"聚合 |
| 赛程 | 小组/淘汰赛筛选 | 日期 + 轮次（第 N 轮）筛选 |
| 球队 | 按大洲/小组筛选的国家队 | 按联赛筛选的俱乐部（国家、球场、近期战绩） |
| 积分榜 | 小组积分榜 + 最佳第三名 | 20 队单表 + 欧战/降级区标记 + 主客场切换 |
| 比赛详情 | 现有（含所在小组积分） | 第 N 轮、双方近况、焦点战标签 |

### 5.3 功能优先级

| 优先级 | 功能 |
|---|---|
| **P0（必须）** | 赛事切换（默认世界杯）、五大联赛赛程/比分/状态同步、联赛积分榜、轮次视图 |
| **P1（增强）** | 跨赛事今日聚合、关注球队跨赛事追踪（收藏列表按赛事分组）、管理后台赛事管理、俱乐部队徽 |
| **P2（差异化）** | 射手榜/助攻榜（官方 Scorers 接口免费提供）、比赛提醒扩展微信/钉钉、预测竞猜、历史赛季回看 |
| **P3（远期）** | 欧冠/欧联、球员页面、球队新闻、社区 |

### 5.4 里程碑

| 阶段 | 内容 | 周期 |
|---|---|---|
| M0 | 基线修复（构建恢复、环境就绪） | 0.5 天 |
| M1 | 新增表与可空列（纯新增）+ 赛事元数据 + 兼容性验收 | 1~2 周 |
| M2 | 联赛同步 + 联赛积分榜 + 管理后台（世界杯路径零改动） | 1~2 周 |
| M3 | 前端赛事切换 + 联赛页面叠加 + 默认世界杯 | 2~3 周 |
| M4 | 今日聚合、关注分组、打磨 + 上线 | 1 周 |

---

## 六、风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 数据源限速（10 req/min） | 中 | 初始全量错峰调度（6~10 分钟完成），日常增量请求量极小 |
| 西甲数据可用性 | 低 | 已验证 PD 在免费档内，无需付费 |
| 联赛与国家队 tla 撞车 | 低 | 俱乐部用 `external_code` 新列 + `team_type` 区分，`fifa_code` 唯一索引不参与俱乐部 |
| 联赛积分排序规则差异 | 低 | 先统一规则（积分/净胜球/进球），后续逐联赛微调 `zone` 配置 |
| 现有表加列影响老代码 | 低 | 全部新增列可空、有默认值；GORM `AutoMigrate` 只增不删；M1 验收含表结构 diff 对比 |
| 五大联赛 logo/队徽版权 | 低 | 非商业用途无碍；商业化前需确认授权 |

---

## 七、结论

底层架构干净、数据源免费且覆盖完整、用户功能通用，**以纯新增方式拓展五大联赛完全可行**。现有世界杯功能与数据表**一行不改、一点不动**，联赛能力全部叠加在新表/新列/新代码路径上，任何阶段可回退。预计 5~8 周完成 P0。执行细节见《五大联赛拓展开发计划.md》。
